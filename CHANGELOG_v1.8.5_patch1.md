# Исправления в версии 1.8.5 (патч 1)

## 🔧 Исправленные ошибки:

### 1. Классификация оптических компонентов (КРИТИЧЕСКОЕ)
**Проблема:** Коммутатор оптический (например, "NSSW-1232213131-D100") попадал в категорию "Отладочные платы" вместо "Оптические компоненты".

**Исправление:**
- ✅ **КРИТИЧНО:** Проверка оптических компонентов перемещена ВЫШЕ проверки кабелей
- ✅ Любой компонент с "оптическ" или "optical" теперь ВСЕГДА попадает в "Оптические компоненты"
- ✅ Добавлены ключевые слова: "коммутатор оптический", "кабель оптический", "optical cable", "optical switch"
- ✅ Порядок проверки: наши разработки → резисторы/конденсаторы → **оптические компоненты** → кабели → остальное

**Примеры:**
- "Кабель соединительный оптический FC/APC-FC/APC-1,0" → **optics** ✅
- "Коммутатор оптический NSSW-123" → **optics** ✅
- "Кабель Ethernet RJ45" → **cables** ✅

**Файл:** `bom_categorizer/classifiers.py`

---

### 2. Границы ячеек в Excel
**Проблема:** В выходном Excel файле не было видно границ между ячейками.

**Исправление:**
- ✅ Добавлены тонкие черные границы для всех ячеек
- ✅ Границы применяются ко всем листам Excel
- ✅ Улучшена читаемость таблиц

**Файл:** `bom_categorizer/excel_writer.py`

---

### 3. Ошибка при сравнении файлов с пустыми количествами
**Проблема:** При сравнении BOM файлов возникала ошибка "invalid literal for int() with base 10: ''" если в файле были пустые значения количества.

**Исправление:**
- ✅ Добавлена обработка пустых значений, NaN и строк
- ✅ Пустые количества теперь интерпретируются как 0
- ✅ Улучшена стабильность функции сравнения

**Файл:** `bom_categorizer/main.py`

---

### 4. Вертикальная прокрутка в GUI
**Проблема:** Главное окно приложения стало слишком большим (900px) и не помещалось на экран.

**Исправление:**
- ✅ Добавлен Canvas с вертикальным Scrollbar
- ✅ Теперь можно прокручивать содержимое колесом мыши
- ✅ Высота окна уменьшена: 900px → 700px
- ✅ Ширина увеличена: 720px → 750px (для scrollbar)
- ✅ Окно теперь помещается на экран любого размера

**Файл:** `bom_categorizer/gui.py`

---

## 📊 Примеры исправлений:

### Классификация оптических компонентов:

**До исправления:**
```
Коммутатор оптический NSSW-123 → dev_boards (отладочные платы) ❌
```

**После исправления:**
```
Коммутатор оптический NSSW-123 → optics (оптические компоненты) ✅
Оптический модуль MP2320 → optics ✅
Optical switch NSSW-456 → optics ✅
```

### Границы в Excel:

**До:**
- Ячейки без границ, таблица плохо читается

**После:**
- Все ячейки с тонкими черными границами
- Таблица выглядит профессионально

### Прокрутка в GUI:

**До:**
- Окно 720x900px не помещается на экран
- Нижние элементы недоступны

**После:**
- Окно 750x700px помещается на любом экране
- Вертикальная прокрутка для доступа ко всем элементам
- Прокрутка колесом мыши

---

### 5. Стандартизация данных перед сравнением (КРИТИЧЕСКОЕ)
**Проблема:** При сравнении двух файлов (где второй был получен из первого) показывались различия, хотя их не должно быть. Файлы не приводились к единому стандартному виду.

**Исправление:**
- ✅ Добавлен параметр `force_reprocess=True` для принудительной повторной обработки
- ✅ При сравнении оба файла ВСЕГДА полностью обрабатываются заново:
  - Удаляются старые колонки "ТУ" и "Примечание"
  - Заново извлекаются ТУ коды
  - Заново очищаются названия
  - Применяется сортировка
- ✅ Оба файла приводятся к ОДИНАКОВОМУ стандартному виду
- ✅ Сравнение теперь корректное

**Алгоритм сравнения:**
```
Для каждого файла:
1. Загрузка → 2. Нормализация → 3. Классификация 
   → 4. Очистка → 5. ПОЛНАЯ обработка (force_reprocess=True) 
   → 6. Сравнение стандартизированных данных
```

**Файлы:** `bom_categorizer/main.py`, `bom_categorizer/excel_writer.py`

---

### 6. Классификация сердечников и согласованных нагрузок
**Проблема:** 
- "СЕРДЕЧНИК М600НН-3 С1,2Х10" не попадал в "Индуктивности"
- "Нагрузка согласованная ANNE-50+" не попадал в "Отладочные платы"

**Исправление:**
- ✅ Добавлены ключевые слова "сердечник", "core" для категории "inductors"
- ✅ "Нагрузка согласованная" перенесена из "others" в "dev_boards"
- ✅ Удалены "сердечник" и "core" из общей категории "others"

**Примеры:**
- "СЕРДЕЧНИК М600НН-3 С1,2Х10" → **inductors** ✅
- "Core ferrite" → **inductors** ✅
- "Нагрузка согласованная ANNE-50+" → **dev_boards** ✅
- "Matched load 50 Ohm" → **dev_boards** ✅

**Файл:** `bom_categorizer/classifiers.py`

---

### 7. Ошибка int() при обработке файлов (КРИТИЧЕСКОЕ)
**Проблема:** При обработке файлов для сравнения возникала ошибка "invalid literal for int() with base 10: ''" при попытке преобразовать пустые строки в номерах п/п и количествах.

**Исправление:**
- ✅ Добавлена безопасная обработка пустых значений в номерах п/п
- ✅ Добавлена безопасная обработка в подсчете общего количества
- ✅ Проверка на `pd.notna()`, пустую строку и try/except для преобразования типов
- ✅ Пустые значения корректно обрабатываются как 0 или игнорируются

**Файл:** `bom_categorizer/excel_writer.py`

---

### 8. Удаление колонки "unnamed"
**Проблема:** В выходных файлах появлялась ненужная колонка "unnamed: 3".

**Исправление:**
- ✅ Добавлена автоматическая фильтрация всех колонок с "unnamed" в названии
- ✅ Колонки удаляются из всех выходных листов

**Файл:** `bom_categorizer/excel_writer.py`

---

### 9. Сортировка микросхем: латинские перед кириллическими
**Проблема:** Микросхемы сортировались неправильно - латинские названия перемешивались с кириллическими.

**Исправление:**
- ✅ Специальная логика сортировки для категории "Микросхемы"
- ✅ Латинские названия (A-Z) идут ПЕРВЫМИ
- ✅ Кириллические названия (А-Я) идут ВТОРЫМИ
- ✅ Учитываются названия, начинающиеся с цифр (например, "1564ТЛ2У" - определяется по первой букве "Т")

**Примеры правильной сортировки:**
```
1. AD9221AR           [LATIN]
2. DS18B20            [LATIN]
3. HMC 435 AMS8GE     [LATIN]
4. SN74LVC8T245DWR    [LATIN]
5. 1272ПН3Т           [CYRILLIC - первая буква "П"]
6. 1303ЕН3.3П         [CYRILLIC - первая буква "Е"]
7. 140УД17А ВК        [CYRILLIC - первая буква "У"]
```

**Файл:** `bom_categorizer/excel_writer.py`

---

### 10. Обработка артикулов инструментальных плат
**Проблема:** "Плата инструментальная Analog Device, артикул EVAL-ADF4351EB1Z" - в названии оставалось все, включая префикс и производителя.

**Исправление:**
- ✅ Удаляется префикс "Плата инструментальная"
- ✅ Извлекается только артикул после слова "артикул"
- ✅ Производитель извлекается в колонку "ТУ"
- ✅ Нормализация "Analog Device" → "Analog Devices"
- ✅ Поддержка символа '+' в артикулах (например, "ZX60-33LN-S+")

**Примеры:**
- "Плата инструментальная Analog Device, артикул EVAL-ADF4351EB1Z" → **EVAL-ADF4351EB1Z**, ТУ: Analog Devices ✅
- "Плата инструментальная MINI-CIRCUITS, артикул ZX60-33LN-S+" → **ZX60-33LN-S+**, ТУ: MINI-CIRCUITS ✅

**Файлы:** `bom_categorizer/formatters.py`

---

### 11. Автоматическое удаление элементов с "АМФИ" из выходных файлов
**Функция:** Все элементы, содержащие "АМФИ" в названии, теперь автоматически исключаются из выходного файла.

**Реализация:**
- ✅ Автоматическая фильтрация в основном режиме обработки
- ✅ Автоматическая фильтрация в режиме сравнения
- ✅ Регистронезависимый поиск (АМФИ, Амфи, амфи)
- ✅ Вывод количества удаленных элементов в лог

**Примеры:**
- "БФ ШСК-М АМФИ.436717.ХХХ" → **удалено** ✅
- "Плата АМФИ.411734.013" → **удалено** ✅

**Файлы:** `bom_categorizer/main.py`

---

### 12. Улучшение интерфейса исключения элементов
**Проблема:** Интерфейс для исключения элементов из BOM был неочевиден для пользователя - непонятно было, какую кнопку нажимать после ввода.

**Улучшения:**
- ✅ Простой и понятный заголовок секции
- ✅ Инструкция с форматом и примером
- ✅ Явное указание: "После ввода элементов нажмите кнопку 'Запустить обработку' выше"
- ✅ Простое текстовое поле без лишних украшений

**Формат:**
```
Название ИВП, количество

Примеры:
AD9221AR, 2
Резистор 1кОм, 10
Конденсатор 100pF, 5
```

**Файлы:** `bom_categorizer/gui.py`

---

## ✅ Все изменения протестированы и готовы к использованию!

**Краткий итог:**
- ✅ Оптические компоненты классифицируются с наивысшим приоритетом
- ✅ Границы ячеек в Excel
- ✅ Ошибка с пустыми количествами исправлена (в сравнении и обработке)
- ✅ Вертикальная прокрутка в GUI
- ✅ Стандартизация файлов перед сравнением
- ✅ Сердечники → Индуктивности
- ✅ Согласованные нагрузки → Отладочные платы
- ✅ Удаление колонки "unnamed"
- ✅ Сортировка микросхем: латинские перед кириллическими
- ✅ Обработка артикулов инструментальных плат
- ✅ Автоматическое удаление элементов с "АМФИ"
- ✅ Улучшенный интерфейс исключения элементов

*Патч-релиз: 09.10.2025*

