# 🧪 Система тестирования BOM Categorizer

Это руководство поможет вам запускать существующие тесты и создавать новые для проверки функционала приложения.

---

## ⚡️ Быстрая справка (Cheatsheet)

### 🚀 Запуск тестов (Windows)
```cmd
# Быстрые unit-тесты (без обработки файлов)
run_tests.bat quick

# Полный прогон всех тестов
run_tests.bat

# Тесты на реальных файлах (из папки example/)
test_examples.bat
```

### 🚀 Запуск тестов (macOS / Linux)
```bash
# Активация окружения
source venv/bin/activate

# Запуск всех тестов
pytest

# Запуск конкретного теста
pytest tests/test_classifiers.py
```

---

## 🏗 Структура тестов

Все тесты находятся в папке `tests/`.

| Файл | Назначение |
|------|------------|
| **`tests/test_classifiers.py`** | Тестирование логики определения категорий (Резисторы, Микросхемы и т.д.). |
| **`tests/test_formatters.py`** | Тестирование очистки и форматирования строк (номиналы, допуски). |
| **`tests/test_database.py`** | Тестирование работы с базой данных компонентов (JSON). |
| **`tests/test_integration.py`** | Интеграционные тесты: проверка полного цикла обработки файлов. |
| **`conftest.py`** | Общие настройки и фикстуры (fixtures) для pytest. |

---

## 📝 Как создавать тесты

Мы используем фреймворк **pytest**. Тесты — это просто функции, начинающиеся с `test_`.

### 1. Создание Unit-теста (Пример)
Допустим, вы хотите протестировать новую функцию классификации.
Создайте файл `tests/test_my_feature.py` или добавьте в существующий.

```python
from bom_categorizer.classifiers import classify_component

def test_resistor_classification():
    # 1. Подготовка данных (Arrange)
    description = "RES 10k 1% 0603"
    
    # 2. Вызов функции (Act)
    category = classify_component(description)
    
    # 3. Проверка результата (Assert)
    assert category == "resistors"

def test_unknown_component():
    description = "Strange Device 3000"
    category = classify_component(description)
    assert category == "unclassified"
```

### 2. Использование фикстур (Fixtures)
Если тесту нужны сложные данные (например, временная база данных), используйте фикстуры из `conftest.py`.

```python
def test_database_add(mock_database):
    # mock_database - это временная копия БД, которая удалится после теста
    mock_database.add("New Part", "chips")
    assert mock_database.get("New Part") == "chips"
```

### 3. Параметризация (Много тестов в одном)
Чтобы не писать 10 похожих тестов, используйте `@pytest.mark.parametrize`.

```python
import pytest
from bom_categorizer.formatters import normalize_value

@pytest.mark.parametrize("input_val, expected", [
    ("10k", "10 kOhm"),
    ("4k7", "4.7 kOhm"),
    ("100R", "100 Ohm"),
    ("0.1uF", "100 nF"),
])
def test_normalization(input_val, expected):
    assert normalize_value(input_val) == expected
```

---

## 🏃‍♂️ Как запускать тесты (Подробно)

### Вариант 1: Через BAT файлы (Windows)
Самый простой способ. Скрипты сами активируют виртуальное окружение.

*   **`run_tests.bat quick`** — запускает только быстрые тесты (пропускает медленные интеграционные). Идеально для частых проверок при разработке.
*   **`run_tests.bat coverage`** — запускает тесты и создает отчет о покрытии кода (насколько хорошо код протестирован). Отчет будет в папке `htmlcov/`.

### Вариант 2: Через Pytest (Универсально)
Для гибкого запуска конкретных тестов.

```bash
# 1. Активируйте venv
# Windows:
.\.venv\Scripts\Activate.ps1
# macOS/Linux:
source venv/bin/activate

# 2. Запускайте pytest
pytest -v                          # Подробный вывод
pytest tests/test_classifiers.py   # Только один файл
pytest -k "resistor"               # Только тесты, в названии которых есть "resistor"
pytest --last-failed               # Запустить только те, что упали в прошлый раз
```

---

## 📂 Тестирование на реальных файлах

Для проверки работы на реальных BOM файлах используется папка `example/`.
**Важно:** Эта папка исключена из Git (`.gitignore`), чтобы вы случайно не выложили конфиденциальные файлы.

1.  Создайте папку `example/` в корне проекта.
2.  Положите туда свои файлы (`.doc`, `.docx`, `.xlsx`, `.txt`).
3.  Запустите:
    ```cmd
    test_examples.bat
    ```
4.  Результаты появятся в папке `test_output/`. Проверьте созданные Excel файлы.

---

## 📊 Отчеты и метрики

### Покрытие кода (Code Coverage)
Показывает, какой процент кода выполняется тестами.
```cmd
run_tests.bat coverage
```
Откройте `htmlcov/index.html` в браузере, чтобы увидеть детальный отчет по строкам.

### HTML отчет
Создает красивый отчет о результатах тестирования.
```cmd
pytest --html=report.html
```

---

## 🛠 Устранение проблем

| Проблема | Решение |
|----------|---------|
| **ModuleNotFoundError: No module named 'pytest'** | Вы не установили зависимости. Выполните `pip install -r requirements.txt`. |
| **Тесты падают на файлах** | Убедитесь, что папка `example/` существует и в ней есть файлы, если запускаете `test_examples.bat`. |
| **Ошибка кодировки (Windows)** | В консоли выполните `chcp 65001` перед запуском. |
| **Database locked / conflict** | Тесты используют общую БД? Используйте фикстуру `mock_database` для изоляции. |

---

**Разработчик:** Куреин М.Н.
