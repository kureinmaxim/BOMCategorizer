# Изменения в версии 1.8.5 (09.10.2025)

## 🎯 Основная функция: Сравнение двух BOM файлов

### ⭐ НОВАЯ ФУНКЦИЯ: Сравнение BOM файлов

**Описание:** Добавлена возможность сравнивать два BOM файла и создавать отчет о различиях между ними. Это позволяет отслеживать изменения между версиями проекта.

---

## 📋 Использование

### GUI интерфейс:

**Новая секция "Сравнение двух BOM файлов":**

1. **Первый файл (базовый):** Выберите исходный файл BOM
2. **Второй файл (новый):** Выберите обновленный файл BOM
3. **Файл результата:** Укажите имя выходного файла (например, `comparison.xlsx`)
4. **Кнопка "Сравнить файлы":** Запуск сравнения

**Расположение в интерфейсе:**
```
┌─────────────────────────────────────────┐
│ Входные файлы                          │
│ Настройки обработки                    │
│ Перенос компонентов в "Не распределено"│
│ Исключение элементов из BOM            │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│ 🆕 Сравнение двух BOM файлов           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│ Логи                                    │
└─────────────────────────────────────────┘
```

### CLI интерфейс:

```bash
split_bom --compare file1.xlsx file2.xlsx --compare-output comparison.xlsx
```

**Параметры:**
- `--compare FILE1 FILE2` - Указать два файла для сравнения
- `--compare-output OUTPUT` - Указать выходной файл для результатов

---

## 🔄 Алгоритм работы

### 1. Подготовка файлов

Оба файла обрабатываются по стандартной методике:
- ✅ Загрузка данных
- ✅ Нормализация колонок
- ✅ Классификация элементов по категориям
- ✅ **Автоматический перенос нераспределенных элементов в категорию "Другие"**
- ✅ Очистка названий компонентов
- ✅ Извлечение ТУ кодов и производителей

**Важно:** Интерактивный режим отключен. Все элементы, которые не удается классифицировать автоматически, переносятся в категорию "Другие".

### 2. Сравнение

Для каждой категории:
- Создается словарь `{Название ИВП: Количество}`
- Суммируется количество одинаковых элементов
- Находятся различия:
  - **Добавлено**: элемент отсутствует в файле 1, но есть в файле 2
  - **Удалено**: элемент есть в файле 1, но отсутствует в файле 2
  - **Изменено**: элемент присутствует в обоих файлах, но с разным количеством

### 3. Формирование отчета

Создается Excel файл с одним листом "Сравнение", содержащий таблицу:

| Категория | Изменение | Наименование ИВП | Кол-во в файле 1 | Кол-во в файле 2 | Разница |
|-----------|-----------|------------------|------------------|------------------|---------|
| resistors | Добавлено | R100 1kΩ         | 0                | 5                | +5      |
| resistors | Удалено   | R200 10kΩ        | 3                | 0                | -3      |
| capacitors| Изменено  | C100 10µF        | 10               | 15               | +5      |

---

## 📊 Пример работы

### Входные данные:

**Файл 1 (file1.xlsx):**
```
Наименование       Кол-во  Категория
AD9221AR           2       ics
GRM1885C1H681J     5       capacitors
SN74LVC8T245DWR    3       ics
```

**Файл 2 (file2.xlsx):**
```
Наименование       Кол-во  Категория
AD9221AR           4       ics
GRM1885C1H681J     5       capacitors
TLV320AIC3204      1       ics
```

### Результат (comparison.xlsx):

```
Категория | Изменение | Наименование ИВП  | Кол-во в файле 1 | Кол-во в файле 2 | Разница
----------|-----------|-------------------|------------------|------------------|--------
ics       | Изменено  | AD9221AR          | 2                | 4                | +2
ics       | Удалено   | SN74LVC8T245DWR   | 3                | 0                | -3
ics       | Добавлено | TLV320AIC3204     | 0                | 1                | +1
```

### Лог в консоли:

```
================================================================================
🔄 СРАВНЕНИЕ BOM ФАЙЛОВ
================================================================================

📂 Обработка файла: file1.xlsx
✓ Файл обработан: 10 элементов в 5 категориях

📂 Обработка файла: file2.xlsx
ℹ️  Перенос 2 нераспределенных элементов в категорию 'Другие'
✓ Файл обработан: 11 элементов в 5 категориях

📊 Сравнение по категориям...

✅ Результаты сравнения записаны: comparison.xlsx
   Найдено различий: 3
   Добавлено: 1
   Удалено: 1
   Изменено: 1
```

---

## 🛠 Технические детали

### Измененные файлы:

1. **`config.json`**
   - ✅ Обновлена версия: 1.8.4 → 1.8.5

2. **`bom_categorizer/__init__.py`**
   - ✅ Обновлена версия: 1.8.4 → 1.8.5

3. **`bom_categorizer/gui.py`**
   - ✅ Добавлены переменные: `compare_file1`, `compare_file2`, `compare_output`
   - ✅ Добавлена секция GUI для сравнения файлов (после исключения элементов, перед логами)
   - ✅ Добавлены обработчики:
     - `on_select_compare_file1()` - выбор первого файла
     - `on_select_compare_file2()` - выбор второго файла
     - `on_select_compare_output()` - выбор выходного файла
     - `on_compare_files()` - запуск сравнения
   - ✅ Увеличен размер окна: 720x750 → 720x900

4. **`bom_categorizer/main.py`**
   - ✅ Добавлены аргументы CLI:
     - `--compare FILE1 FILE2` - два файла для сравнения
     - `--compare-output OUTPUT` - выходной файл
   - ✅ Сделаны `--inputs` и `--xlsx` опциональными (для режима сравнения)
   - ✅ Добавлена функция `process_file_for_comparison(file_path, no_interactive)`:
     - Обрабатывает файл по стандартной методике
     - Автоматически переносит unclassified в "others"
     - Возвращает словарь категорий
   - ✅ Добавлена функция `compare_bom_files(file1, file2, output, no_interactive)`:
     - Обрабатывает оба файла
     - Сравнивает по категориям
     - Находит добавленные/удаленные/измененные элементы
     - Создает Excel отчет
     - Выводит статистику

### Логика сравнения:

```python
def compare_logic():
    # Для каждой категории:
    items1 = {name: qty} для файла 1
    items2 = {name: qty} для файла 2
    
    for item_name in all_items:
        if qty1 == 0 and qty2 > 0:
            → Добавлено
        elif qty1 > 0 and qty2 == 0:
            → Удалено
        elif qty1 != qty2:
            → Изменено
```

---

## ✅ Преимущества

1. **Автоматизация:** Не нужно вручную сравнивать файлы
2. **Точность:** Учитываются все категории и количества
3. **Наглядность:** Четкий отчет в формате Excel
4. **Гибкость:** Работает как в GUI, так и в CLI
5. **Совместимость:** Работает с любыми BOM файлами, которые поддерживает приложение
6. **Статистика:** Автоматический подсчет добавленных/удаленных/измененных элементов

---

## 🎯 Применение

### Сценарии использования:

1. **Контроль версий проекта:**
   - Сравнение BOM между версиями v1.0 и v1.1
   - Отслеживание изменений в комплектации

2. **Проверка корректности обновлений:**
   - Убедиться, что все необходимые компоненты добавлены
   - Проверить, что лишние компоненты удалены

3. **Документирование изменений:**
   - Создание отчета для согласования с заказчиком
   - Подготовка документации для производства

4. **Аудит комплектации:**
   - Сравнение планового и фактического состава
   - Выявление расхождений

---

## 🔧 Обратная совместимость

✅ Все изменения обратно совместимы  
✅ Параметры `--compare` и `--compare-output` опциональные  
✅ Существующий функционал работает без изменений  
✅ Интерфейс GUI расширен, но не изменен  

---

## 📝 Примечания

- Если файлы идентичны, создается Excel файл с сообщением "Файлы идентичны, различий не найдено"
- Элементы группируются по категориям
- Количество суммируется для одинаковых элементов
- Сравнение нечувствительно к регистру
- После сравнения предлагается открыть результирующий файл

---

*Версия: 1.8.5*  
*Дата релиза: 09.10.2025*  
*Разработчик: Куреин М.Н.*

