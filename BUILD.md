# ⚙️ Инструкция по сборке инсталлятора

Этот документ описывает, как создать установочные файлы для Windows и macOS.

## 📦 Две версии приложения

Проект поддерживает **две независимые версии**:

| Версия | GUI | Файл | Инсталлятор |
|--------|-----|------|-------------|
| **Standard v3.2.2** | Tkinter | `app.py` | `BOMCategorizerSetup.exe` / `BOMCategorizer-3.2.2-macOS-Standard.dmg` |
| **Modern Edition v4.0.0** | PySide6 | `app_qt.py` | `BOMCategorizerModernSetup.exe` / `BOMCategorizer-4.0.0-macOS-Modern.dmg` |

Полное сравнение версий см. в файле [EDITIONS.md](EDITIONS.md)

---

## 🚀 Быстрый способ (рекомендуется)

> 🆕 **ВАЖНО:** Начиная с версии 4.2.3, проект использует централизованное управление версиями.  
> Подробности: [Управление версиями](#-управление-версий-новое) или [docs/VERSION_MANAGEMENT.md](docs/VERSION_MANAGEMENT.md)

### Windows: Вариант 1 - Быстрая сборка

**Используйте автоматический скрипт с выбором версии:**

```powershell
python build_installer.py
```

**Диалог выбора:**
```
============================================================
  ВЫБЕРИТЕ ВЕРСИЮ ДЛЯ СБОРКИ:
============================================================

  [1] Standard v3.2.2
      Tkinter GUI (стабильная версия)
      Файл: BOMCategorizerSetup.exe

  [2] Modern Edition v4.0.0
      PySide6 GUI (современный дизайн)
      Файл: BOMCategorizerModernSetup.exe

============================================================

Введите номер версии (1 или 2): _
```

Скрипт автоматически:
1. ✅ Спросит какую версию собирать
2. ✅ Создаст временную директорию `temp_installer/`
3. ✅ Скопирует правильные файлы (config.json/config_qt.json)
4. ✅ Запустит Inno Setup Compiler
5. ✅ Создаст инсталлятор

**Результат:** Готовый инсталлятор в корне проекта!

### Вариант 2: Полная пересборка с чистым окружением

**⚠️ ВНИМАНИЕ: Не нужно запускать каждый раз!**

Скрипт `rebuild_venv.ps1` выполняет полное пересоздание виртуального окружения. Это занимает 2-3 минуты и нужно только в особых случаях.

```powershell
.\rebuild_venv.ps1
```

Этот скрипт делает **всё автоматически**:
1. 🔄 Удаляет старое виртуальное окружение `.venv`
2. ✨ Создаёт новое окружение
3. 🔌 Активирует его
4. 📦 Устанавливает зависимости из `requirements.txt`
5. 🚀 Запускает `build_installer.py`

**Использование:**
```powershell
# Из PowerShell в директории проекта
.\rebuild_venv.ps1

# Если политика выполнения запрещает:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\rebuild_venv.ps1
```

### 📋 Когда использовать `rebuild_venv.ps1`:

**НЕ нужно запускать каждый раз!** Используйте только в этих случаях:

- 🆕 **Первая установка** - когда `.venv` еще не создан
- 🔧 **Что-то сломалось** - окружение повреждено, ошибки при импорте модулей
- 📋 **Обновились зависимости** - изменился `requirements.txt`
- 🐛 **Конфликты пакетов** - нужно пересоздать всё с нуля
- 🎯 **Перед релизом** - чистая сборка для распространения
- 💻 **Перенос на новый компьютер** - первый запуск на новой машине

### ✅ Обычная сборка (ежедневно):

Если окружение уже настроено, используйте простую команду:

```powershell
# 1. Активировать окружение (если еще не активно)
.\.venv\Scripts\Activate.ps1

# 2. Собрать инсталлятор
python build_installer.py
```

**Это быстро (10-20 секунд) и достаточно для 99% случаев!**

---

## 📋 Ручной способ (детальный)

Если нужен контроль над каждым шагом или возникли проблемы с автоматическим скриптом.

### 1. Предварительные требования

Для сборки инсталлятора вам понадобится **Inno Setup Compiler**.

- **Скачайте** его с официального сайта: [innosetup.com](https://jrsoftware.org/isinfo.php)
- **Установите** программу. Обычно она устанавливается в `C:\Program Files (x86)\Inno Setup 6\`.

### 2. Ключевые файлы

- **`build_installer.py`**: 🆕 Автоматический скрипт сборки - выполняет все шаги автоматически
- **`installer_clean.iss`**: Основной скрипт для сборки инсталлятора. Он использует метод "чистой сборки" — сначала копирует все нужные файлы во временную папку `temp_installer`, а затем создает инсталлятор из неё. Это предотвращает ошибки и гарантирует, что в инсталлятор не попадут лишние файлы.
- **`config.json`**: Файл конфигурации, из которого приложение берет номер версии.
- **`run_app.bat`**: Скрипт, который будет запускаться по ярлыку в меню "Пуск". Он активирует виртуальное окружение и запускает GUI.

### macOS: Сборка DMG

**Используйте скрипт с выбором версии:**

```bash
chmod +x build_macos.sh  # Один раз
./build_macos.sh
```

**Диалог выбора:**
```
============================================================
  ВЫБЕРИТЕ ВЕРСИЮ ДЛЯ СБОРКИ:
============================================================

  [1] Standard v3.2.2
      Tkinter GUI (стабильная версия)
      Файл: BOMCategorizer-3.2.2-macOS-Standard.dmg

  [2] Modern Edition v4.0.0
      PySide6 GUI (современный дизайн)
      Файл: BOMCategorizer-4.0.0-macOS-Modern.dmg

============================================================

Введите номер версии (1 или 2): _
```

---

## 📝 Управление версиями (НОВОЕ!)

### 🎯 Централизованная система управления версиями

**С версии 4.2.3 проект использует централизованную систему управления версиями.**

**Единый источник правды:**
```
config.json.template       → Standard Edition
config_qt.json.template    → Modern Edition
```

Все скрипты сборки (`build_macos.sh`, `installer_*.iss`) **автоматически читают** версии из этих шаблонов!

### ✨ Утилита update_version.py

**Показать текущие версии:**
```bash
python update_version.py status
```

**Обновить версию одной командой:**
```bash
# Standard Edition
python update_version.py set standard 3.4.0

# Modern Edition
python update_version.py set modern 4.3.0

# Обе версии сразу
python update_version.py set both 5.0.0
```

**Что происходит автоматически:**
1. ✅ Обновляется версия в шаблоне config
2. ✅ Обновляется дата релиза
3. ✅ Синхронизируются `.iss` файлы (Windows installer)
4. ✅ `build_macos.sh` автоматически прочитает новую версию

**Синхронизация файлов сборки:**
```bash
# После клонирования репозитория или смены ветки
python update_version.py sync
```

### 📖 Полная документация

Подробности в **[docs/VERSION_MANAGEMENT.md](docs/VERSION_MANAGEMENT.md)**

### ⚠️ Старый способ (НЕ РЕКОМЕНДУЕТСЯ)

<details>
<summary>Ручное редактирование версий (устаревший метод)</summary>

**Standard:** Редактировать `config.json.template`
```json
{
  "app_info": {
    "version": "3.4.0",
    "edition": "Standard"
  }
}
```

**Modern Edition:** Редактировать `config_qt.json.template`
```json
{
  "app_info": {
    "version": "4.3.0",
    "edition": "Modern Edition"
  }
}
```

**После ручного редактирования обязательно:**
```bash
python update_version.py sync
```

</details>

### 4. Процесс сборки (ручной)

**Важно:** Скрипт `installer_clean.iss` использует метод "чистой сборки". Это значит, что перед запуском компилятора нужно сначала скопировать все актуальные файлы проекта во временную папку `temp_installer`.

> 🎯 **Рекомендация:** Используйте `python build_installer.py` вместо ручных команд ниже

#### Шаг 1: Подготовка (создание и заполнение `temp_installer`)

Откройте PowerShell в папке проекта и выполните следующие команды, чтобы создать папку и скопировать в нее все необходимые файлы:

```powershell
# Создаем временную папку (если ее нет)
New-Item -ItemType Directory -Force -Path "temp_installer"

# Копируем все необходимые файлы
Copy-Item -Path "app.py", "split_bom.py", "config.json", "README.md", "requirements.txt", "BUILD.md", "interactive_classify.py", "interactive_classify_improved.py", "post_install.ps1", "preview_unclassified.py", "rules.json", "run_app.bat", "split_bom.bat", "start_gui.bat", "installer_clean.iss" -Destination "temp_installer" -Force

# Копируем модульную структуру
Copy-Item -Path "bom_categorizer" -Destination "temp_installer\bom_categorizer" -Recurse -Force

# Копируем папку с документацией
Copy-Item -Path "docs" -Destination "temp_installer\docs" -Recurse -Force

# ✨ Копируем offline_packages для офлайн инсталлятора
Copy-Item -Path "offline_packages" -Destination "temp_installer\offline_packages" -Recurse -Force
```

**📦 Подготовка offline_packages (для офлайн инсталлятора):**

Если директория `offline_packages` не существует или нужно обновить пакеты:

```powershell
# Создать директорию
New-Item -ItemType Directory -Force -Path "offline_packages"

# Скачать все wheel файлы зависимостей для Python 3.13 Windows x64
python -m pip download -r requirements.txt `
    -d offline_packages `
    --only-binary=:all: `
    --platform win_amd64 `
    --python-version 313
```

Это создаст ~38 МБ wheel файлов, которые будут встроены в инсталлятор.

#### Шаг 2: Копирование .iss в корень

```powershell
Copy-Item -Path "temp_installer\installer_clean.iss" -Destination "installer_clean.iss" -Force
```

#### Шаг 3: Компиляция

После того как папка `temp_installer` готова, запустите сборку одним из способов:

##### Способ A: Через графический интерфейс Inno Setup

1.  Запустите **Inno Setup Compiler**.
2.  Откройте файл `installer_clean.iss` в редакторе (`File -> Open`).
3.  Нажмите **`Build -> Compile`** (или клавишу **F9**).
4.  Дождитесь завершения компиляции.

##### Способ B: Через командную строку (PowerShell)

Этот способ используется в `build_installer.py`.

1.  Откройте PowerShell в папке проекта.
2.  Выполните команду:

    ```powershell
    & "C:\Program Files (x86)\Inno Setup 6\iscc.exe" "installer_clean.iss"
    ```
    *(Путь к `iscc.exe` может отличаться, если вы установили Inno Setup в другую директорию).*

---

## 🔧 Настройка build_installer.py

Если Inno Setup установлен в нестандартную директорию, отредактируйте `build_installer.py`:

```python
# Найдите эту строку и укажите ваш путь:
INNO_SETUP_PATH = r"C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
```

## 5. Результат

После успешной компиляции в корневой папке вашего проекта появится файл:

- **`BOMCategorizerSetup.exe`** (~25-40 МБ)

Это и есть готовый инсталлятор, который можно передавать пользователям.

### Что включено в инсталлятор:
- ✅ Все Python скрипты приложения
- ✅ Модульная структура `bom_categorizer/`
- ✅ Offline пакеты Python (pandas, openpyxl, etc.)
- ✅ Документация (docs/)
- ✅ BAT файлы для удобного запуска
- ✅ Автоматическое создание виртуального окружения
- ✅ Ярлыки на рабочем столе и в меню "Пуск"

---

## 🐧 Linux

### Требования:
- Python 3.8+
- tkinter: `sudo apt-get install python3-tk` (Debian/Ubuntu)
- PySide6 (для Modern Edition): устанавливается через pip

### Установка из исходников:

```bash
# Клонировать репозиторий
git clone https://github.com/kureinmaxim/BOMCategorizer.git
cd BOMCategorizer

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить Standard Edition
python3 app.py

# Или запустить Modern Edition
python3 app_qt.py
```

### Создание .desktop файла (опционально):

**Standard Edition:**
```bash
cat > ~/.local/share/applications/bomcategorizer-standard.desktop << EOF
[Desktop Entry]
Version=1.0
Type=Application
Name=BOM Categorizer Standard
Comment=Bill of Materials Categorization Tool (Tkinter)
Exec=/путь/к/BOMCategorizer/venv/bin/python3 /путь/к/BOMCategorizer/app.py
Icon=application-x-executable
Terminal=false
Categories=Utility;Development;
EOF
```

**Modern Edition:**
```bash
cat > ~/.local/share/applications/bomcategorizer-modern.desktop << EOF
[Desktop Entry]
Version=1.0
Type=Application
Name=BOM Categorizer Modern Edition
Comment=Bill of Materials Categorization Tool (PySide6)
Exec=/путь/к/BOMCategorizer/venv/bin/python3 /путь/к/BOMCategorizer/app_qt.py
Icon=application-x-executable
Terminal=false
Categories=Utility;Development;
EOF
```

---

## 🐛 Устранение проблем

### Проблема: "Inno Setup не найден"

**Решение:**
```powershell
# Проверьте путь к ISCC.exe
Test-Path "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"

# Если False - отредактируйте build_installer.py с правильным путем
```

### Проблема: "Не удалось скопировать файлы"

**Решение:**
```powershell
# Убедитесь что все файлы существуют
Test-Path "bom_categorizer"
Test-Path "offline_packages"
Test-Path "docs"
```

### Проблема: Старая версия в инсталляторе

**Решение (новый способ):**
```bash
# 1. Обновить версию централизованно
python update_version.py set modern 4.3.0

# 2. Синхронизировать (если нужно)
python update_version.py sync

# 3. Пересобрать
python build_installer.py
```

### Проблема: Версии рассинхронизированы

**Симптомы:**
- Разные версии в `.iss` файлах и шаблонах
- Старые версии после переключения веток

**Решение:**
```bash
# Синхронизировать все файлы сборки
python update_version.py sync

# Проверить статус
python update_version.py status
```

---

## 📝 Пример полного цикла сборки

### 🆕 Новый способ (рекомендуется):

```bash
# 1. Обновите версию одной командой
python update_version.py set modern 4.3.0
# ✅ Версия обновлена
# ✅ Дата обновлена
# ✅ Файлы сборки синхронизированы

# 2. Проверьте статус
python update_version.py status

# 3. Соберите инсталлятор
# Windows:
python build_installer.py

# macOS:
./build_macos.sh

# 4. Проверьте результат (Windows)
Test-Path "BOMCategorizerModernSetup.exe"
# True - успех! ✅

# 5. Проверьте размер
(Get-Item "BOMCategorizerModernSetup.exe").Length / 1MB
# ~35-50 MB
```

### 📊 Полный workflow релиза:

```bash
# 1. Обновить версию
python update_version.py set modern 4.3.0

# 2. Закоммитить изменения (без локальных config!)
git add config_qt.json.template installer_qt.iss
git add update_version.py sync_installer_versions.py
git commit -m "Release: Modern Edition v4.3.0"

# 3. Собрать инсталляторы
python build_installer.py        # Windows
./build_macos.sh                 # macOS

# 4. Создать тег и push
git tag v4.3.0
git push origin experimental/new-feature
git push origin v4.3.0

# 5. Создать GitHub Release с файлами
```

---

## 📊 Результаты сборки

### Windows
- **Standard:** `BOMCategorizerSetup.exe` (~25-40 MB)
- **Modern Edition:** `BOMCategorizerModernSetup.exe` (~35-50 MB, включает PySide6)

### macOS
- **Standard:** `BOMCategorizer-3.2.2-macOS-Standard.dmg` (~500 KB)
- **Modern Edition:** `BOMCategorizer-4.0.0-macOS-Modern.dmg` (~500 KB)

---

## 🧪 Тестирование инсталлятора

После создания инсталлятора **обязательно** протестируйте на чистой системе:

### Чек-лист тестирования:
1. ✅ Установка проходит без ошибок
2. ✅ Первый запуск работает корректно
3. ✅ PIN-защита функционирует
4. ✅ База данных создается и сохраняется
5. ✅ Обработка файлов работает
6. ✅ Экспорт результатов успешен
7. ✅ Удаление через "Установка и удаление программ" работает
8. ✅ После удаления база данных сохраняется в `%APPDATA%` (опционально можно удалить вручную)

### Рекомендации:
- Тестируйте на виртуальной машине или компьютере коллеги
- Проверьте обе версии (Standard и Modern Edition) независимо
- Запишите найденные проблемы для следующего релиза

---

## 📤 Распространение

### Рекомендуемые способы:

1. **GitHub Releases** (рекомендуется)
   - ✅ Автоматическое версионирование
   - ✅ Changelog встроен
   - ✅ Статистика скачиваний
   - ✅ Надежное хранилище

2. **Прямая загрузка с сервера**
   - Для корпоративного использования
   - Требует веб-сервер

3. **Файлообменники**
   - Для больших файлов (>50 MB)
   - Google Drive, Dropbox, OneDrive

### Пример GitHub Release:

```bash
# Создать релиз с обеими версиями
gh release create v3.2.2-4.0.0 \
  BOMCategorizerSetup.exe \
  BOMCategorizerModernSetup.exe \
  BOMCategorizer-3.2.2-macOS-Standard.dmg \
  BOMCategorizer-4.0.0-macOS-Modern.dmg \
  --title "BOM Categorizer v3.2.2 (Standard) / v4.0.0 (Modern Edition)" \
  --notes "См. CHANGELOG.md"
```

---

## 📚 Дополнительная документация

- **[docs/VERSION_MANAGEMENT.md](docs/VERSION_MANAGEMENT.md)** - 🆕 Управление версиями проекта
- **[docs/PLATFORM_COMPARISON.md](docs/PLATFORM_COMPARISON.md)** - 🆕 Сравнение Windows vs macOS
- **[EDITIONS.md](EDITIONS.md)** - Подробное сравнение версий
- **[ANALYSIS_PROJECT.md](ANALYSIS_PROJECT.md)** - Архитектура проекта
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений
- **[docs/QUICK_START.md](docs/QUICK_START.md)** - Быстрый старт

---

*Документ создан: 03.10.2025*  
*Обновлено: 12.11.2025 (v3.3.0/v4.2.3 - централизованное управление версиями)*  
*Версия: Standard 3.3.0 / Modern Edition 4.2.3*


