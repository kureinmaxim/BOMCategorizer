# 🎨 Настройка иконок приложения

## 📋 Что нужно сделать

### Шаг 1: Сохраните иконку

1. Сохраните изображение в корень проекта как `icon.png`
2. Рекомендуемый размер: **512x512** или **1024x1024** пикселей
3. Формат: PNG с прозрачным фоном

### Шаг 2: Создайте иконки для обеих платформ

```bash
# Активируйте виртуальное окружение
source venv/bin/activate      # macOS/Linux
.\venv\Scripts\Activate.ps1   # Windows

# Запустите скрипт создания иконок
python create_icons.py
```

**Скрипт создаст:**
- `icon.ico` - для Windows (многослойная, размеры: 16x16, 24x24, 32x32, 40x40, 48x48, 64x64, 96x96, 128x128, 256x256)
  - ✨ **Улучшено:** Добавлена оптимизация резкости для маленьких размеров (16x16, 24x24, 32x32)
  - ✨ **Улучшено:** Расширенный набор размеров для лучшего отображения в разных контекстах Windows
  - ✨ **Новое:** Скрипт может использовать `icon.icns` как источник, если `icon.png` недоступен
- `icon.icns` - для macOS (только на macOS с помощью `iconutil`, или создайте через онлайн конвертер)

**💡 Важно для четкости на Windows:**
- Используйте исходное изображение **минимум 512x512 пикселей** (лучше 1024x1024)
- Изображение должно быть высокого качества без размытия
- Формат PNG с прозрачным фоном для лучшего результата
- **Альтернатива:** Если у вас уже есть `icon.icns`, скрипт попытается извлечь из него изображение для создания `icon.ico`

### Шаг 3 (альтернатива для macOS): Онлайн конвертация

**Если вы работаете на Windows или Linux, но вам нужен файл `.icns` для macOS:**

`.icns` - это формат иконок для macOS. Если вы разрабатываете на Windows/Linux, но планируете собирать приложение для macOS, вам понадобится файл `icon.icns`.

**Создание `.icns` через онлайн конвертер:**

1. Откройте: https://cloudconvert.com/png-to-icns
2. Загрузите `icon.png`
3. Скачайте `icon.icns`
4. Поместите в корень проекта

**Примечание:** Файл `icon.icns` используется только для macOS. Для Windows нужен `icon.ico`, который создается из `icon.png` автоматически.

---

## 🔧 Автоматическое обновление конфигураций

После создания иконок они автоматически будут использоваться:

### Windows (Inno Setup)

**Файлы обновятся автоматически:**
- `installer_clean.iss` (Standard)
- `installer_qt.iss` (Modern Edition)

**Что добавится:**
```iss
SetupIconFile=icon.ico
UninstallDisplayIcon={app}\icon.ico
```

### macOS (py2app)

**Файл обновится автоматически:**
- `setup_macos.py`

**Что добавится:**
```python
OPTIONS = {
    'iconfile': 'icon.icns',
    ...
}
```

---

## ✅ Проверка результата

### Windows:
1. **Пересоздайте иконку** (если она нечеткая):
   ```bash
   python create_icons.py
   ```
   
2. **Очистите кэш иконок Windows** (если старая иконка все еще отображается):
   ```powershell
   # Закройте все окна приложения
   # Затем выполните:
   ie4uinit.exe -show
   # Или перезагрузите проводник:
   taskkill /f /im explorer.exe && start explorer.exe
   ```

3. Соберите инсталлятор: `python build_installer.py`

4. Запустите `BOMCategorizerSetup.exe` или `BOMCategorizerModernSetup.exe`

5. Иконка появится:
   - В мастере установки
   - На ярлыке рабочего стола
   - В меню "Пуск"
   - В "Установка и удаление программ"

**⚠️ Если иконка все еще нечеткая:**
- Убедитесь что исходное `icon.png` имеет размер минимум 512x512 пикселей
- Проверьте что файл `icon.ico` содержит все размеры (должен быть больше 50 KB)
- Удалите старый ярлык и создайте новый
- Перезагрузите компьютер для полной очистки кэша Windows

### macOS:
1. Соберите DMG: `./build_macos.sh`
2. Откройте DMG
3. Иконка появится:
   - У `.app` файла в DMG
   - У `.app` в папке Applications после установки
   - В Dock при запуске

---

## 📦 Структура файлов

```
ProjectSnabjenie/
├── icon.png          # Исходное изображение (ваше)
├── icon.ico          # Windows иконка (создается автоматически)
├── icon.icns         # macOS иконка (создается автоматически на macOS)
├── create_icons.py   # Скрипт создания иконок
└── ICONS_SETUP.md    # Эта инструкция
```

---

## 🎯 Результат

После выполнения всех шагов:
- ✅ Обе версии (Standard и Modern Edition) используют одну иконку
- ✅ Иконка отображается на всех платформах (Windows и macOS)
- ✅ Единый визуальный стиль приложения

---

## 🔍 Устранение проблем с нечеткой иконкой на Windows

### Проблема: Иконка нечеткая на Windows, хотя на macOS четкая

**Причины:**
1. Недостаточно размеров в ICO файле
2. Windows кэширует старую иконку
3. Исходное изображение недостаточно высокого разрешения

**Решение:**

#### Шаг 1: Проверьте исходное изображение
```bash
# Проверьте размер icon.png
# Должен быть минимум 512x512, лучше 1024x1024
```

#### Шаг 2: Пересоздайте иконку

**⚠️ Важно:** Pillow может не создать правильную многослойную ICO. Используйте один из методов:

**Метод 1 - Онлайн конвертер (рекомендуется):**
1. Откройте: https://convertio.co/png-ico/ или https://cloudconvert.com/png-to-ico
2. Загрузите `icon.png`
3. Выберите опцию **"Multiple sizes"** или **"All sizes"**
4. Выберите размеры: **16, 24, 32, 40, 48, 64, 96, 128, 256**
5. Скачайте `icon.ico` и замените файл в проекте

**Метод 2 - ImageMagick (если установлен):**
```bash
magick icon.png -define icon:auto-resize=16,24,32,40,48,64,96,128,256 icon.ico
```

**Метод 3 - Скрипт (может не включить все размеры):**
```bash
python create_icons.py
```
⚠️ Проверьте размер файла - должен быть **> 20 KB**. Если меньше - используйте Метод 1 или 2.

#### Шаг 3: Очистите кэш Windows
```powershell
# Вариант 1: Обновить кэш иконок
ie4uinit.exe -show

# Вариант 2: Перезапустить проводник
taskkill /f /im explorer.exe
start explorer.exe

# Вариант 3: Полная очистка (требует перезагрузки)
# Удалите папку: C:\Users\USERNAME\AppData\Local\IconCache.db
# Затем перезагрузите компьютер
```

#### Шаг 4: Пересоберите инсталлятор
```bash
python build_installer.py
```

#### Шаг 5: Переустановите приложение
- Удалите старую версию
- Установите новую версию с обновленной иконкой

---

**Дата создания:** 08.11.2025  
**Последнее обновление:** 11.11.2025  
**Версия:** 2.0

