# 🗄️ Архитектура базы данных компонентов

**Версия документа:** 2.1  
**Дата:** 16.11.2025  
**Версия приложения:** Standard 3.3.0 / Modern Edition 4.4.1  
**Автор / Author:** Куреин М.Н. / Kurein M.N.

---

## 🎯 Концепция

База данных компонентов в проекте имеет **двухуровневую структуру** и используется **обеими версиями приложения** (Standard и Modern Edition).

### ⚠️ Важно: Совместимость версий

**Обе версии приложения используют одну и ту же базу данных:**
- ✅ **Standard Edition v3.3.0** (Tkinter)
- ✅ **Modern Edition v4.4.1** (PySide6)

База данных находится в `%APPDATA%\BOMCategorizer\Data\component_database.json` и доступна для обеих версий. Это означает:
- Можно переключаться между версиями без потери данных
- Классификации, сделанные в одной версии, доступны в другой
- Обновление одной версии не влияет на другую
- **Важно:** База данных общая для обеих версий, но конфигурации (`config.json` и `config_qt.json`) раздельные

### 1. **Образцовая БД** (для разработки)
- **Файл:** `component_database.json`
- **Расположение:** В папке проекта
- **Назначение:** Накопление знаний при разработке и тестировании
- **Git:** Исключена из репозитория (`.gitignore`)
- **Использование:** Разработка, отладка, тестирование приложения

### 2. **Шаблон БД** (для пользователей)
- **Файл:** `component_database_template.json`
- **Расположение:** В папке проекта
- **Назначение:** Пустая БД для новых установок (версия 1.0.0)
- **Git:** Включена в репозиторий ✅
- **Использование:** При первой установке приложения (обеих версий)
- **Структура:** Содержит пустой `components: {}`, метаданные с версией "1.0.0", пустую историю, все категории

---

## 📋 Жизненный цикл БД

### Для разработчика:

```
1. Разработка
   ├─ component_database.json (накопление данных)
   ├─ Тестирование на реальных файлах
   ├─ Добавление компонентов
   └─ Улучшение классификации

2. Подготовка к релизу
   ├─ component_database_template.json (пустой шаблон)
   └─ Commit в Git

3. GitHub
   ├─ Только шаблон (пустая БД)
   └─ Образцовая БД остается локально
```

### Для пользователя:

```
1. Установка приложения
   └─ component_database_template.json → копируется в %APPDATA%

2. Первый запуск
   ├─ Создается пустая БД из шаблона
   └─ Предложение импортировать существующую БД (опционально)

3. Работа
   ├─ База пополняется автоматически
   ├─ Интерактивная классификация
   └─ Импорт из выходных файлов

4. Обновление приложения
   ├─ БД НЕ удаляется (в %APPDATA%)
   └─ Накопленные данные сохраняются
```

---

## 🔧 Техническая реализация

### Определение пути к базе данных:

```python
def get_database_path() -> str:
    """
    Получить путь к файлу базы данных компонентов
    
    Алгоритм определения пути:
    1. Определяет базовую директорию модуля (bom_categorizer/)
    2. Проверяет наличие маркера `.installed` в родительской директории
    3. Если маркер есть → установленная версия:
       - Windows: %APPDATA%\BOMCategorizer\Data\component_database.json
       - Linux/Mac: ~/.local/share/BOMCategorizer/Data/component_database.json
    4. Если маркера нет → режим разработки:
       - C:\Project\BOMCategorizer\component_database.json
    
    Returns:
        Полный путь к файлу базы данных
    """
    base_dir = os.path.dirname(os.path.abspath(__file__))
    parent_dir = os.path.dirname(base_dir)
    installed_marker = os.path.join(parent_dir, ".installed")
    
    if os.path.exists(installed_marker):
        # Установленная версия
        if sys.platform == "win32":
            appdata = os.environ.get('APPDATA', os.path.expanduser('~'))
            data_dir = os.path.join(appdata, 'BOMCategorizer', 'Data')
        else:
            data_dir = os.path.expanduser('~/.local/share/BOMCategorizer/Data')
        os.makedirs(data_dir, exist_ok=True)
        return os.path.join(data_dir, "component_database.json")
    else:
        # Режим разработки
        return os.path.join(parent_dir, "component_database.json")
```

### Структура шаблона:

```json
{
  "metadata": {
    "version": "1.0.0",
    "created": "2025-11-07",
    "last_updated": "2025-11-07",
    "total_components": 0,
    "description": "Template database for BOM Categorizer - empty database for new installations",
    "previous_hash": "",
    "current_hash": ""
  },
  "history": [],
    "categories": {
        "resistors": "Резисторы",
        "capacitors": "Конденсаторы",
        "inductors": "Дроссели/Катушки индуктивности",
        "ics": "Микросхемы",
        "semiconductors": "Полупроводники (диоды, транзисторы)",
        "connectors": "Разъемы",
        "dev_boards": "Отладочные платы и модули",
        "optics": "Оптические компоненты",
        "rf_modules": "СВЧ модули",
        "cables": "Кабели и провода",
        "power_modules": "Модули питания",
        "our_developments": "Наши разработки",
        "others": "Другие компоненты",
        "unclassified": "Неклассифицированные",
        "non_bom": "Не ИВП (служебная информация)"
    },
    "components": {}
}
```

### Формат версионирования (X.Y):

База данных использует **двузначное версионирование** (формат X.Y) после первого изменения:
- **X** (major) - увеличивается при импорте из файла
- **Y** (minor) - увеличивается при ручном добавлении компонентов

**Начальная версия шаблона:** `1.0.0` (трехзначный формат для шаблона)
**После первого изменения:** формат переходит в `X.Y` (двузначный)

**Примеры:**
- `1.0.0` - пустая база из шаблона (начальная версия)
- `1.0` - первый импорт из файла (формат X.Y)
- `1.1` - первое ручное добавление после импорта
- `2.0` - второй импорт из файла (Y сбрасывается)
- `2.5` - после 5 ручных добавлений после второго импорта

**Примечание:** Шаблон использует формат версии `1.0.0` (трехзначный), но после первого изменения версия переходит в формат `X.Y` (двузначный).

### Blockchain-like версионирование:

База данных использует хэши для отслеживания изменений:
- **previous_hash** - SHA256 хэш предыдущей версии
- **current_hash** - SHA256 хэш текущей версии
- **history** - массив записей об изменениях с хэшами

Это позволяет:
- ✅ Отслеживать целостность данных
- ✅ Восстанавливать историю изменений
- ✅ Обнаруживать несанкционированные изменения

### Расположение файлов:

**В проекте (для разработки):**
```
C:\Project\BOMCategorizer\
├── component_database.json              # Образцовая (НЕ в Git)
└── component_database_template.json     # Шаблон (В Git) ✅
```

**После установки (у пользователя):**
```
%APPDATA%\BOMCategorizer\Data\
├── component_database.json              # Пользовательская БД (общая для обеих версий)
└── database_backups\                    # Резервные копии
    └── component_database_backup_*.json
```

**Примечание:** Обе версии приложения (Standard и Modern Edition) используют **одну и ту же базу данных** в `%APPDATA%\BOMCategorizer\Data\`. Это обеспечивает синхронизацию классификаций между версиями.

---

## 📦 Инсталлятор

### Что копируется:

```inno
[Files]
; Копируем ШАБЛОН, а не реальную БД
Source: "component_database_template.json"; DestDir: "{app}"; Flags: ignoreversion
```

### При первом запуске:

```python
def initialize_database_from_template():
    """
    Инициализирует БД из шаблона при первом запуске
    Копирует component_database_template.json в рабочую БД
    
    Алгоритм:
    1. Проверяет наличие БД по пути из get_database_path()
    2. Если БД уже есть → ничего не делает (выход)
    3. Если БД нет → ищет шаблон в родительской директории проекта
    4. Если шаблон найден → копирует его в рабочую БД
    5. Если шаблона нет → создает пустую БД с версией "1.0.0"
    6. Создает папку для резервных копий (database_backups)
    """
    db_path = get_database_path()
    
    # Если БД уже есть - ничего не делаем
    if os.path.exists(db_path):
        return
    
    # Ищем шаблон БД в родительской директории проекта
    base_dir = os.path.dirname(os.path.abspath(__file__))
    parent_dir = os.path.dirname(base_dir)
    template_path = os.path.join(parent_dir, "component_database_template.json")
    
    if os.path.exists(template_path):
        # Копируем шаблон
        os.makedirs(os.path.dirname(db_path), exist_ok=True)
        shutil.copy2(template_path, db_path)
        safe_print(f"✅ Инициализирована БД из шаблона: {db_path}")
    else:
        # Если шаблона нет - создаем пустую БД
        structured_db = {
            "metadata": {
                "version": "1.0.0",
                "created": datetime.now().strftime("%Y-%m-%d"),
                "last_updated": datetime.now().strftime("%Y-%m-%d"),
                "total_components": 0,
                "description": "База данных компонентов для BOM классификатора",
                "previous_hash": "",
                "current_hash": ""
            },
            "history": [],
            "categories": CATEGORY_NAMES,
            "components": {}
        }
        _save_structured_database(structured_db)
    
    # Создаем папку для резервных копий
    backup_dir = os.path.join(os.path.dirname(db_path), "database_backups")
    os.makedirs(backup_dir, exist_ok=True)

def is_first_run() -> bool:
    """
    Проверяет, является ли это первым запуском (пустая или почти пустая БД)
    
    Алгоритм:
    1. Если файла БД нет → True (первый запуск)
    2. Если файл есть → загружает БД и проверяет количество компонентов
    3. Если компонентов <= 10 → True (первый запуск)
    4. Если компонентов > 10 → False (не первый запуск)
    
    Returns:
        True если это первый запуск (БД пустая или содержит <= 10 компонентов)
    """
    db_path = get_database_path()
    
    # Если файла БД нет - это первый запуск
    if not os.path.exists(db_path):
        return True
    
    # Загружаем БД и проверяем количество компонентов
    try:
        components = load_component_database()
        # Считаем первым запуском если компонентов 10 или меньше
        return len(components) <= 10
    except Exception:
        # При ошибке загрузки считаем первым запуском
        return True
```

**Логика первого запуска:**
1. При инициализации приложения вызывается `initialize_database_from_template()`
   - Если БД нет → создается из шаблона или пустая
2. Проверка `is_first_run()` - БД пустая или ≤10 компонентов
3. Если первый запуск → показывается диалог импорта существующей БД (опционально)
4. Если не первый → используется существующая БД без диалога

---

## ✅ Преимущества архитектуры

### Для разработчика:
- ✅ Накопление знаний в `component_database.json`
- ✅ Тестирование на реальных данных
- ✅ База не попадает в Git (конфиденциальность)
- ✅ Образцовая БД помогает в отладке

### Для пользователя:
- ✅ Чистая установка с пустой БД
- ✅ Возможность импорта существующей БД
- ✅ БД не теряется при обновлении
- ✅ Персональные данные в безопасности

### Для проекта:
- ✅ GitHub содержит только шаблон
- ✅ Нет конфиденциальных данных в репозитории
- ✅ Каждый пользователь накапливает свою БД
- ✅ Профессиональная архитектура

---

## 🔄 Импорт существующей БД

При первом запуске приложение спрашивает:

```
╔════════════════════════════════════════╗
║  Импорт базы данных компонентов       ║
╠════════════════════════════════════════╣
║                                        ║
║  У вас уже есть база данных            ║
║  компонентов?                          ║
║                                        ║
║  Вы можете импортировать её для        ║
║  автоматической классификации.         ║
║                                        ║
║  [ Импортировать ] [ Начать с нуля ]  ║
║                                        ║
╚════════════════════════════════════════╝
```

**Если пользователь выбирает "Импортировать":**
- Открывается диалог выбора файла (JSON или Excel)
- Компоненты импортируются
- Статистика отображается

**Если выбирает "Начать с нуля":**
- Используется пустая БД из шаблона
- База пополняется по мере работы

---

## 📊 Пример использования

### Сценарий 1: Новый пользователь

```
1. Установка → component_database_template.json
2. Первый запуск → создается пустая БД
3. Обработка файлов → интерактивная классификация
4. Накопление данных → БД растет автоматически
```

### Сценарий 2: Опытный пользователь

```
1. Установка → component_database_template.json
2. Первый запуск → предложение импорта
3. Импорт → загрузка существующей БД
4. Работа → используются накопленные знания
```

### Сценарий 3: Разработчик

```
1. Клонирование Git → component_database_template.json
2. Разработка → создается component_database.json локально
3. Тестирование → накопление данных
4. Commit → только шаблон идет в Git
```

---

## 🚀 Рекомендации

### При разработке:
1. Работайте с `component_database.json` локально
2. Тестируйте на реальных файлах
3. Накапливайте знания о компонентах
4. НЕ коммитьте `component_database.json` в Git

### Перед релизом:
1. Убедитесь что `component_database_template.json` обновлен
2. Проверьте что шаблон содержит пустой `components: {}`
3. Проверьте что инсталлятор копирует шаблон
4. Протестируйте первый запуск на чистой системе

### При публикации:
1. В GitHub идет только `component_database_template.json`
2. Образцовая БД остается локально
3. Пользователи получают чистую БД
4. Каждый накапливает свои данные

---

## 📁 Файлы

| Файл | Git | Назначение | Расположение |
|------|-----|-----------|--------------|
| `component_database.json` | ❌ | Образцовая БД (разработка) | Проект |
| `component_database_template.json` | ✅ | Шаблон (пустая БД, версия 0.0) | Проект + Git |
| `component_database.json` | - | Пользовательская БД | %APPDATA%\BOMCategorizer\Data\ |
| `database_backups\*.json` | - | Резервные копии БД | %APPDATA%\BOMCategorizer\Data\database_backups\ |

## 🔄 Версионирование базы данных

### Формат версии: X.Y

**Правила инкрементации:**
- **Импорт из файла** → увеличивается X, Y сбрасывается в 0
  - Пример: `1.5` → `2.0` (импорт из файла)
- **Ручное добавление** → увеличивается Y, X не меняется
  - Пример: `1.5` → `1.6` (ручное добавление)
- **Версия 0.0** (пустая база):
  - Первый импорт: `0.0` → `1.0`
  - Первое ручное добавление: `0.0` → `0.1`

### История изменений (history)

Каждое изменение записывается в массив `history`:
```json
{
  "version": "1.5",
  "timestamp": "2025-11-11 15:30:45",
  "action": "import_from_file",  // или "manual_add", "import_from_excel", "database_cleared", "version_changed"
  "source": "input_file.xlsx",
  "components_added": 5,
  "component_names": ["Резистор...", "Конденсатор..."],
  "previous_hash": "abc123...",
  "current_hash": "def456..."
}
```

**Типы действий:**
- `import_from_file` - импорт из выходного файла
- `import_from_excel` - импорт из Excel
- `manual_add` - ручное добавление через интерактивную классификацию
- `database_cleared` - очистка базы данных
- `version_changed` - ручное изменение версии

---

## 🎓 Выводы

Эта архитектура обеспечивает:
- **Конфиденциальность:** Личные данные не попадают в Git
- **Гибкость:** Каждый пользователь накапливает свою БД
- **Профессионализм:** Четкое разделение данных
- **Удобство:** Импорт существующих данных
- **Безопасность:** БД не теряется при обновлении

---

**Версия документа:** 2.1  
**Последнее обновление:** 16.11.2025  
**Версия приложения:** Standard 3.3.0 / Modern Edition 4.4.1  
**Статус:** ✅ Актуально

