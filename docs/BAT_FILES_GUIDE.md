# Шпаргалка по BAT файлам

В проекте используются несколько `.bat` файлов для автоматизации рутинных задач, таких как запуск приложения, управление базой данных и выполнение тестов.

## 🚀 Основные скрипты

### `run_app.bat`
**Основной файл для запуска приложения.**

- **Назначение:** Запускает графический интерфейс BOM Categorizer.
- **Логика работы:**
    1.  Переходит в директорию со скриптом.
    2.  Проверяет, существует ли виртуальное окружение `.venv`.
    3.  **Если окружение не найдено (первый запуск):**
        - Запускает `post_install.ps1` для автоматического создания окружения и установки всех необходимых зависимостей из `requirements.txt`.
    4.  **Если окружение найдено:**
        - Запускает приложение через `.venv\Scripts\python.exe`.
- **Особенности:**
    - Всегда показывает консольное окно, что позволяет видеть любые ошибки при запуске или во время работы.
    - В конце выполнения ожидает нажатия клавиши (`pause`), чтобы консоль не закрылась сразу после завершения работы приложения.
    - Это **единственный** скрипт, который следует использовать для запуска приложения в обычном режиме.

### `repair_install.bat`
**Скрипт для восстановления установки.**

- **Назначение:** Принудительно запускает скрипт установки `post_install.ps1`.
- **Когда использовать:**
    - Если приложение перестало запускаться из-за проблем с виртуальным окружением.
    - Если вы вручную удалили папку `.venv` и хотите ее восстановить.
    - Если кажется, что зависимости установлены некорректно.

## 📦 Управление базой данных

### `manage_database.bat`
**Интерактивный менеджер базы данных.**

- **Назначение:** Запускает `manage_database.py` — консольный скрипт для выполнения различных операций с базой данных компонентов.
- **Возможности:**
    - Просмотр статистики.
    - Поиск компонентов.
    - Добавление, редактирование и удаление записей.
    - Создание резервных копий и восстановление.

### `database_backup.bat`
**Быстрое резервное копирование базы данных.**

- **Назначение:** Создает резервную копию файла `component_database.json` в папке `backups`.
- **Особенности:** Добавляет к имени файла дату и время создания копии.

### `database_export.bat`
**Экспорт базы данных в Excel.**

- **Назначение:** Запускает экспорт содержимого базы данных в `.xlsx` файл для удобного просмотра и анализа.

### `database_stats.bat`
**Просмотр статистики базы данных.**

- **Назначение:** Выводит в консоль основную статистику по базе данных: количество компонентов, категорий и т.д.

## ⚙️ Утилиты и Тестирование

### `run_tests.bat`
**Запуск тестов.**

- **Назначение:** Запускает автоматические тесты для проверки работоспособности основных модулей проекта.

### `split_bom.bat`
**Разделение BOM файла (CLI).**

- **Назначение:** Позволяет запустить основную логику категоризации напрямую из командной строки, без графического интерфейса.

### `download_fonts.bat`
**Загрузка шрифтов.**

- **Назначение:** Скачивает необходимые шрифты (например, DejaVu Sans), которые используются для корректного создания PDF отчетов с поддержкой кириллицы.

### `check_pdf_fonts.bat`
**Проверка наличия шрифтов.**

- **Назначение:** Проверяет, установлены ли в системе шрифты, необходимые для генерации PDF.
