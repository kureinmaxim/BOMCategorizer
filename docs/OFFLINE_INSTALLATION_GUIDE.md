# 📦 Руководство по офлайн установке BOM Categorizer

**Версия:** 4.3.3 (Modern Edition) / 3.3.0 (Standard Edition)  
**Обновлено:** 2025-01-XX

## ✅ Что такое офлайн установка?

Это установка приложения **БЕЗ подключения к интернету**. Все необходимые компоненты уже встроены в установщик:
- ✅ Python пакеты (19 wheel файлов в `offline_packages/`)
- ✅ Шрифты для PDF (DejaVu Sans в `fonts/`)
- ✅ Все исходные файлы приложения

---

## 🌟 Преимущества

### ✅ Работает БЕЗ интернета
- Установка на машинах без доступа в интернет
- Установка на защищённых корпоративных сетях
- Не зависит от доступности PyPI серверов

### ✅ Быстрая установка
- Не нужно ждать скачивания пакетов
- Установка занимает 1-2 минуты

### ✅ Гарантированная версия пакетов
- Всегда устанавливаются одни и те же версии зависимостей
- Нет риска конфликтов из-за новых версий пакетов

### ✅ Полностью автономная работа
- После установки приложение не требует интернета
- Все операции выполняются локально

---

## 🚀 Процесс установки на машине БЕЗ интернета

### Шаг 1: Получите установщик

Скопируйте файл установщика на машину без интернета:
- `BOMCategorizerSetup.exe` (Standard Edition v3.3.0+)
- `BOMCategorizerModernSetup.exe` (Modern Edition v4.3.3+)

**Размер:** ~43-276 МБ (в зависимости от версии и сжатия)

### Шаг 2: Запустите установщик

1. Дважды кликните на `.exe` файл
2. Следуйте инструкциям мастера установки
3. Выберите папку установки (по умолчанию: `%APPDATA%\BOMCategorizer` или `%APPDATA%\BOMCategorizerModern`)

### Шаг 3: Автоматическая установка

Установщик **автоматически** выполнит:

1. **Копирование файлов**
   - Все файлы приложения
   - Папка `offline_packages/` с Python пакетами (19 файлов)
   - Папка `fonts/` со шрифтами (DejaVu Sans)

2. **Создание виртуального окружения**
   - Создаст `.venv/` в папке установки
   - Использует системный Python (должен быть установлен)

3. **Установка пакетов БЕЗ интернета**
   ```powershell
   python -m pip install --no-index --find-links=offline_packages -r requirements.txt
   ```
   - `--no-index` - не обращаться к PyPI (интернет не нужен)
   - `--find-links=offline_packages` - использовать локальные пакеты

4. **Настройка базы данных**
   - Создаст папку `%APPDATA%\BOMCategorizer\Data` (Standard) или `%APPDATA%\BOMCategorizerModern\Data` (Modern)
   - Инициализирует пустую базу данных
   - Мигрирует существующую базу данных (если есть)

5. **Создание ярлыков**
   - В меню Пуск
   - На рабочем столе

### Шаг 4: Готово!

После установки приложение готово к работе:
- ✅ Запуск из меню Пуск
- ✅ Все зависимости установлены
- ✅ Шрифты для PDF готовы
- ✅ Интернет НЕ требуется

---

## 📋 Системные требования

### Обязательно:
- ✅ **Windows 10/11** (x64)
- ✅ **Python 3.13+** установлен в системе и доступен в PATH
- ✅ **~200 МБ** свободного места на диске

### НЕ требуется:
- ❌ Интернет
- ❌ Дополнительные библиотеки
- ❌ Права администратора (установка в пользовательскую папку)

### Требования для сборки инсталлятора (для разработчиков):
- Inno Setup 6.4.3+
- Python 3.13+ (для скачивания пакетов)
- Интернет (только для первоначального скачивания пакетов в `offline_packages/`)

---

## 📦 Что входит в установщик?

### Python пакеты (19 файлов в `offline_packages/`):

| Пакет | Версия | Назначение |
|-------|--------|------------|
| pandas | 2.3.3 | Обработка данных |
| numpy | 2.3.3 | Численные вычисления |
| openpyxl | 3.1.5 | Работа с Excel |
| python-docx | 1.2.0 | Работа с Word |
| pywin32 | 311 | Windows API |
| PySide6 | 6.10.0 | GUI (Modern Edition) |
| reportlab | 4.4.4 | Генерация PDF |
| pillow | 12.0.0 | Обработка изображений |
| lxml | 6.0.2 | Парсинг XML/HTML |
| pytz | 2025.2 | Работа с часовыми поясами |
| И другие зависимости | - | Транзитивные зависимости |

**Всего:** 19 пакетов, ~240+ МБ wheel файлов (до сжатия)

### Шрифты (в `fonts/`):
- **DejaVuSans.ttf** - основной шрифт для PDF (поддержка кириллицы)
- **DejaVuSans-Bold.ttf** - жирный шрифт для PDF

### Исходные файлы:
- Все Python скрипты
- Конфигурационные файлы
- Документация
- Скрипты управления (BAT, PS1)

---

## 🔧 Технические детали

### Как это работает?

1. **Встроенная директория `offline_packages`**
   - Содержит все `.whl` файлы зависимостей
   - Упакована в инсталлятор с LZMA компрессией
   - Все пакеты для Python 3.13, Windows x64

2. **Умный скрипт установки (`post_install.ps1`)**
   ```powershell
   # post_install.ps1
   python -m pip install --no-index --find-links="offline_packages" -r requirements.txt
   ```
   - `--no-index` - не обращаться к PyPI
   - `--find-links` - искать пакеты в локальной директории
   - Автоматически создает виртуальное окружение

3. **Fallback на онлайн установку**
   - Если `offline_packages/` не найдена (теоретически)
   - Автоматически переключится на установку из PyPI
   - Но при правильной сборке это не требуется

---

## 🔍 Проверка установки

### 1. Проверьте логи установки

После установки проверьте файл:
```
{папка_установки}\install_log.txt
```

Должно быть:
```
[OK] Found offline packages directory
[OK] All dependencies installed successfully from offline packages
```

### 2. Проверьте установленные пакеты

Откройте PowerShell в папке установки:
```powershell
cd "C:\Users\ВашеИмя\AppData\Roaming\BOMCategorizer"
.venv\Scripts\python.exe -m pip list
```

Должны быть установлены:
- pandas
- openpyxl
- python-docx
- pywin32
- PySide6 (для Modern Edition)
- reportlab
- pillow
- И их зависимости

### 3. Запустите приложение

Меню Пуск → "BOM Categorizer Modern" (или "BOM Categorizer Standard")

---

## ⚠️ Возможные проблемы

### Проблема 1: "Python is not found"

**Решение:**
1. Установите Python 3.13+ с python.org
2. При установке отметьте "Add Python to PATH"
3. Перезапустите установщик

### Проблема 2: "offline_packages directory not found"

**Решение:**
1. Установщик поврежден или неполный
2. Скачайте установщик заново
3. Проверьте размер файла (должен быть ~43-276 МБ)

### Проблема 3: "Some packages could not be installed"

**Решение:**
1. Проверьте версию Python: `python --version` (должна быть 3.13+)
2. Проверьте архитектуру: должна быть x64 (64-bit)
3. Запустите `repair_install.bat` из папки установки

### Проблема 4: Установщик пытается подключиться к интернету

**Это нормально!** Установщик:
1. Сначала пытается установить из `offline_packages/` (БЕЗ интернета)
2. Если что-то не найдено, пытается установить из PyPI (требует интернет)

Если у вас нет интернета и установка не удалась:
- Проверьте, что все файлы в `offline_packages/` на месте (19 файлов)
- Проверьте версию Python (должна быть 3.13)
- Проверьте архитектуру (должна быть x64)

---

## 🔧 Ручная установка (если автоматическая не сработала)

Если установщик не сработал, можно установить вручную:

### 1. Распакуйте установщик

Используйте 7-Zip или WinRAR для извлечения файлов из `.exe`

### 2. Перейдите в папку установки

```powershell
cd "C:\Users\ВашеИмя\AppData\Roaming\BOMCategorizer"
```

### 3. Создайте виртуальное окружение

```powershell
python -m venv .venv
```

### 4. Активируйте виртуальное окружение

```powershell
.venv\Scripts\activate
```

### 5. Установите пакеты из offline_packages

```powershell
python -m pip install --no-index --find-links=offline_packages -r requirements.txt
```

### 6. Запустите приложение

```powershell
# Для Modern Edition
python app_qt.py

# Для Standard Edition
python app.py
```

---

## 💾 Копирование на полностью изолированную машину

Если у вас есть доступ к установленному приложению на одной машине и нужно перенести его на **полностью изолированную машину** (без интернета и без возможности запуска установщика):

### Вариант 1: Копирование установленного приложения (рекомендуется)

#### Шаг 1: Подготовка на машине с установленным приложением

1. Найдите папку установки:
   - **Standard Edition:** `%APPDATA%\BOMCategorizer\`
   - **Modern Edition:** `%APPDATA%\BOMCategorizerModern\`

2. Скопируйте **всю папку** установки на внешний носитель (USB, внешний диск):
   ```
   C:\Users\ВашеИмя\AppData\Roaming\BOMCategorizer\
   ├── .venv\              # Виртуальное окружение (уже настроено)
   ├── offline_packages\   # Python пакеты (19 файлов)
   ├── fonts\              # Шрифты для PDF (2 файла)
   ├── bom_categorizer\    # Исходный код
   ├── app.py              # Или app_qt.py для Modern Edition
   ├── requirements.txt
   └── ... (все остальные файлы)
   ```

#### Шаг 2: Перенос на изолированную машину

1. Скопируйте всю папку на изолированную машину в то же место:
   ```
   C:\Users\Пользователь\AppData\Roaming\BOMCategorizer\
   ```

2. **Важно:** Убедитесь, что на изолированной машине установлен **Python 3.13+** и доступен в PATH

3. Запустите приложение:
   ```powershell
   cd "C:\Users\Пользователь\AppData\Roaming\BOMCategorizer"
   .venv\Scripts\python.exe app_qt.py
   ```

**✅ Этого достаточно!** Виртуальное окружение уже настроено, пакеты установлены.

---

### Вариант 2: Копирование только необходимых компонентов

Если нужно скопировать только минимально необходимое:

#### Шаг 1: Скопируйте папки и файлы

С машины с установленным приложением скопируйте:

1. **Папку `offline_packages/`** (19 wheel файлов):
   ```
   offline_packages\
   ├── charset_normalizer-3.4.4-cp313-cp313-win_amd64.whl
   ├── pillow-12.0.0-cp313-cp313-win_amd64.whl
   ├── reportlab-4.4.4-py3-none-any.whl
   ├── shiboken6-6.10.0-cp39-abi3-win_amd64.whl
   ├── pyside6_essentials-6.10.0-cp39-abi3-win_amd64.whl
   ├── pyside6_addons-6.10.0-cp39-abi3-win_amd64.whl
   ├── pyside6-6.10.0-cp39-abi3-win_amd64.whl
   ├── tzdata-2025.2-py2.py3-none-any.whl
   ├── typing_extensions-4.15.0-py3-none-any.whl
   ├── pywin32-311-cp313-cp313-win_amd64.whl
   ├── six-1.17.0-py2.py3-none-any.whl
   ├── pandas-2.3.3-cp313-cp313-win_amd64.whl
   ├── python_dateutil-2.9.0.post0-py2.py3-none-any.whl
   ├── python_docx-1.2.0-py3-none-any.whl
   ├── pytz-2025.2-py2.py3-none-any.whl
   ├── openpyxl-3.1.5-py2.py3-none-any.whl
   ├── numpy-2.3.3-cp313-cp313-win_amd64.whl
   ├── lxml-6.0.2-cp313-cp313-win_amd64.whl
   └── et_xmlfile-2.0.0-py3-none-any.whl
   ```

2. **Папку `fonts/`** (2 шрифта):
   ```
   fonts\
   ├── DejaVuSans.ttf
   └── DejaVuSans-Bold.ttf
   ```

3. **Все исходные файлы проекта** (из папки установки):
   - `bom_categorizer/` (вся папка)
   - `app.py` или `app_qt.py`
   - `requirements.txt`
   - `config.json` или `config_qt.json`
   - `rules.json`
   - И другие необходимые файлы

#### Шаг 2: Установка на изолированной машине

1. Создайте папку для приложения:
   ```powershell
   mkdir "C:\BOMCategorizer"
   cd "C:\BOMCategorizer"
   ```

2. Скопируйте все файлы в эту папку, сохраняя структуру:
   ```
   C:\BOMCategorizer\
   ├── offline_packages\    # Скопированная папка
   ├── fonts\                # Скопированная папка
   ├── bom_categorizer\      # Скопированная папка
   ├── app_qt.py
   ├── requirements.txt
   └── ... (остальные файлы)
   ```

3. Создайте виртуальное окружение:
   ```powershell
   python -m venv .venv
   ```

4. Активируйте виртуальное окружение:
   ```powershell
   .venv\Scripts\activate
   ```

5. Установите пакеты из `offline_packages/`:
   ```powershell
   python -m pip install --no-index --find-links=offline_packages -r requirements.txt
   ```

6. Запустите приложение:
   ```powershell
   python app_qt.py
   ```

---

### ⚠️ Важные замечания

1. **Python должен быть установлен** на изолированной машине (3.13+)
2. **Архитектура должна совпадать** (x64)
3. **Пути к папкам:** Можно разместить в любом месте, но структура должна сохраняться:
   ```
   {любая_папка}/
   ├── offline_packages/    # Должна быть на том же уровне
   ├── fonts/                # Должна быть на том же уровне
   ├── bom_categorizer/
   └── app_qt.py
   ```

4. **Размеры:**
   - `offline_packages/` - ~240 МБ (19 файлов)
   - `fonts/` - ~1.4 МБ (2 файла)
   - Исходный код - ~5-10 МБ

5. **Проверка:** После установки проверьте:
   ```powershell
   .venv\Scripts\python.exe -m pip list
   ```
   Должны быть установлены: pandas, openpyxl, PySide6, reportlab и т.д.

---

### ✅ Будет ли этого достаточно?

**Да, этого достаточно!** Если вы скопировали:
- ✅ Папку `offline_packages/` (19 файлов)
- ✅ Папку `fonts/` (2 файла)
- ✅ Все исходные файлы проекта
- ✅ И установили пакеты из `offline_packages/`

Приложение будет работать полностью офлайн на изолированной машине.

---

## ✅ Проверка офлайн работы

После установки приложение работает **полностью офлайн**:

- ✅ Обработка Excel/Word файлов
- ✅ Категоризация компонентов
- ✅ Экспорт в Excel/PDF/TXT
- ✅ Работа с базой данных
- ✅ Все функции доступны без интернета

---

## 🔄 Обновление offline_packages (для разработчиков)

Если нужно обновить версии пакетов для будущих сборок:

### 1. Скачать новые пакеты

```powershell
# Удалить старые
Remove-Item offline_packages\* -Force

# Скачать актуальные версии
python -m pip download -r requirements_install.txt `
    -d offline_packages `
    --only-binary=:all: `
    --platform win_amd64 `
    --python-version 313
```

### 2. Пересобрать инсталлятор

```powershell
# Используйте build_installer.py
python build_installer.py
```

Или вручную:
```powershell
# Копировать в temp_installer
Copy-Item offline_packages temp_installer\offline_packages -Recurse -Force

# Скомпилировать
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer_qt.iss
```

---

## 📝 Дополнительная информация

### Где хранятся данные?

- **Приложение (Standard):** `%APPDATA%\BOMCategorizer\`
- **Приложение (Modern):** `%APPDATA%\BOMCategorizerModern\`
- **База данных (Standard):** `%APPDATA%\BOMCategorizer\Data\component_database.json`
- **База данных (Modern):** `%APPDATA%\BOMCategorizerModern\Data\component_database.json`
- **Резервные копии:** `{Data}\database_backups\`

### Как обновить приложение?

1. Скачайте новую версию установщика
2. Запустите установку поверх старой
3. База данных сохранится автоматически (миграция выполнится автоматически)

### Как удалить приложение?

1. Панель управления → Программы → Удаление
2. Или запустите `uninstall.exe` из папки установки

**Важно:** База данных НЕ удаляется! Она остается в `%APPDATA%\BOMCategorizer\Data\` или `%APPDATA%\BOMCategorizerModern\Data\`

---

## ❓ FAQ

**Q: Можно ли установить на компьютер без интернета?**  
A: Да! Это и есть цель офлайн инсталлятора. Все необходимые компоненты встроены.

**Q: Нужен ли Python предустановленный?**  
A: Да, Python 3.13+ должен быть установлен и доступен в PATH.

**Q: Можно ли обновить пакеты после установки?**  
A: Да, но потребуется интернет. Лучше пересобрать инсталлятор с новыми версиями.

**Q: Работает ли приложение офлайн после установки?**  
A: Да, приложение полностью офлайн - не обращается к интернету.

**Q: Какой размер инсталлятора?**  
A: ~43-276 МБ (в зависимости от версии и сжатия LZMA)

**Q: Что будет с базой данных при деинсталляции?**  
A: База данных НЕ удаляется! Она хранится в `%APPDATA%\BOMCategorizer\Data\` независимо от программы.

**Q: Как перенести базу данных на другой ПК?**  
A: Используйте `database_export.bat` для экспорта в Excel, затем импортируйте на другом ПК.

**Q: Где найти инструменты управления базой данных?**  
A: В папке установки найдите BAT файлы: `database_stats.bat`, `database_backup.bat`, `database_export.bat`

**Q: В чем разница между Standard и Modern Edition?**  
A: 
- **Standard Edition** - Tkinter GUI, стабильная версия
- **Modern Edition** - PySide6 GUI, современный дизайн, экспериментальные функции

---

## 📚 История версий

### v4.3.3+ (Modern Edition) - Текущая версия
- ✅ Полная поддержка PySide6
- ✅ Шрифты для PDF включены в установщик
- ✅ 19 пакетов в offline_packages (включая PySide6, reportlab)
- ✅ Автоматическая миграция базы данных

### v4.0.0 (09.11.2025) - Modern Edition ✨
- ✨ **Modern Edition на PySide6** - современный дизайн
- ⭐ **Горячие клавиши** - Ctrl+O, Ctrl+R, Ctrl+Q
- 🖱️ **Drag & Drop** - перетаскивание файлов
- 📚 **Меню "Помощь"** - контекстная помощь (F1)
- 🔍 **Масштабирование интерфейса** - 80% до 150%

### v3.3.0+ (Standard Edition) - Текущая версия
- ✅ Стабильная версия на Tkinter
- ✅ Все функции работают офлайн
- ✅ База данных компонентов

### v3.0.0 (01.11.2025) - Database Management Edition 🎉
- 🎉 **Полная система управления базой данных компонентов**
- 📊 База данных с метаданными
- 🛡️ Постоянное хранение данных в %APPDATA%
- 🔧 Инструменты управления: stats, export, import, backup, restore

### v2.0.33 (31.10.2025)
- Парсинг многострочных элементов из DOC
- Извлечение подборных элементов PAT/РАТ
- База данных компонентов в инсталляторе
- Скрипты восстановления установки

### v1.1.0 (06.10.2025) - Offline Edition
- ✅ Полностью офлайн инсталлятор
- ✅ Встроены все Python зависимости
- ✅ Не требует интернет для установки

---

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте `install_log.txt` в папке установки
2. Запустите `repair_install.bat` для восстановления
3. Проверьте документацию в `docs/` папке установки
4. Убедитесь, что Python 3.13+ установлен и доступен в PATH

---

## 📚 Дополнительная документация

- 📘 [QUICK_START.md](QUICK_START.md) - Быстрый старт приложения
- 📘 [TESTING_GUIDE.md](TESTING_GUIDE.md) - Руководство по тестированию
- 📊 [DATABASE_MANAGEMENT_GUIDE.md](DATABASE_MANAGEMENT_GUIDE.md) - Управление базой данных
- 📚 [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура проекта
- 🔧 [REPAIR_INSTALLATION.md](REPAIR_INSTALLATION.md) - Восстановление установки

---

**Версия документа:** 2.0  
**Обновлено:** 2025-01-XX  
**Для версии:** BOM Categorizer Standard 3.3.0+ / Modern Edition 4.3.3+  
**Автор:** Куреин М.Н. / Kurein M.N.
