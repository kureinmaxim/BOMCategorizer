# 🧪 Инструкция по тестированию инсталлятора v1.0.9

## 📋 Подготовка к тестированию

### Шаг 1: Полное удаление предыдущей установки

Откройте PowerShell **от имени администратора** и выполните:

```powershell
# Удалить директорию приложения
Remove-Item -Path "$env:APPDATA\BOMCategorizer" -Recurse -Force -ErrorAction SilentlyContinue

# Проверить, что папка удалена
Test-Path "$env:APPDATA\BOMCategorizer"
# Должно вернуть: False
```

---

## 🚀 Шаг 2: Установка новой версии

1. **Запустите** `BOMCategorizerSetup.exe` (версия 1.0.9)
2. **Следуйте** инструкциям инсталлятора
3. **Дождитесь** завершения установки (процесс установки Python окружения может занять 1-2 минуты)

---

## 🔍 Шаг 3: Проверка лога установки

Откройте лог-файл для проверки:

```powershell
notepad "$env:APPDATA\BOMCategorizer\install_log.txt"
```

### ✅ Что должно быть в логе:

1. **Правильная рабочая директория:**
   ```
   Script execution directory: C:\Users\z6364\AppData\Roaming\BOMCategorizer
   Current working directory: C:\Users\z6364\AppData\Roaming\BOMCategorizer
   ```

2. **Успешное создание виртуального окружения:**
   ```
   Creating new virtual environment at: .venv
   Virtual environment created successfully.
   ```

3. **Установка pip (один из вариантов):**
   
   **Вариант A** - pip.exe найден:
   ```
   Checking for pip executable...
   Scripts directory contents:
     - Activate.ps1
     - activate
     - activate.bat
     - deactivate.bat
     - pip.exe
     - pip3.exe
     - pip3.13.exe
     - python.exe
     - pythonw.exe
   Found pip at: C:\Users\z6364\AppData\Roaming\BOMCategorizer\.venv\Scripts\pip.exe
   ```
   
   **Вариант B** - используется python -m pip:
   ```
   Checking for pip executable...
   Scripts directory contents:
     - python.exe
     - pythonw.exe
   No pip.exe found, will use 'python -m pip' instead
   pip 24.3.1 from ...
   python -m pip works, using this method
   ```

4. **Успешная установка зависимостей:**
   ```
   Upgrading pip...
   Installing application dependencies...
   Successfully installed pandas-X.X.X openpyxl-X.X.X python-docx-X.X.X pywin32-XXX
   Installation script finished successfully.
   ```

---

## ▶️ Шаг 4: Запуск приложения

1. Откройте **меню Пуск**
2. Найдите **"BOM Categorizer"**
3. Запустите приложение

Или запустите вручную:

```powershell
cd "$env:APPDATA\BOMCategorizer"
.\run_app.bat
```

---

## 🐛 Если что-то пошло не так

### Проблема: Установка зависла

- Подождите 3-5 минут (установка пакетов может занять время)
- Проверьте лог-файл на наличие ошибок

### Проблема: Приложение не запускается

1. Проверьте файл `debug_log.txt` в папке приложения:
   ```powershell
   notepad "$env:APPDATA\BOMCategorizer\debug_log.txt"
   ```

2. Проверьте установленные пакеты:
   ```powershell
   cd "$env:APPDATA\BOMCategorizer"
   .\.venv\Scripts\python.exe -m pip list
   ```

### Проблема: Ошибка в логе установки

Отправьте полный лог разработчику для анализа:
```powershell
Get-Content "$env:APPDATA\BOMCategorizer\install_log.txt"
```

---

## ✨ Ключевые улучшения версии 1.0.9

1. ✅ **Правильная рабочая директория** - venv создаётся в папке приложения
2. ✅ **Умная установка pip** - проверка нескольких вариантов (pip.exe, pip3.exe, pip3.13.exe)
3. ✅ **Fallback на `python -m pip`** - работает даже без pip.exe
4. ✅ **Расширенная диагностика** - полный лог процесса установки
5. ✅ **Совместимость с Python 3.13** на Windows

---

*Создано: 06.10.2025*
*Версия инсталлятора: 1.0.9*

