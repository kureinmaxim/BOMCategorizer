# ⚡ Быстрая шпаргалка (v1.8.3)

## 🚀 Запуск из проекта (Windows)

> 🆕 **Новое в v1.8.3:** Унификация DOCX/XLSX - файлы обрабатываются одинаково перед объединением  
> 📂 **Новое в v1.8.3:** Разделение всех категорий по источникам (пустые строки между файлами)  
> 🔄 **v1.8.2:** Перенос компонентов в "Не распределено" через GUI + исправлена сортировка  
> 🔒 **v1.7.5+:** GUI приложение защищено PIN-кодом для безопасности

### Активация виртуального окружения

```powershell
# В PowerShell (рекомендуется)
.\.venv\Scripts\Activate.ps1

# Или в CMD
.venv\Scripts\activate.bat
```

**После активации** командная строка будет выглядеть так:
```
(.venv) PS C:\Project\ProjectSnabjenie>
```

Теперь можно запускать команды просто через `python`:

```bash
# Запуск GUI
python app.py

# Запуск CLI
python split_bom.py --inputs "myfile.xlsx" --xlsx result.xlsx --combine
```

### ⚡ Быстрые команды БЕЗ активации

Если не хотите активировать окружение каждый раз:

```powershell
# Запуск GUI
.\.venv\Scripts\python.exe app.py

# Запуск CLI
.\.venv\Scripts\python.exe split_bom.py --inputs "myfile.xlsx" --xlsx result.xlsx --combine
```

---

## 🎯 Основные сценарии использования

### Сценарий 1: Быстрая обработка файла

```bash
# Простейший вариант
python split_bom.py --inputs "myfile.xlsx" --xlsx result.xlsx --combine

# С экспортом в TXT (рекомендуется!)
python split_bom.py --inputs "myfile.xlsx" --xlsx result.xlsx --txt-dir result_txt --combine

# Или БЕЗ активации виртуального окружения:
.\.venv\Scripts\python.exe split_bom.py --inputs "myfile.xlsx" --xlsx result.xlsx --txt-dir result_txt --combine
```

**Результат:**
- 📄 `result.xlsx` - основной файл с листами по категориям
- 📁 `result_txt/` - папка с TXT файлами для удобного просмотра

---

### Сценарий 2: Интерактивная классификация

Это самый эффективный способ "научить" программу правильно распознавать ваши компоненты.

```bash
python interactive_classify.py --input "myfile.xlsx" --output result.xlsx

# Или БЕЗ активации:
.\.venv\Scripts\python.exe interactive_classify.py --input "myfile.xlsx" --output result.xlsx
```

**Процесс:**
1.  Скрипт сам проведет первичную классификацию.
2.  Затем в удобном меню покажет вам все нераспознанные элементы.
3.  Вы выберете для них правильные категории.
4.  Скрипт автоматически создаст правила в `rules.json`.
5.  В конце сессии он **сразу же запустит финальную обработку** и создаст `result.xlsx`.

**Результат:**
- ✅ Все элементы правильно классифицированы.
- ✅ Правила сохранены для будущих файлов.
- ✅ Готовый `result.xlsx` создан автоматически.

---

### Сценарий 3: Обработка нескольких файлов (с умным объединением)

```bash
python split_bom.py --inputs "file1.xlsx" "file2.doc" "file3.txt" --xlsx combined.xlsx --txt-dir combined_txt --assign-json rules.json --combine

# Или БЕЗ активации:
.\.venv\Scripts\python.exe split_bom.py --inputs "file1.xlsx" "file2.doc" "file3.txt" --xlsx combined.xlsx --txt-dir combined_txt --assign-json rules.json --combine
```

**Результат:**
- ✅ Все файлы объединены в один результат
- ✅ DOCX/XLSX унифицируются (ТУ-коды извлекаются одинаково)
- ✅ Одинаковые компоненты из разных форматов корректно объединяются
- ✅ Каждая категория разделена пустыми строками между источниками
- ✅ Применены существующие правила
- ✅ Создан лист SUMMARY с общей комплектацией

**Пример объединения:**
```
Файл 1 (Плата_4.xlsx):  1272ПН3Т  | АЕЯР.431320.420ТУ | 2 шт
Файл 2 (Плата_4.docx):  1272ПН3Т  | АЕЯР.431320.420ТУ | 3 шт
                        ↓ (унификация + объединение)
Результат:               1272ПН3Т  | АЕЯР.431320.420ТУ | 5 шт
```

---

### Сценарий 4: Использование GUI с PIN защитой 🔒

```bash
python app.py

# Или БЕЗ активации:
.\.venv\Scripts\python.exe app.py

# Или через BAT файл:
start_gui.bat
```

**В интерфейсе:**
1. 🔓 Разблокируйте приложение (двойной клик по имени разработчика, PIN: `5421`)
2. Добавьте файлы
3. ✅ Включите "Создать TXT по категориям"
4. Укажите файл правил (если есть)
5. ✅ Включите "Суммарная комплектация (SUMMARY)"
6. Нажмите "Запустить"
7. 🎯 При наличии нераспределенных элементов система предложит интерактивную классификацию

### Сценарий 5: Перенос неправильно классифицированных компонентов 🔄 (НОВОЕ v1.8.2)

**Если после обработки обнаружили ошибки в категориях:**

1. Откройте результат в Excel и найдите неправильно классифицированные компоненты
2. Скопируйте их названия (можно частично)
3. В GUI вставьте названия в поле "Перенос компонентов в 'Не распределено'" (по одному на строку)
4. Нажмите "Перенести в 'Не распределено'"
5. Нажмите "Интерактивная классификация"
6. Выберите правильные категории

**Пример:**
```
Модуль передающий MP2320
Аттенюатор оптический
NT1 ф.Chengdu
Коммутатор IGS-500T
```

**Преимущества:**
- ✅ Поиск по частичному совпадению (регистронезависимый)
- ✅ Автоматический перенос из всех категорий
- ✅ Сохранение всех данных компонента
- ✅ Интеграция с интерактивной классификацией

---

## 📋 Частые команды

### Обработка DOC файла
```bash
python split_bom.py --inputs "example/БЗ.doc" --xlsx result.xlsx --txt-dir result_txt --combine
```

### Обработка с правилами
```bash
python split_bom.py --inputs "file.xlsx" --xlsx result.xlsx --assign-json rules.json --combine
```

### Интерактивный режим
```bash
python interactive_classify.py --input "file.xlsx" --rules rules.json
```

### Обработка конкретных листов XLSX
```bash
python split_bom.py --inputs "file.xlsx" --sheets "3,4" --xlsx result.xlsx --combine
```

> 💡 **Примечание:** Все команды выше предполагают активированное виртуальное окружение. Если не активировали, добавьте `.\.venv\Scripts\` перед `python.exe`

---

## 🎨 Интерактивный режим: Подсказка

Когда видите элемент вроде:
```
📝 [W1] Линия задержек программируемая PADL3-20
```

**Выбирайте категорию по смыслу:**
- **W** (волновод) + "линия задержек" → `8` (СВЧ модули)
- **А** (аттенюатор) + "оптический" → `7` (Оптические компоненты)
- **U** + "модуль оптический" → `7` (Оптические компоненты)
- **A** + "плата контроллера ШСК-М" → `12` (Наши разработки)
- **S** + "тумблер" → `13` (Другие компоненты)

**Создастся правило:** следующий файл с похожим элементом классифицируется автоматически!

---

## 📂 Структура результатов

После обработки вы получите:

```
result.xlsx                    ← Основной файл с листами
result_txt/
  ├── Резисторы.txt           ← Удобно для просмотра
  ├── Конденсаторы.txt
  ├── Оптические компоненты.txt
  ├── СВЧ модули.txt
  ├── Микросхемы.txt
  ├── Разъемы.txt
  ├── Наши разработки.txt
  └── ...
```

**Листы в result.xlsx:**
- Резисторы, Конденсаторы, Дроссели, Микросхемы, Разъемы
- Оптические компоненты, СВЧ модули, Кабели
- Модули питания, Диоды, Наши разработки, Другие
- Не распределено
- **SUMMARY** (общая комплектация)
- **SOURCES** (список входных файлов)

---

## 💡 Полезные советы

### 1. Всегда используйте --txt-dir
TXT файлы намного удобнее для быстрого просмотра, чем Excel.

### 2. Начинайте с интерактивного режима
Создайте базовый набор правил для типовых компонентов.

### 3. Проверяйте "Не распределено.txt"
Этот файл покажет что требует дополнительной классификации.

### 4. Используйте --combine
Лист SUMMARY очень удобен для анализа общего состава.

### 5. Сохраняйте rules.json
Этот файл - ваша база знаний о компонентах!

---

## 🆘 Если что-то пошло не так

### Проблема: Неправильная категория

**Решение 1:** Используйте интерактивный режим
```bash
python interactive_classify.py --input "file.xlsx"
```

**Решение 2:** Отредактируйте `rules.json` вручную

### Проблема: Слишком много в "Не распределено"

**Решение:** 
1. Добавьте флаг `--loose` для менее строгой классификации
2. Используйте интерактивный режим для создания правил

### Проблема: Кодировка в консоли

**Решение:**
- Используйте `interactive_classify.py` (с исправленной кодировкой)
- Или используйте GUI (`app.py`)

---

## 📚 Документация

- `README.md` - основная документация
- `TXT_EXPORT_GUIDE.md` - подробно про TXT экспорт
- `INTERACTIVE_MODE_GUIDE.md` - подробно про интерактивный режим
- `SUMMARY_OF_IMPROVEMENTS.md` - все улучшения проекта

---

## 🔧 Для разработчиков

### Сборка инсталлятора

Самый простой способ собрать `.exe` инсталлятор:

```powershell
python build_installer.py
```

Этот скрипт автоматически:
1. ✅ Создаст временную директорию `temp_installer/`
2. ✅ Скопирует все необходимые файлы
3. ✅ Скопирует модульную структуру `bom_categorizer/`
4. ✅ Скопирует offline пакеты
5. ✅ Запустит Inno Setup Compiler
6. ✅ Создаст `BOMCategorizerSetup.exe`

**Требования:**
- Установленный Inno Setup 6
- Python 3.13+
- Все файлы проекта в корне

**Подробнее:** См. `BUILD.md`

---

**Приятной работы!** 🚀

