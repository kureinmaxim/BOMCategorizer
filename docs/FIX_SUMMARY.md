# Исправления BOM Categorizer

## v2.0.14 - Исправление интерактивной классификации после переноса компонентов

### ❌ Проблема:
После использования функции "Перенос компонентов в 'Не распределено'" кнопка "Интерактивная классификация" выдавала ошибку:
```
Не удалось прочитать нераспределенные элементы: 
Worksheet named 'Не распределено' not found
```

**Сценарий возникновения:**
1. Пользователь обработал большой файл → получил `categorized.xlsx`
2. Обнаружил ошибочно классифицированные компоненты (например, "Модуль электропитания" попали в "Микросхемы")
3. Перенес 9 компонентов в "Не распределено" (успешно)
4. Нажал "Интерактивная классификация" → **ОШИБКА** ❌

### ✅ Решение:
Изменена логика метода `on_interactive_classify()` в `bom_categorizer/gui.py`:

**Было:**
- Кнопка всегда создавала НОВЫЙ временный файл
- Игнорировала существующий файл с перенесенными компонентами
- Заново обрабатывала входные файлы

**Стало:**
1. **Сначала проверяет существующий файл:**
   - Если выходной файл существует И содержит лист "Не распределено" с элементами
   - → Использует СУЩЕСТВУЮЩИЙ файл для классификации ✅

2. **Fallback на новую обработку:**
   - Если файла нет или лист "Не распределено" пуст
   - → Создает новый временный файл и обрабатывает входные данные

### 📊 Правильный рабочий процесс:
```
1. Запуск обработки → categorized.xlsx создан
2. Перенос ошибочных в "Не распределено" → 9 компонентов перенесены
3. "Интерактивная классификация" → 
   📂 Используется существующий файл: categorized.xlsx
   📊 Найдено 9 нераспределенных элементов
   ✅ Открывается окно классификации
```

### 🎯 Применение:
- ✅ Работает с существующими файлами после переноса компонентов
- ✅ Сохраняет обратную совместимость (создает новый файл, если нужно)
- ✅ Выводит информативные сообщения о используемом файле

---

## v2.0.12 - Автоматическое добавление знака ± перед процентами

### 📋 Задача:
Добавить знак плюс-минус (±) перед всеми процентами допуска компонентов для стандартизации формата.

**Примеры требуемого формата:**
- `P1 - 12 - 0,125 - 22 кОм ± 5% - М` ✅
- `100 Ом ± 2%-Т` ✅
- `220 пФ ± 10%-М` ✅

### ✅ Решение:
Добавлена функция `add_plus_minus_to_percentages()` в `bom_categorizer/excel_writer.py`:

1. **Автоматическое добавление знака ±:**
   - Функция находит все проценты после единиц измерения
   - Добавляет ` ± ` (с пробелами) между единицей и процентом
   - `100 Ом 5%-Т` → `100 Ом ± 5%-Т`
   - `1 кОм2%` → `1 кОм ± 2%`

2. **Защита от дублирования:**
   - Проверяет, нет ли уже знака ± перед найденным процентом
   - Если знак уже есть, повторно не добавляется
   - `100 Ом ± 5%` → `100 Ом ± 5%` (не меняется)

3. **Применение:**
   - Функция применяется автоматически при записи в Excel
   - Работает для всех категорий компонентов
   - Обрабатывает колонку "Наименование ИВП" перед записью

### 📊 Технические детали:
```python
# Паттерн поиска: единица измерения + процент
pattern = r'(Ом|пФ|нФ|мкФ|мФ|кОм|МОм|Гн|мГн|мкГн|нГн|\d)\s*(\d+%)'

# Замена: добавление ± между единицей и процентом
result = f"{unit_or_digit} ± {percentage}"
```

### 🎯 Применение:
- ✅ Работает для **ВСЕХ категорий**: резисторы, конденсаторы, индуктивности
- ✅ Применяется автоматически при обработке любых файлов
- ✅ Совместимо с нормализацией пробелов (v2.0.11)

---

## v2.0.11 - Нормализация описаний компонентов

### ❌ Проблема:
Одинаковые компоненты с незначительными различиями в форматировании (пробелы вокруг дефисов, пробелы перед процентами) попадали в разные строки вместо объединения.

**Примеры:**
- `P1-12-0,1-100 Ом 5%-Т` (4 шт) и `P1-12-0,1-100 Ом5%-Т` (47 шт) → две разные строки ❌
- `P1 - 12 - 0,125 - 1 кОм 5% - М` (5 шт) и `P1 - 12 - 0,125-1 кОм 5% - М` (3 шт) → две разные строки ❌

### ✅ Решение:
Улучшена функция `normalize_description()` в `bom_categorizer/main.py`:

1. **Нормализация пробелов вокруг дефисов:**
   - Все вариации приводятся к единому формату: ` - ` (пробел-дефис-пробел)
   - `P1-12-0,1-100` → `P1 - 12 - 0,1 - 100`
   - `P1 - 12 - 0,125-1` → `P1 - 12 - 0,125 - 1`

2. **Нормализация пробелов между единицами измерения и процентами:**
   - Всегда добавляется пробел перед процентами
   - `100 Ом5%-Т` → `100 Ом 5%-Т`
   - `1 кОм5%` → `1 кОм 5%`

3. **Агрегация при флаге --combine:**
   - Теперь агрегация всегда выполняется при использовании `--combine`, даже для XLSX файлов
   - Это позволяет объединять дубликаты при повторной обработке файлов

### 📊 Результаты:
```
До нормализации:
  P1-12-0,1-100 Ом 5%-Т    4 шт
  P1-12-0,1-100 Ом5%-Т    47 шт
  
После нормализации:
  P1 - 12 - 0,1 - 100 Ом 5% - Т    51 шт  ✅
```

### 🎯 Применение:
- ✅ Работает для **ВСЕХ категорий**: резисторы, конденсаторы, индуктивности, микросхемы, и т.д.
- ✅ Применяется автоматически при обработке любых файлов
- ✅ Совместимо с функцией `--combine` для объединения файлов

---

## v1.8.x - Категоризация компонентов

### Что было исправлено:

### ❌ Проблема:
Компоненты неправильно сортировались по категориям. Многие элементы попадали в "Не распределено".

### ✅ Решение:
Исправлена функция классификации в файле `split_bom.py`:

1. **Добавлена поддержка русских обозначений компонентов:**
   - `А1`, `А2` → Оптические компоненты (аттенюаторы)
   - `W1`, `W2` → СВЧ модули (усилители, линии задержек)
   - `WS1-WS3` → СВЧ делители
   - `WU1-WU4` → СВЧ аттенюаторы
   - `H1`, `H2` → Индикаторы/светодиоды
   - `S4` → Тумблеры/переключатели

2. **Расширены ключевые слова для распознавания:**
   - **Оптика**: "аттенюат", "fc/apc", "fc/upc", "pigtail"
   - **СВЧ**: "линия задержек", "делитель", "усилитель", "polaris", "mini-circuits"
   - **Переключатели**: "тумблер", "кнопка", "переключ"

## Результаты на примере БЗ.doc:

| Категория | До | После | Улучшение |
|-----------|-----|-------|-----------|
| Оптические компоненты | 8 | **10** | ✅ +2 |
| СВЧ модули | 11 | **18** | ✅ +7 |
| Другие (тумблеры) | 2 | **3** | ✅ +1 |
| **Не распределено** | **67** | **57** | ✅ **-10** |

## Как использовать:

### Через GUI (app.py):
```bash
.venv\Scripts\python.exe app.py
```
1. Добавьте файлы (XLSX/DOC/DOCX/TXT)
2. Укажите выходной файл
3. Нажмите "Запустить"

### Через командную строку:
```bash
.venv\Scripts\python.exe split_bom.py --inputs "example/БЗ.doc" --xlsx categorized.xlsx --combine
```

## Категории компонентов:

✅ **Работают правильно:**
- Резисторы (R)
- Конденсаторы (C)
- Дроссели (L)
- Микросхемы (U, DD, DA, IC)
- Разъемы (J, X, P, K)
- **Оптика (А, ключевые слова)**
- **СВЧ модули (W, WS, WU, ключевые слова)**
- **Индикаторы (H)**
- **Тумблеры (S + ключевые слова)**
- Отладочные платы
- Кабели
- Модули питания
- Наши разработки
- Другие компоненты

## Примечания:

- Элементы в категории "Не распределено" (57 шт) в основном пустые строки или элементы без описания
- Для дополнительной настройки и обучения системы используйте интерактивный скрипт:

```bash
.venv\Scripts\python.exe interactive_classify.py --input "example/БЗ.doc"
```

---
**Автор исправлений:** AI Assistant  
**Дата:** 03.10.2025  
**Версия:** 1.0.4+fix

