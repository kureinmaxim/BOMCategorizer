# История изменений инсталлятора

## Версия 1.1.0 (06.10.2025) - OFFLINE EDITION ✅🌟

### 🎉 Полностью офлайн инсталлятор
- **Не требует интернета для установки**
  - Все Python пакеты (~38 МБ) встроены в инсталлятор
  - Установка через `pip install --no-index --find-links=offline_packages`
  - Работает на изолированных/корпоративных сетях без доступа к PyPI

### Встроенные пакеты (12 wheel файлов):
- pandas-2.3.3 (11 MB)
- numpy-2.3.3 (12.8 MB)  
- pywin32-311 (9.5 MB)
- lxml-6.0.2 (4 MB)
- openpyxl-3.1.5, python-docx-1.2.0
- + транзитивные зависимости

### Исправления в приложении:
- ✅ Исправлен импорт `scrolledtext` в app.py (было: `import scrolledtext`, стало: `from tkinter import scrolledtext`)
- ✅ Улучшен run_app.bat для поиска pip3.exe/pip.exe
- ✅ Добавлен fallback на `python -m pip list` в run_app.bat

### Технические улучшения:
- Убрана зависимость от интернета в post_install.ps1 (удалён fallback на get-pip.py)
- Добавлена директория offline_packages в инсталлятор
- Размер инсталлятора: ~39.5 МБ (со сжатием LZMA)

---

## Версия 1.0.9 (06.10.2025)

### Улучшения установки pip
- **Добавлена расширенная диагностика pip**
  - Вывод содержимого директории `.venv\Scripts` в лог
  - Проверка альтернативных имён pip: `pip.exe`, `pip3.exe`, `pip3.13.exe`
  - Задержка 2 секунды после установки pip для синхронизации файловой системы
  
- **Fallback на `python -m pip`**
  - Если pip.exe не найден, автоматически используется `python -m pip`
  - Проверка работоспособности `python -m pip --version` перед использованием
  - Гарантированная установка зависимостей даже без pip.exe

### Технические улучшения
- Универсальная логика работы с pip, подходящая для разных конфигураций Python
- Детальное логирование каждого этапа для упрощения диагностики проблем

---

## Версия 1.0.8 (06.10.2025)

### Критическое исправление
- **Исправлена ошибка с рабочей директорией**
  - Добавлен `Set-Location $PSScriptRoot` в начало скрипта установки
  - Теперь виртуальное окружение создаётся в правильной директории приложения
  - Исправлена ошибка "Permission denied" при попытке создать venv в системной папке Windows
  
### Проблема, которая была исправлена
PowerShell скрипт запускался из системной папки и пытался создать `.venv` в `C:\Windows\system32\WindowsPowerShell\v1.0\`, что приводило к ошибке доступа. Теперь скрипт явно переключается на директорию приложения перед началом работы.

### Улучшения логирования
- Добавлен вывод текущей рабочей директории в лог для диагностики

---

## Версия 1.0.7 (06.10.2025)

### Критические исправления
- **Исправлена проблема с отсутствием pip** в виртуальном окружении на Python 3.13
  - Добавлено автоматическое обнаружение и удаление повреждённых виртуальных окружений
  - Создание venv с флагом `--without-pip` для более надёжной работы
  - Автоматическая установка pip через `python -m ensurepip --upgrade`
  - Резервный метод установки pip через официальный скрипт `get-pip.py`
  
### Улучшения
- Добавлена детальная диагностика процесса установки
- Улучшенная проверка корректности создания виртуального окружения
- Более информативные сообщения об ошибках в логах

### Технические детали
Изменения в `post_install.ps1`:
1. Проверка существования `.venv` и наличия `python.exe`
2. Автоматическое удаление неполных/повреждённых venv
3. Создание venv с `--without-pip` для стабильности
4. Двухэтапная установка pip с fallback механизмом
5. Проверка успешности каждого этапа установки

---

## Версия 1.0.6 (06.10.2025)

### Изменения
- Первая попытка исправления проблемы с pip
- Добавлена базовая проверка наличия pip.exe
- Добавлен вызов `ensurepip` при отсутствии pip

### Известные проблемы
- Не обрабатывалась ситуация с повреждённым venv
- Отсутствовал fallback метод установки pip
- ❌ Версия не работала корректно на чистых системах

---

## Версия 1.0.5 и ранее

Базовая функциональность инсталлятора без автоматической установки pip.


