# 🚀 Экспериментальные функции

Ветка `experimental/new-feature` содержит новые расширенные возможности для экспертного режима.

## 📋 Список новых функций

### 1. 🎯 Улучшенный Drag & Drop

**Статус:** ✅ Реализовано  
**Модуль:** `bom_categorizer/drag_drop_qt.py`  
**Доступ:** Экспертный режим → Экспертные инструменты

#### Возможности:
- 📁 Перетаскивание файлов из проводника
- 🔄 Изменение порядка файлов в списке
- ↔️ Перетаскивание между разными списками
- 🖱️ Контекстное меню (ПКМ):
  - Открыть файл
  - Показать в проводнике
  - Копировать путь
  - Удалить из списка
- 🎨 Визуальная подсветка зоны сброса

#### Как использовать:
1. Включите в Экспертных инструментах: `🎯 Улучшенный Drag & Drop`
2. Перетаскивайте файлы для изменения порядка
3. ПКМ на файле для доп. действий

📄 **Полная документация:** [DRAG_DROP_README.md](DRAG_DROP_README.md)

---

### 2. 💻 Интерактивная командная строка (CLI)

**Статус:** ✅ Реализовано  
**Модуль:** `bom_categorizer/cli_interactive.py`  
**Доступ:** Экспертный режим → Экспертные инструменты → Открыть CLI

#### Возможности:
- 📝 Командная строка с автодополнением (Tab)
- 🔄 История команд (↑↓)
- 🎨 Подсветка синтаксиса
- 💡 Встроенная справка (help)

#### Доступные команды:

**Общие:**
- `help` / `?` - справка по командам
- `clear` / `cls` - очистить экран
- `exit` / `quit` - закрыть CLI
- `history` - история команд

**Файлы:**
- `list` / `ls` - список файлов
- `add <путь>` - добавить файл
- `remove <индекс>` / `rm` - удалить файл
- `process` / `run` - запустить обработку

**База данных:**
- `dbstats` - статистика БД
- `dbsearch <название>` / `search` - поиск компонента
- `dbexport` - экспорт БД в Excel
- `dbbackup` - резервная копия БД

**Система:**
- `status` - статус приложения
- `config` - показать конфигурацию
- `theme [dark|light]` - переключить тему
- `scale <0.7-1.25>` - изменить масштаб

#### Пример использования:
```bash
>>> add C:\BOM\file1.xlsx
✅ Файл добавлен: file1.xlsx

>>> list
📁 Входные файлы:
  1. file1.xlsx (x1)
     C:\BOM\file1.xlsx

>>> process
🚀 Запущена обработка 1 файлов...

>>> dbstats
📊 Статистика базы данных:
Всего компонентов: 1234
Версия БД: 5.12
```

---

### 3. 📄 Экспорт в PDF

**Статус:** ✅ Реализовано  
**Модуль:** `bom_categorizer/pdf_exporter.py`  
**Доступ:** Экспертный режим → Экспертные инструменты

#### Возможности:
- 📊 Конвертация Excel результатов в PDF
- 📋 Титульная страница со сводной информацией
- 🎨 Сохранение форматирования таблиц
- 📑 Многостраничные документы
- 🖨️ Готово для печати

#### Как использовать:

**Вариант 1: Ручной экспорт**
1. Выполните обработку файлов
2. В Экспертных инструментах нажмите:
   `Экспортировать последний результат в PDF`
3. Выберите место сохранения PDF
4. Готово!

**Вариант 2: Автоматический экспорт**
1. Включите: `Автоматически создавать PDF после обработки`
2. Теперь после каждой обработки будет создаваться PDF

#### Требования:
```bash
pip install reportlab
```

#### Структура PDF:
1. **Титульная страница**
   - Название отчета
   - Сводная информация
   - Дата создания
   
2. **Содержимое по листам**
   - Каждый лист Excel = отдельная секция
   - Форматированные таблицы
   - Счетчики записей

---

### 4. 🔍 Глобальный поиск (обновлено)

**Статус:** ✅ Улучшено  
**Доступ:** Меню "🔍 Поиск" (после "Помощь")

#### Изменения:
- Меню теперь всегда видимо
- Активно только в **Экспертном режиме**
- В других режимах - затенено с подсказкой
- Поиск по базе данных и файлам

---

### 5. 📚 Расширенная справка

**Статус:** ✅ Добавлено  
**Доступ:** Меню "Помощь" → "🎯 Как использовать Drag & Drop"

#### Новые разделы:
- Drag & Drop руководство
- Интерактивные примеры
- Горячие клавиши
- Советы по использованию

---

### 6. 🔄 Автоматическая конвертация .doc → .docx

**Статус:** ✅ Реализовано  
**Модуль:** `bom_categorizer/gui_qt.py`  
**Доступ:** Автоматически при добавлении .doc файлов

#### Возможности:
- 🪟 **Windows:** Конвертация через Microsoft Word (pywin32)
- 🍎 **macOS/Linux:** Конвертация через LibreOffice (бесплатно!)
- ✅ Автоматическое определение платформы
- 📊 Прогресс-диалог с отображением статуса
- 🔍 Автопроверка наличия необходимого ПО
- 🌐 Ссылка на скачивание при отсутствии

#### Как работает:

**На Windows:**
1. Используется Microsoft Word через COM-интерфейс
2. Требует установленный MS Office
3. Конвертация с сохранением форматирования

**На macOS/Linux:**
1. Используется LibreOffice в headless режиме
2. Автопоиск в стандартных путях:
   - macOS: `/Applications/LibreOffice.app/Contents/MacOS/soffice`
   - Linux: `/usr/bin/libreoffice` или `/usr/bin/soffice`
3. Если не найден - предложение скачать с официального сайта

#### Как использовать:

1. Добавьте .doc файл в приложение
2. При попытке обработки появится диалог:
   - **Windows:** "Конвертировать с помощью Word"
   - **macOS/Linux:** "Конвертировать с помощью LibreOffice"
   - "Конвертировать вручную и продолжить"
   - "Отмена"
3. Выберите первый вариант для автоматической конвертации
4. Дождитесь завершения (показывается прогресс)
5. Файлы .docx автоматически добавятся в список

#### Требования:

**Windows:**
```bash
pip install pywin32
# + Microsoft Office (Word)
```

**macOS:**
```bash
# Скачать с официального сайта:
# https://www.libreoffice.org/download/download/

# Или через Homebrew:
brew install --cask libreoffice
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt install libreoffice
```

**Linux (Fedora):**
```bash
sudo dnf install libreoffice
```

**Linux (Arch):**
```bash
sudo pacman -S libreoffice-still
```

#### Технические детали:

**Метод для Windows:**
```python
def _convert_doc_files_with_word(self, doc_files: list) -> bool:
    # Использует win32com.client для COM-интерфейса с Word
    # Открывает .doc и сохраняет как .docx
```

**Метод для macOS/Linux:**
```python
def _convert_doc_with_libreoffice(self, doc_files: list) -> bool:
    # Использует subprocess для запуска:
    # soffice --headless --convert-to docx --outdir <dir> <file>
```

#### Особенности:

✅ **Преимущества:**
- Полностью автоматическая конвертация
- Кроссплатформенность (Windows/macOS/Linux)
- Проверка наличия необходимого ПО
- Сохранение форматирования таблиц
- Прогресс-диалог с детальной информацией

⚠️ **Ограничения:**
- Windows: требует установленный MS Office
- macOS/Linux: требует установленный LibreOffice
- Таймаут 60 секунд на файл (для очень больших файлов)
- Может не сохранить сложное форматирование

#### Примеры использования:

**Сценарий 1: LibreOffice установлен**
```
1. Добавляете old_bom.doc
2. Нажимаете "Обработать"
3. Видите диалог: "Обнаружены .doc файлы"
4. Нажимаете "Конвертировать с помощью LibreOffice"
5. Видите прогресс: "Конвертация 1 из 1..."
6. ✅ Успешно: old_bom.docx
7. Файл автоматически добавлен в список
```

**Сценарий 2: LibreOffice не установлен**
```
1. Добавляете old_bom.doc
2. Нажимаете "Обработать"
3. Видите диалог: "Обнаружены .doc файлы"
4. Нажимаете "Конвертировать с помощью LibreOffice"
5. Видите сообщение: "LibreOffice не найден"
6. Вопрос: "Хотите скачать LibreOffice?"
7. При "Да" открывается браузер со страницей загрузки
8. Можно выбрать "Конвертировать вручную"
```

#### Версия LibreOffice:

Протестировано с:
- LibreOffice 25.8.3.2 (macOS)
- LibreOffice 7.x (Linux)
- LibreOffice 6.x (Linux)

---

## 🔧 Технические детали

### Новые файлы:
```
bom_categorizer/
├── drag_drop_qt.py         # Улучшенный D&D
├── cli_interactive.py      # Интерактивный CLI
└── pdf_exporter.py         # Экспорт в PDF

docs/
├── DRAG_DROP_README.md     # Документация D&D
├── EXPERIMENTAL_FEATURES.md # Этот файл
└── MACOS_RETINA_FIX.md     # Исправление шрифтов на Retina
```

### Обновленные файлы:
```
bom_categorizer/
├── gui_qt.py              # + методы конвертации .doc, High DPI для macOS
├── dialogs_qt.py          # + адаптивный диалог конвертации
└── gui_sections_qt.py     # Расширена секция экспертных инструментов
```

### Зависимости:
```bash
# Для экспорта в PDF
pip install reportlab

# Для конвертации .doc на Windows
pip install pywin32

# Для конвертации .doc на macOS/Linux (системные пакеты):
# macOS: brew install --cask libreoffice
# Linux: sudo apt install libreoffice

# Уже установлены
openpyxl
PySide6
```

---

## 📊 Статистика

| Функция | Строк кода | Файлов | Статус |
|---------|-----------|--------|--------|
| Drag & Drop | ~430 | 1 | ✅ |
| CLI | ~550 | 1 | ✅ |
| PDF Export | ~380 | 1 | ✅ |
| .doc конвертация | ~280 | 2 | ✅ |
| **Итого** | **~1640** | **4** | ✅ |

---

## 🎯 Roadmap

### В разработке:
- [ ] История отмены/повтора действий (Ctrl+Z/Ctrl+Y)
- [ ] Drag & Drop для файлов сравнения
- [ ] CLI команды для сравнения файлов
- [ ] Экспорт в другие форматы (CSV, JSON)

### Планируется:
- [ ] Визуальные превью при D&D
- [ ] Группировка файлов
- [ ] Плагины и расширения
- [ ] Веб-интерфейс для просмотра результатов
- [ ] Batch processing в фоне

---

## ⚠️ Важные замечания

### Совместимость:
- ✅ Windows 10/11
- ✅ Linux
- ✅ macOS

### Производительность:
- Drag & Drop: до 100 файлов одновременно
- CLI: мгновенное выполнение команд
- PDF Export: ~5-10 сек для файла 1000 строк
- .doc конвертация: ~2-5 сек на файл (зависит от размера)

### Ограничения:
1. **Drag & Drop**
   - Требует перезапуск для отключения
   - Не поддерживается D&D между приложениями

2. **CLI**
   - Максимум 1000 команд в истории
   - Некоторые команды требуют GUI элементов

3. **PDF Export**
   - Требует библиотеку reportlab
   - Максимум 1000 строк на лист (ограничение производительности)
   - Шрифты с кириллицей (Windows only)

4. **.doc конвертация**
   - Windows: требует MS Office (Word)
   - macOS/Linux: требует LibreOffice (бесплатно)
   - Таймаут 60 секунд на файл
   - Сложное форматирование может потеряться

---

## 📝 Как протестировать

### 1. Установка зависимостей:
```bash
pip install reportlab
```

### 2. Переключение на ветку:
```bash
git checkout experimental/new-feature
```

### 3. Запуск:
```bash
python app_qt.py
```

### 4. Тестирование Drag & Drop:
1. Переключитесь в Экспертный режим
2. Включите "🎯 Улучшенный Drag & Drop"
3. Перетащите несколько файлов
4. Попробуйте ПКМ на файле

### 5. Тестирование CLI:
1. В Экспертных инструментах нажмите "Открыть CLI"
2. Введите `help` для справки
3. Попробуйте команды `list`, `status`, `dbstats`
4. Используйте ↑↓ для истории

### 6. Тестирование PDF Export:
1. Обработайте несколько файлов
2. Нажмите "Экспортировать последний результат в PDF"
3. Откройте созданный PDF

### 7. Тестирование .doc конвертации:
1. Подготовьте тестовый .doc файл
2. Добавьте его в приложение
3. Нажмите "Обработать"
4. Выберите автоматическую конвертацию
5. Проверьте результат (.docx должен появиться в списке)

---

## 🐛 Известные проблемы

### Drag & Drop:
- [ ] При большом количестве файлов (>100) может быть медленным

### CLI:
- [ ] Автодополнение не показывает popup (работает по Tab)
- [ ] История ограничена последними 20 командами в отображении

### PDF Export:
- [ ] На Linux может потребовать дополнительные шрифты
- [ ] Очень большие таблицы могут выходить за границы страницы

### .doc конвертация:
- [ ] На macOS/Linux требуется установка LibreOffice отдельно
- [ ] Очень большие файлы (>50MB) могут превысить таймаут
- [ ] Редкие форматы .doc могут не конвертироваться корректно

---

## 💬 Обратная связь

Для сообщения об ошибках или предложений:
- GitHub Issues: [BOMCategorizer/issues](https://github.com/kureinmaxim/BOMCategorizer/issues)
- Или создайте Pull Request

---

## 📜 Лицензия

Все экспериментальные функции распространяются под той же лицензией, что и основной проект.

---

**Последнее обновление:** 15.11.2025  
**Версия:** 4.3.7 + experimental features  
**Автор:** Claude & Team

## 📝 Changelog

### 15.11.2025
- ✅ Добавлена автоматическая конвертация .doc → .docx через LibreOffice (macOS/Linux)
- ✅ Исправлены мелкие шрифты на macOS Retina дисплеях
- ✅ Улучшена кроссплатформенная поддержка конвертации документов

### 12.11.2025
- ✅ Первый релиз экспериментальных функций
- ✅ Drag & Drop, CLI, PDF Export

