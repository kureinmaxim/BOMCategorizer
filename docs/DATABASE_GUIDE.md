# 🗄️ Полное руководство по базе данных компонентов

> **Обновлено:** 16 ноября 2025  
> **Версия:** 3.3.0 / 4.4.1  
> **Разработчик:** Куреин М.Н.

---

## 📋 Содержание

1. [Информация о БД в GUI](#-информация-о-базе-данных-в-gui)
2. [Кнопки управления БД](#-кнопки-управления-базой-данных)
3. [Синхронизация между проектом и установкой](#-синхронизация-между-проектом-и-установкой)
4. [Быстрые сценарии](#-быстрые-сценарии)
5. [Расположение файлов](#-расположение-файлов)
6. [Безопасность и резервное копирование](#-безопасность-и-резервное-копирование)
7. [FAQ](#-часто-задаваемые-вопросы)
8. [Технические детали](#-технические-детали)

---

## 📊 Информация о базе данных в GUI

### Визуальное отображение

В футер GUI приложения добавлена **вторая строка** с информацией о базе данных компонентов.

#### Футер состоит из двух строк:

```
┌─────────────────────────────────────────────────────────────┐
│           BOM Categorizer v3.3.0 / v4.4.1                    │
│                                                              │
│                    [Основной интерфейс]                     │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│ Разработчик: Куреин М.Н. | Дата выпуска: 16.11.2025        │
│ 🗄️ БД: v1.0 (42 компонента) | 📁 Установка (%APPDATA%)    │
└─────────────────────────────────────────────────────────────┘
```

### Отображаемая информация

#### Строка 1 (информация о разработчике):
- **Разработчик:** Куреин М.Н. (кликабельно для PIN-кода)
- **Дата выпуска:** 16.11.2025

#### Строка 2 (информация о базе данных):

##### 🗄️ База данных:
- **Версия БД:** из `metadata.version` (например, v1.0)
- **Количество компонентов:** из `metadata.total_components` (например, 42 компонента)
- Формат: `🗄️ БД: v1.0 (42 компонента)`
- Шрифт: Arial 9, жирный, цвет #424242 (темно-серый)

##### 📁 Расположение:
Показывает, откуда загружена база данных:

**Вариант 1: Установленная версия**
- Текст: `📁 Установка (%APPDATA%)`
- Цвет: **#1565C0 (синий)**
- Определяется по наличию "AppData" или "Roaming" в пути
- Путь: `C:\Users\USERNAME\AppData\Roaming\BOMCategorizer\Data\component_database.json`
- **Кликабельно:** открывает папку установки Modern Edition (`BOMCategorizerModern`) или папку базы данных (Standard)

**Вариант 2: Режим разработки**
- Текст: `📁 Проект (разработка)`
- Цвет: **#F57C00 (оранжевый)**
- Путь: `C:\Project\BOMCategorizer\component_database.json`
- **Кликабельно:** открывает папку с базой данных

### Примеры отображения

#### Сценарий 1: После установки (новый пользователь)
```
🗄️ БД: v1.0 (0 компонентов) | 📁 Установка (%APPDATA%)
                                    └── Синий цвет, кликабельно
```

#### Сценарий 2: После нескольких классификаций
```
🗄️ БД: v1.0 (15 компонентов) | 📁 Установка (%APPDATA%)
```

#### Сценарий 3: Опытный пользователь
```
🗄️ БД: v1.0 (127 компонентов) | 📁 Установка (%APPDATA%)
```

#### Сценарий 4: Режим разработки
```
🗄️ БД: v1.0 (42 компонента) | 📁 Проект (разработка)
                                   └── Оранжевый цвет, кликабельно
```

#### Сценарий 5: Ошибка загрузки
```
🗄️ БД: информация недоступна
└── Серый цвет (#757575)
```

### Цветовая схема

| Элемент | Цвет | Hex | Назначение |
|---------|------|-----|------------|
| Разработчик | Зеленый | #2E7D32 | Подчеркивает кликабельность |
| Версия БД | Темно-серый | #424242 | Нейтральный, читаемый |
| Установка | Синий | #1565C0 | Стабильность, официальная версия |
| Разработка | Оранжевый | #F57C00 | Внимание, тестовый режим |
| Ошибка | Серый | #757575 | Неактивно, проблема |

### Обновление информации

#### При запуске приложения:
- Информация загружается **один раз** при инициализации GUI
- Вызов `get_database_path()` и `get_database_stats()`
- Никаких задержек при запуске (функции быстрые)

#### Динамическое обновление:
- Количество компонентов **НЕ обновляется автоматически** во время работы
- Требуется **перезапуск приложения** для обновления счетчика
- Это допустимо, так как классификация происходит редко

---

## 🔘 Кнопки управления базой данных

В главном окне приложения находится секция **"🗄️ Управление базой данных"** с 7 кнопками:

### Расположение кнопок

```
┌─────────────────────────────────────────────────────────────┐
│              🗄️ Управление базой данных                     │
├─────────────────────────────────────────────────────────────┤
│  Ряд 1: [📊 Статистика] [📤 Экспорт в Excel] [💾 Резервная копия]  │
│  Ряд 2: [📥 Импорт из Excel] [📁 Открыть папку] [🔄 Заменить БД]   │
│  Ряд 3: [⬇️ Добавить все из выходного файла]                        │
└─────────────────────────────────────────────────────────────┘
```

### Описание кнопок

#### 📊 Статистика
Показывает подробную информацию о базе данных:
- Путь к файлу базы данных
- Версию, дату создания и обновления
- Общее количество компонентов
- Распределение по категориям (резисторы, конденсаторы и т.д.)

**Использование:**
1. Нажмите кнопку "📊 Статистика"
2. Посмотрите количество компонентов и распределение
3. Закройте окно

#### 📤 Экспорт в Excel
Сохраняет базу данных в файл Excel для:
- Редактирования компонентов в Excel
- Переноса на другой ПК
- Создания резервной копии в удобном формате

**Использование:**
1. Нажмите "📤 Экспорт в Excel"
2. Выберите место сохранения
3. Откройте файл в Excel для редактирования

#### 💾 Резервная копия
Создает резервную копию базы данных в формате JSON с временной меткой:
```
C:\Users\USERNAME\AppData\Roaming\BOMCategorizer\Data\database_backups\
  component_database_backup_20251116_143022.json
```

**Использование:**
1. Нажмите "💾 Резервная копия"
2. Копия создана автоматически с временной меткой
3. (Опционально) "📁 Открыть папку" → скопировать в облако

#### 📥 Импорт из Excel
Импортирует компоненты из Excel файла:
- **Объединить** - добавить новые компоненты к существующим
- **Заменить** - полностью заменить базу данных
- ✅ Автоматическое резервное копирование перед импортом

**Использование:**
1. Нажмите "📥 Импорт из Excel"
2. Выберите Excel файл
3. Выберите режим: "Объединить" или "Заменить"
4. Подтвердите импорт

#### 📁 Открыть папку
Открывает папку с базой данных в проводнике Windows.

**Использование:**
1. Нажмите "📁 Открыть папку"
2. Проводник откроется с выделенным файлом `component_database.json`

#### 🔄 Заменить базу данных
**Основная функция для синхронизации!**

Заменяет текущую базу данных файлом из другого источника:
- ✅ Синхронизация между проектом и установкой
- ✅ Импорт базы из другого проекта
- ✅ Восстановление из резервной копии
- ✅ Автоматическое резервное копирование перед заменой
- ✅ Проверка формата и количества компонентов

**Диалог подтверждения замены:**

```
╔═══════════════════════════════════════════════════════════╗
║              🔄 ЗАМЕНА БАЗЫ ДАННЫХ                        ║
╠═══════════════════════════════════════════════════════════╣
║ Текущая база данных:                                      ║
║   📊 Компонентов: 50                                      ║
║   📁 Расположение: ...\AppData\...\component_database.json║
║                                                            ║
║ Новая база данных:                                        ║
║   📊 Компонентов: 83                                      ║
║   📁 Файл: component_database.json                        ║
║                                                            ║
║ ⚠️ Текущая база будет заменена!                           ║
║ Резервная копия будет создана автоматически.              ║
║                                                            ║
║         [ Да ]              [ Нет ]                        ║
╚═══════════════════════════════════════════════════════════╝
```

#### ⬇️ Добавить все из выходного файла
Автоматически добавляет все компоненты из обработанного выходного файла в базу данных:
1. Обработайте входные файлы
2. Проверьте выходной .xlsx файл
3. Нажмите кнопку - все компоненты добавятся в базу!

**Использование:**
1. Обработайте файлы через "Запустить обработку"
2. Проверьте результат в выходном Excel файле
3. Нажмите "⬇️ Добавить все из выходного файла"
4. Все компоненты из всех листов категорий добавятся в базу!

---

## 🔄 Синхронизация между проектом и установкой

### Проблема

При работе с приложением база данных хранится в разных местах:
- **Проект (разработка)**: `C:\Project\BOMCategorizer\component_database.json`
- **Установленная версия**: `C:\Users\USERNAME\AppData\Roaming\BOMCategorizer\Data\component_database.json`

**Результат:** Компоненты, добавленные в проекте, не доступны в установке и наоборот!

### Решение

Используйте кнопку **"🔄 Заменить базу данных"** для синхронизации баз между проектом и установкой.

### Пошаговая инструкция

#### Вариант 1: Проект → Установка

```
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 1: Работа в проекте                                    │
└─────────────────────────────────────────────────────────────┘
1. Запустить приложение из проекта (start_gui.bat или app_qt.py)
2. Обработать файлы и проверить результат
3. Нажать "⬇️ Добавить все из выходного файла"
   ✅ База в ПРОЕКТЕ обновлена (например, 83 компонента)

┌─────────────────────────────────────────────────────────────┐
│  ШАГ 2: Синхронизация с установкой                          │
└─────────────────────────────────────────────────────────────┘
4. Запустить УСТАНОВЛЕННУЮ версию (Пуск → BOM Categorizer)
5. Нажать "🔄 Заменить базу данных"
6. Выбрать файл: C:\Project\BOMCategorizer\component_database.json
7. Подтвердить замену в диалоге
   ✅ База в УСТАНОВКЕ синхронизирована (83 компонента)
   💾 Резервная копия старой базы создана автоматически
```

#### Вариант 2: Установка → Проект

```
1. В установленной версии: "📁 Открыть папку"
2. Скопировать component_database.json
3. В проекте: "🔄 Заменить базу данных"
4. Выбрать скопированный файл
   ✅ Синхронизировано!
```

---

## 🚀 Быстрые сценарии

### Сценарий 1: Просмотр информации о базе
```
1. Нажать "📊 Статистика"
2. Посмотреть количество компонентов и распределение
3. Закрыть окно
```

### Сценарий 2: Перенос базы на другой ПК
```
ПК-1:
1. Нажать "📤 Экспорт в Excel"
2. Сохранить файл на USB-диск

ПК-2:
1. Установить программу
2. Нажать "📥 Импорт из Excel"
3. Выбрать файл с USB-диска
4. Выбрать "Объединить" или "Заменить"
   ✅ База импортирована!
```

### Сценарий 3: Регулярное резервное копирование
```
1. Раз в неделю: нажать "💾 Резервная копия"
2. (Опционально) "📁 Открыть папку" → скопировать в облако
```

### Сценарий 4: Редактирование компонентов
```
1. "📤 Экспорт в Excel"
2. Открыть файл в Excel
3. Отредактировать названия/категории
4. Сохранить файл
5. "📥 Импорт из Excel" → "Заменить"
   ✅ Изменения применены!
```

### Сценарий 5: Восстановление из резервной копии
```
1. "📁 Открыть папку"
2. Перейти в папку "database_backups"
3. Найти нужную резервную копию по дате
4. "🔄 Заменить базу данных"
5. Выбрать файл резервной копии
   ✅ База восстановлена!
```

### Сценарий 6: Импорт базы из другого проекта
```
1. Получить component_database.json из другого проекта
2. "🔄 Заменить базу данных"
3. Выбрать полученный файл
4. Подтвердить замену
   ✅ 150 компонентов импортировано!
```

### Сценарий 7: Синхронизация после работы в проекте
```
1. Поработали в проекте → добавили компоненты
2. Запустить установленную версию
3. "🔄 Заменить базу данных"
4. Выбрать базу из проекта
   ✅ Синхронизировано!
```

---

## 📁 Расположение файлов

### Пути к базе данных

| Режим | Путь |
|-------|------|
| **Проект** | `C:\Project\BOMCategorizer\component_database.json` |
| **Установка (Standard)** | `%APPDATA%\BOMCategorizer\Data\component_database.json` |
| **Установка (Modern Edition)** | `%APPDATA%\BOMCategorizer\Data\component_database.json` |
| **Резервные копии** | `%APPDATA%\BOMCategorizer\Data\database_backups\` |

### Быстрый доступ к папкам

- **Win + R** → `%APPDATA%\BOMCategorizer\Data` → **Enter**
- Клик на **"📁 Установка"** в футере приложения (Modern Edition открывает папку установки)
- Кнопка **"📁 Открыть папку"** в секции управления БД

### Определение режима работы

Приложение автоматически определяет режим:
- **Есть файл `.installed`** → Установленная версия → База в `%APPDATA%`
- **Нет файла `.installed`** → Режим разработки → База в папке проекта

---

## 🛡️ Безопасность и резервное копирование

### Автоматические резервные копии

Резервные копии создаются автоматически:
- ✅ Перед импортом из Excel
- ✅ Перед заменой базы данных

Резервные копии **НЕ удаляются** автоматически - можете очистить вручную если нужно.

### Защита от потери данных

| Операция | Резервная копия | Подтверждение |
|----------|----------------|---------------|
| Экспорт в Excel | ❌ Не требуется | ❌ Не требуется |
| Резервная копия | ✅ Создает копию | ❌ Не требуется |
| Импорт из Excel | ✅ Автоматически | ✅ Выбор режима |
| Заменить БД | ✅ Автоматически | ✅ Диалог подтверждения |
| Добавить из файла | ❌ Не создается | ❌ Не требуется |

### Рекомендации по безопасности

✅ **Регулярное резервное копирование**
- Создавайте резервные копии перед важными операциями
- Раз в неделю делайте ручной бэкап

✅ **Проверка перед заменой**
- Всегда смотрите на количество компонентов в диалоге
- Убедитесь что заменяете правильный файл

✅ **Использование Excel для редактирования**
- Не редактируйте JSON вручную
- Экспорт → Excel → Импорт безопаснее

---

## ❓ Часто задаваемые вопросы

### Q: Что произойдет с текущей базой при замене?
**A:** Создается автоматическая резервная копия, затем текущая база полностью заменяется новой.

### Q: Можно ли отменить замену базы?
**A:** Да! Используйте резервную копию из папки `database_backups`.

### Q: Как объединить две базы данных?
**A:** 
1. Экспортируйте обе базы в Excel
2. Объедините их вручную в Excel
3. Импортируйте объединенную базу в режиме "Заменить"

### Q: Нужно ли перезапускать приложение после операций с БД?
**A:** Рекомендуется для обновления информации в футере (количество компонентов).

### Q: База данных удалится при деинсталляции?
**A:** **НЕТ!** База хранится в отдельной папке `Data` и НЕ удаляется при деинсталляции.

### Q: Как понять, какая база используется - проекта или установки?
**A:** Посмотрите в футер приложения:
- **"📁 Установка (%APPDATA%)"** - используется база установки
- **"📁 Проект (разработка)"** - используется база проекта

### Q: Что делать если база повредилась?
**A:** Восстановите из резервной копии или создайте новую базу заново.

### Q: Можно ли редактировать базу данных JSON вручную?
**A:** Можно, но **не рекомендуется**. Используйте экспорт в Excel → редактирование → импорт.

### Q: Как посмотреть полный путь к базе данных?
**A:** Нажмите **"📊 Статистика"** - в первой строке будет показан полный путь.

### Q: Почему количество компонентов в футере не обновляется автоматически?
**A:** Для оптимизации производительности информация загружается только при запуске. Перезапустите приложение для обновления счетчика.

### Q: Что означает цвет расположения в футере?
**A:** 
- **Синий** (#1565C0) = установленная версия (стабильно)
- **Оранжевый** (#F57C00) = режим разработки (тестирование)

---

## 🔧 Технические детали

### Форматы файлов

Поддерживаются оба формата JSON:

**Новый формат (с метаданными):**
```json
{
  "metadata": {
    "version": "1.0.0",
    "created": "2025-11-01",
    "last_updated": "2025-11-16",
    "total_components": 83
  },
  "categories": {
    "resistors": "Резисторы",
    "capacitors": "Конденсаторы",
    ...
  },
  "components": {
    "1594ТЛ2Т": "ics",
    "HMC435AMS8GE": "ics"
  }
}
```

**Старый формат (прямой словарь):**
```json
{
  "1594ТЛ2Т": "ics",
  "HMC435AMS8GE": "ics"
}
```

### Используемые функции

#### Из `component_database.py`:

- **`get_database_path()`** - получить путь к БД
- **`get_database_stats()`** - получить статистику БД
- **`load_component_database()`** - загрузить БД (косвенно)
- **`add_component_to_database()`** - добавить компонент
- **`export_database_to_excel()`** - экспорт в Excel
- **`import_database_from_excel()`** - импорт из Excel
- **`backup_database()`** - резервная копия

### Используемые поля из статистики:

```python
{
    "metadata": {
        "version": "1.0",           # Версия БД
        "total_components": 42,     # Количество компонентов
        "created": "...",
        "last_updated": "..."
    }
}
```

### Изменения в коде

#### `bom_categorizer/gui.py` (Standard Edition)

**Добавлен импорт:**
```python
from .component_database import add_component_to_database, get_database_path, get_database_stats
```

**Обновлена функция `_create_footer()`:**
```python
def _create_footer(self):
    """Создает футер с информацией о разработчике и базе данных"""
    
    # ... существующий код для первой строки ...
    
    # Вторая строка: Информация о базе данных
    db_info_frame = ttk.Frame(footer)
    db_info_frame.pack(pady=(3, 0))
    
    try:
        db_path = get_database_path()
        db_stats = get_database_stats()
        
        # Определяем, откуда загружена БД
        if "AppData" in db_path or "Roaming" in db_path:
            location = "Установка (%APPDATA%)"
            location_color = "#1565C0"  # Синий
        else:
            location = "Проект (разработка)"
            location_color = "#F57C00"  # Оранжевый
        
        # Версия БД
        db_version = db_stats.get("metadata", {}).get("version", "N/A")
        total_components = db_stats.get("metadata", {}).get("total_components", 0)
        
        # Отображение информации...
        
    except Exception as e:
        # Fallback при ошибке
        ttk.Label(db_info_frame, 
                 text="🗄️ БД: информация недоступна", 
                 font=("Arial", 9),
                 foreground="#757575").pack(side=tk.LEFT)
```

#### `bom_categorizer/gui_qt.py` (Modern Edition)

**Аналогичная логика** с использованием PySide6 виджетов:
- `QLabel` для отображения информации
- Кликабельные метки для открытия папок
- Современная цветовая схема

### Статистика изменений

- **Файлов изменено:** 2 (`bom_categorizer/gui.py`, `bom_categorizer/gui_qt.py`)
- **Строк добавлено:** ~45 (Standard) + ~50 (Modern)
- **Новые импорты:** 2 (`get_database_path`, `get_database_stats`)
- **Новые фреймы:** 1 (`db_info_frame`)
- **Новые метки:** 6 (иконки, текст, разделители)

---

## 💡 Советы по использованию

### Для пользователя:

1. **Проверьте после установки:**
   - Должно быть: `📁 Установка (%APPDATA%)`
   - Если `Проект (разработка)` — что-то не так с установкой

2. **Отслеживайте рост базы:**
   - После классификации компонентов перезапустите GUI
   - Проверьте, что количество увеличилось

3. **При проблемах:**
   - Сделайте скриншот футера
   - Отправьте разработчику для диагностики

4. **Регулярная синхронизация:**
   - Поработали в проекте → синхронизируйте с установкой
   - Это гарантирует актуальность базы везде

### Для разработчика:

1. **Тестирование:**
   - Проверьте оба режима (проект и установка)
   - Убедитесь в правильных цветах
   - Протестируйте fallback при ошибке

2. **Поддержка:**
   - Попросите пользователя прислать скриншот футера
   - Вся необходимая информация в одном месте

---

## 📞 Поддержка

Если возникли проблемы:

1. ✅ Проверьте наличие резервных копий
2. ✅ Посмотрите лог в текстовом поле приложения
3. ✅ Нажмите "📊 Статистика" для диагностики
4. ✅ Проверьте формат JSON файла (если редактировали вручную)
5. ✅ Обратитесь к разработчику

---

## 🎊 Заключение

База данных компонентов — это **ключевая функция** BOM Categorizer, которая:

**Ключевые преимущества:**
- ✅ Автоматическое сохранение классификаций
- ✅ Переиспользование знаний между сессиями
- ✅ Прозрачность для пользователя
- ✅ Упрощение диагностики
- ✅ Визуальная обратная связь
- ✅ Контроль версии и расположения БД
- ✅ Простая синхронизация между проектом и установкой

**Реализация:**
- ✅ Минимальные изменения в коде
- ✅ Нет задержек при запуске
- ✅ Элегантное визуальное решение
- ✅ Полный набор инструментов управления

---

*Создано: 16.11.2025*  
*Версия документа: 2.0 (объединенная)*  
*Версия приложения: Standard 3.3.0 / Modern Edition 4.4.1*  
*Разработчик: Куреин М.Н.*

