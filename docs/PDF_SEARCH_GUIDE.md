# 🔍 Полное руководство: Поиск PDF документации

Экспериментальная функция поиска PDF документации для электронных компонентов в BOM Categorizer Modern Edition.

---

## 📋 Содержание

- [Возможности](#возможности)
- [Настройка путей для локального поиска](#настройка-путей-для-локального-поиска)
- [Настройка API ключей для AI поиска](#настройка-api-ключей-для-ai-поиска)
- [Использование](#использование)
- [Системные папки по умолчанию](#системные-папки-по-умолчанию)
- [Примеры использования](#примеры-использования)
- [Решение проблем](#решение-проблем)
- [Разработка](#разработка)

---

## Возможности

### 📁 Локальный поиск

- Поиск PDF файлов на компьютере
- Автоматический поиск в нескольких папках одновременно
- Поддержка пользовательских папок
- Умный поиск с совпадением 3+ символов подряд
- Рекурсивный поиск во всех вложенных подпапках
- Удаление дубликатов при объединении результатов
- Пример: для "449" найдет `HMC449RXZ.pdf`, `Р449_l.pdf`

### 🤖 AI Поиск

- Поиск информации о компонентах через AI
- Поддержка двух провайдеров:
  - **Anthropic Claude** (Claude 3.5 Sonnet)
  - **OpenAI GPT-4o**

#### Получаемая информация:
- Полное название и производитель
- Тип компонента
- Основные характеристики
- Описание назначения
- Примеры использования
- Ссылка на PDF документацию (с сайта производителя)

---

## Настройка путей для локального поиска

### 🎯 Приоритет поиска

Поиск выполняется в следующем порядке:

1. **✨ Пользовательские папки** (из `config_qt.json` или добавленные через интерфейс)
2. **📁 Папка базы данных** и её подпапки с префиксом `pdf*`
3. **🍎 macOS**: `/Users/olgazaharova/Project` + подпапки с префиксом `pdf*`
4. **🪟 Windows**: `C:\Project` + подпапки с префиксом `pdf*`

**Особенности:**
- **Рекурсивный поиск**: Поиск идет во всех вложенных подпапках
- **Все папки сразу**: Поиск выполняется одновременно во всех настроенных местах
- **Без дубликатов**: Если файл найден в нескольких папках, показывается один раз

### ✨ Способ 1: Через окно локального поиска (рекомендуется)

**Самый быстрый способ - прямо во время поиска:**

1. Откройте: **`📄 Поиск PDF`** → **`🔍 Локальный поиск`**
2. В разделе **"📂 Папки для поиска"** вы увидите все активные папки с иконками:
   - **👤** Пользовательские папки (сохраняются в config)
   - **💾** Папка базы данных (системная)
   - **📄** Папки pdf* (системные)
   - **📁** Папки Project (системные)

3. **Управление папками:**
   - **➕ Добавить папку** - добавить свою папку (будет помечена 👤)
   - **➖ Удалить** - удалить выбранную папку из списка
   - **💾 Сохранить в конфиг** - сохранить пользовательские папки в `config_qt.json`
   - **🔄 Сброс** - вернуть список по умолчанию

**ВАЖНО:** В конфиг сохраняются ТОЛЬКО пользовательские папки (👤). Системные папки (💾 📄 📁) используются автоматически и не попадают в `config_qt.json`.

**Преимущества:**
- ✅ Видно сразу все папки, где идет поиск
- ✅ Можно добавить временную папку для разового поиска
- ✅ Можно сохранить удобный набор папок в конфиг одной кнопкой

### 🔧 Способ 2: Через настройки AI

**Для постоянных изменений:**

1. Запустите **BOM Categorizer Modern Edition**
2. В меню выберите: **`📄 Поиск PDF`** → **`⚙️ Настройки AI`**
3. Перейдите на вкладку **`📂 Пути PDF`** (первая вкладка)
4. Нажмите кнопку **`➕ Добавить папку`**
5. Выберите нужную директорию в диалоге выбора папки
6. Повторите для добавления нескольких папок
7. Нажмите **`OK`** для сохранения

**Управление папками:**
- **➖ Удалить выбранную** - удаляет одну выбранную папку
- **🗑️ Очистить все** - удаляет все пользовательские папки

### 📝 Способ 3: Редактирование config_qt.json вручную

**Где находится файл?**

- **В режиме разработки**: `C:\Project\BOMCategorizer\config_qt.json` (рядом с проектом)
- **После установки (Windows)**: `C:\Users\USERNAME\AppData\Roaming\BOMCategorizerModern\config_qt.json`
- **После установки (macOS)**: `~/Library/Application Support/BOMCategorizerModern/config_qt.json`

**Как редактировать?**

Откройте `config_qt.json` в текстовом редакторе и найдите раздел `pdf_search`:

```json
{
  "pdf_search": {
    "custom_directories": [
      "/Users/username/Documents/Datasheets",
      "/Volumes/External/PDF_Archive",
      "C:\\Users\\username\\Desktop\\PDFs"
    ]
  }
}
```

**Правила:**
1. **Формат JSON**: Пути в кавычках, разделены запятыми
2. **Экранирование на Windows**: Используйте `\\` вместо `\`
   - ❌ `"C:\Folder\PDF"`
   - ✅ `"C:\\Folder\\PDF"`
3. **macOS/Linux**: Обычные пути
   - ✅ `"/Users/username/PDFs"`
   - ✅ `"/Volumes/External/Docs"`

**После редактирования:**
1. Сохраните файл
2. Перезапустите приложение

**Внутри пользовательских папок поиск идет рекурсивно по всем вложенным папкам!**

---

## Настройка API ключей для AI поиска

### Получение API ключей

#### Anthropic Claude
1. Перейдите на https://console.anthropic.com/
2. Зарегистрируйтесь или войдите
3. Создайте API ключ в разделе API Keys
4. Скопируйте ключ вида `sk-ant-api03-...`

#### OpenAI GPT
1. Перейдите на https://platform.openai.com/api-keys
2. Зарегистрируйтесь или войдите
3. Создайте новый API ключ
4. Скопируйте ключ вида `sk-...`

### Установка ключей
1. Откройте меню **📄 Поиск PDF** → **⚙️ Настройки AI**
2. Перейдите на вкладку **🔑 API ключи**
3. Вставьте ваши API ключи
4. Нажмите **💾 Сохранить**

---

## Использование

### Локальный поиск

1. Переключитесь в **Экспертный режим** (Вид → Режим работы → Экспертный режим)
2. Откройте меню **📄 Поиск PDF** → **🔍 Локальный поиск**
3. Введите название компонента (например: `HMC449`)
4. Нажмите **🔎 Найти**
5. Двойной клик по результату открывает PDF

**Логика поиска:**
- **Автоматический поиск** (без указания пути): Ищет во всех папках по умолчанию одновременно, объединяет результаты, удаляет дубликаты, сортирует по релевантности
- **Ручное указание пути**: Если пользователь указывает конкретную папку, поиск идет только там (рекурсивный обход всех подпапок)

### AI поиск

1. Переключитесь в **Экспертный режим**
2. Откройте меню **📄 Поиск PDF** → **🤖 AI поиск компонента**
3. Выберите провайдера (Anthropic или OpenAI)
4. Введите название компонента (например: `LM358`)
5. Нажмите **🔎 Найти**
6. Результаты отобразятся в красивом формате

---

## Системные папки по умолчанию

### 1️⃣ Папка базы данных

- **Основная папка**: где находится `component_database.json`
- **Подпапки с префиксом `pdf*`**: любой регистр (pdf, PDF, Pdf, pDf и т.д.)
- **Рекурсивный поиск**: внутри всех подпапок

**Примеры:**
```
BOMCategorizer/
├── component_database.json
├── PDF_Datasheets/         ← ищем здесь рекурсивно
│   ├── Capacitors/
│   └── Resistors/
└── pdf-docs/               ← и здесь рекурсивно
    └── old/
```

### 2️⃣ macOS: `/Users/olgazaharova/Project`

- **Подпапки с префиксом `pdf*`**: любой регистр
- **Рекурсивный поиск**: внутри всех вложенных папок

**Примеры:**
```
/Users/olgazaharova/Project/
├── PDF_Archive/            ← ищем здесь рекурсивно
│   ├── 2024/
│   └── 2025/
├── pdfDocs/                ← и здесь рекурсивно
│   └── components/
└── other_folder/           ← НЕ ищем (нет префикса pdf*)
```

### 3️⃣ Windows: `C:\Project`

- **Подпапки с префиксом `pdf*`**: любой регистр
- **Рекурсивный поиск**: внутри всех вложенных папок

**Примеры:**
```
C:\Project\
├── PDF_Library\            ← ищем здесь рекурсивно
│   ├── Chips\
│   └── Modules\
├── PdfFiles\               ← и здесь рекурсивно
│   └── datasheets\
└── documents\              ← НЕ ищем (нет префикса pdf*)
```

### 🔠 Регистр букв в названии папок

Префикс `pdf*` распознается **в любом регистре**:

✅ **Найдет:**
- `pdf`, `PDF`, `Pdf`, `pDf`, `pdF`, `PdF`, `pDF`, `PDf`
- `PDF_Datasheets`, `pdf-archive`, `PdfDocs`

❌ **Не найдет:**
- `documents` (нет префикса pdf*)
- `datasheets` (нет префикса pdf*)
- `archived_pdf` (префикс в конце, а не в начале)

---

## Примеры использования

### Примеры запросов

- `HMC449` - найдет RF mixer/multiplier
- `LM358` - найдет операционный усилитель
- `STM32F103` - найдет микроконтроллер
- `2N3904` - найдет транзистор

### Пример 1: Локальная сеть (NAS)

```json
{
  "pdf_search": {
    "custom_directories": [
      "/Volumes/CompanyNAS/Datasheets",
      "\\\\192.168.1.100\\shared\\PDFs"
    ]
  }
}
```

### Пример 2: Внешний диск

```json
{
  "pdf_search": {
    "custom_directories": [
      "/Volumes/MyExternalDrive/ComponentDocs",
      "E:\\DatasheetArchive"
    ]
  }
}
```

### Пример 3: Несколько локальных папок

```json
{
  "pdf_search": {
    "custom_directories": [
      "/Users/engineer/Desktop/CurrentProject/PDFs",
      "/Users/engineer/Documents/Archive/Datasheets",
      "/Users/engineer/Downloads/ComponentDocs"
    ]
  }
}
```

### 💡 Рекомендации для максимальной эффективности

1. **Создайте папки с префиксом `pdf`** в удобных местах:
   - macOS: `/Users/olgazaharova/Project/PDF_Datasheets`
   - Windows: `C:\Project\PDF_Library`

2. **Организуйте PDF файлы по категориям** внутри этих папок:
   ```
   PDF_Datasheets/
   ├── Microcontrollers/
   ├── Sensors/
   ├── Power/
   └── Communication/
   ```

3. **Называйте файлы по артикулу** для точного поиска:
   - ✅ `STM32F407VGT6.pdf`
   - ✅ `LM358_datasheet.pdf`
   - ❌ `Document1.pdf` (неинформативное название)

---

## Решение проблем

### Локальный поиск

#### Проблема: Папка не найдена
**Решение:** Проверьте правильность пути:
- На Windows используйте `\\` вместо `\`
- Убедитесь, что папка существует
- Проверьте права доступа

#### Проблема: Файлы не находятся
**Решение:**
- Убедитесь, что файлы имеют расширение `.pdf`
- Проверьте, что поле "Путь для поиска" в окне поиска пустое (для автоматического поиска)
- Убедитесь, что названия файлов содержат артикул компонента
- Проверьте, что папка содержит подпапки с префиксом "pdf" (для системных папок)
- Укажите путь вручную через кнопку "📁 Обзор..." для тестирования

#### Проблема: Изменения не применяются
**Решение:**
- После редактирования `config_qt.json` перезапустите приложение
- Проверьте синтаксис JSON (запятые, кавычки)
- Используйте JSON валидатор для проверки файла
- Убедитесь, что файл сохранен в правильном месте (см. раздел "Где находится файл?")

### AI поиск

#### Проблема: "API ключ не найден"
**Решение:**
- Проверьте настройки (📄 Поиск PDF → ⚙️ Настройки AI → вкладка 🔑 API ключи)
- Убедитесь что ключ скопирован полностью
- Проверьте, что ключ сохранен в правильном `config_qt.json`

#### Проблема: Ошибки AI поиска
**Решение:**
- Проверьте баланс на аккаунте провайдера
- Проверьте интернет-соединение
- Попробуйте другого провайдера
- Проверьте правильность API ключа

### ⚠️ Важные замечания

#### Производительность:
- Большое количество папок может замедлить поиск
- Рекомендуется не более 5-10 пользовательских папок
- Избегайте добавления корневых директорий с тысячами файлов

#### Доступность:
- Убедитесь, что папки существуют и доступны
- Для сетевых дисков проверьте подключение
- Для внешних дисков убедитесь, что они подключены

#### Безопасность:
- Файл `config_qt.json` хранится локально
- API ключи хранятся в `config_qt.json`
- Используйте `.gitignore` для исключения конфига из репозитория
- Не делитесь API ключами публично
- Пути в конфиге видны любому, кто имеет доступ к файлу
- Не используйте для хранения конфиденциальной информации

---

## Требования

### Python пакеты
```bash
pip install anthropic  # Для Anthropic Claude
pip install openai     # Для OpenAI GPT
```

### API ключи
- Anthropic Claude: требуется API ключ и баланс на аккаунте
- OpenAI GPT: требуется API ключ и баланс на аккаунте

### Ограничения
- AI поиск требует интернет-соединения
- Использование API платное (по тарифам провайдеров)
- Качество результатов зависит от AI модели

---

## Разработка

### Файлы модуля:
- `bom_categorizer/pdf_search.py` - логика поиска
- `bom_categorizer/pdf_search_dialogs.py` - диалоги UI

### Технические детали

#### Функция `get_default_pdf_directories()`:
- Добавлен поиск подпапок с префиксом `pdf*` в папке БД
- Добавлен поиск в `/Users/olgazaharova/Project` (macOS)
- Добавлен поиск в `C:\Project` (Windows)
- Проверка регистра через `.lower().startswith('pdf')`

#### Метод `LocalPDFSearcher.search()`:
- Улучшена логика рекурсивного обхода
- Если `base_directory` сама начинается с `pdf*`, ищет везде внутри
- Если `base_directory` НЕ начинается с `pdf*`, ищет только в подпапках с префиксом `pdf*`

#### Метод `run_local_search()`:
- Изменено поведение: теперь ищет **во всех** папках по умолчанию, а не только в первой
- Добавлено объединение результатов из всех источников
- Добавлено удаление дубликатов по пути

---

## 🔄 История изменений

**Версия 4.4.1** (16.11.2025):
- ✅ Исправлено определение пути к `config_qt.json` для установленных версий
- ✅ Windows: `%APPDATA%\BOMCategorizerModern\config_qt.json`
- ✅ macOS: `~/Library/Application Support/BOMCategorizerModern/config_qt.json`

**Версия 4.4.0** (16.11.2025):
- ✨ **НОВОЕ:** Визуальный список папок с иконками в окне локального поиска
- ✨ **НОВОЕ:** Разделение на пользовательские (👤) и системные (💾 📄 📁) папки
- ✨ **НОВОЕ:** Кнопка "💾 Сохранить в конфиг" прямо в окне поиска
- ✅ Только пользовательские папки сохраняются в `config_qt.json`
- ✅ Системные папки используются автоматически всегда
- ✅ Добавлен поиск в папках с префиксом `pdf*` (любой регистр)
- ✅ Добавлена поддержка `/Users/olgazaharova/Project` (macOS)
- ✅ Добавлена поддержка `C:\Project` (Windows)
- ✅ Реализован одновременный поиск во всех папках
- ✅ Добавлено удаление дубликатов при объединении результатов
- ✅ Улучшена логика рекурсивного обхода папок
- ✅ Новая вкладка "📂 Пути PDF" в настройках для управления путями
- ✅ Тултипы показывают тип папки (пользовательская/системная)

---

## 📚 Связанные документы

- [`docs/TXT_EXPORT_GUIDE.md`](TXT_EXPORT_GUIDE.md) - Экспорт результатов
- [`docs/GUI_DATABASE_INFO.md`](GUI_DATABASE_INFO.md) - Работа с базой данных
- [`README.md`](../README.md) - Общая документация приложения

---

**⚠️ Экспериментальная функция** - находится в разработке в ветке `experimental/new-feature`

**Версия документа:** 2.0  
**Дата:** 16.11.2025  
**Автор:** BOM Categorizer Team

