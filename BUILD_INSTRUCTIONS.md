# Инструкции по созданию инсталляторов

## 📦 Windows

### Требования:
- Inno Setup 6.x
- Python 3.8+
- PowerShell

### Сборка:
1. Запустите скрипт подготовки (создаст temp_installer с необходимыми файлами)
2. Откройте `installer_clean.iss` в Inno Setup
3. Нажмите "Compile" (или F9)
4. Готовый инсталлятор: `BOMCategorizerSetup.exe`

### Инструкция для пользователя Windows:
1. Скачайте `BOMCategorizerSetup.exe`
2. Запустите инсталлятор
3. Следуйте инструкциям мастера установки
4. После установки запустите через ярлык на рабочем столе

---

## 🍎 macOS

### Требования:
- macOS 10.13+
- Python 3.8+

### Сборка (Портативная версия):

```bash
# Дать права на выполнение (один раз)
chmod +x build_macos_simple.sh

# Запустить сборку
./build_macos_simple.sh
```

Скрипт создает:
1. ✅ Портативную папку со всеми файлами
2. ✅ Launcher скрипт с автоматической установкой зависимостей
3. ✅ DMG образ для распространения

### Результат:
- **DMG файл**: `BOMCategorizer-3.2.2-macOS-Portable.dmg` (~536KB)
- **Портативная папка**: `BOM_Categorizer_Portable/`

### Инструкция для пользователя macOS:
1. Скачайте `BOMCategorizer-X.X.X-macOS-Portable.dmg`
2. Откройте DMG (двойной клик)
3. Скопируйте папку "BOM_Categorizer_Portable" в любое место
4. Запустите `BOM Categorizer.command` (двойной клик)
5. При первом запуске:
   - Автоматически установятся зависимости (требуется интернет)
   - Если система блокирует: Системные настройки → Безопасность → "Открыть в любом случае"

### Альтернативный запуск (Terminal):
```bash
cd путь/к/BOM_Categorizer_Portable
python3 app.py
```

### Примечание:
Портативная версия не требует установки и может работать с USB флешки.

---

## 🐧 Linux

### Требования:
- Python 3.8+
- tkinter: `sudo apt-get install python3-tk` (Debian/Ubuntu)

### Установка из исходников:

```bash
# Клонировать репозиторий
git clone https://github.com/kureinmaxim/BOMCategorizer.git
cd BOMCategorizer

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
python3 app.py
```

### Создание .desktop файла (опционально):

```bash
cat > ~/.local/share/applications/bomcategorizer.desktop << EOF
[Desktop Entry]
Version=1.0
Type=Application
Name=BOM Categorizer
Comment=Bill of Materials Categorization Tool
Exec=/путь/к/BOMCategorizer/venv/bin/python3 /путь/к/BOMCategorizer/app.py
Icon=application-x-executable
Terminal=false
Categories=Utility;Development;
EOF
```

---

## 📝 Примечания

### Версионирование:
Версия указывается в `config.json`:
```json
{
  "app_info": {
    "version": "3.2.2",
    ...
  }
}
```

При изменении версии обновите:
- ✅ `config.json` - основная версия
- ✅ `installer_clean.iss` - для Windows (строка 1)
- ⚙️ DMG создается автоматически из config.json

### Тестирование:
После создания инсталлятора протестируйте на чистой системе:
1. Установка
2. Первый запуск
3. Базовая функциональность
4. Удаление

### Распространение:
- GitHub Releases (рекомендуется)
- Прямая загрузка с сервера
- Файлообменники (для больших файлов)

