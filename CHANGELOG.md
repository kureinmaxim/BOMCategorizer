# 📝 Changelog - BOM Categorizer

**Проект / Project:** BOM Categorizer  
**Разработчик / Developer:** Куреин М.Н. / Kurein M.N.

---

## 📑 Содержание

- [Version 3.1.4 - First Run Database Import (07.11.2025)](#version-314---first-run-database-import)
- [Version 3.1.3 - GitHub Ready Edition (07.11.2025)](#version-313---github-ready-edition)
- [Version 3.1.2 - Import from Output & Smart Features (05.11.2025)](#version-312---import-from-output--smart-features)
- [Version 3.0.0 - Database Management Edition (01.11.2025)](#version-300---database-management-edition)

---

# Version 3.1.4 - First Run Database Import

**Дата релиза / Release Date:** 07.11.2025  
**Разработчик / Developer:** Куреин М.Н. / Kurein M.N.

---

## 🎊 Главное нововведение

### 🗄️ Импорт базы данных при первом запуске

Версия 3.1.4 добавляет **проверку первого запуска** с предложением импортировать существующую базу данных компонентов.

---

## 🆕 Что нового

### 1. Диалог импорта БД при первом запуске 🗄️

**Проблема:** После установки приложения БД всегда была пустой, даже если у пользователя уже есть накопленная база

**Решение:** При первом запуске приложение показывает диалог с предложением импортировать существующую БД

**Диалоговое окно:**
```
╔════════════════════════════════════════════════╗
║  🗄️ База данных компонентов                   ║
╠════════════════════════════════════════════════╣
║                                                ║
║  У вас уже есть база данных компонентов?       ║
║                                                ║
║  Вы можете импортировать её для                ║
║  автоматической классификации компонентов.     ║
║                                                ║
║  Поддерживаемые форматы:                       ║
║  • JSON (component_database.json)              ║
║  • Excel (component_database.xlsx)             ║
║                                                ║
║  [ 📁 Импортировать ]  [ ✨ Начать с нуля ]   ║
║                                                ║
╚════════════════════════════════════════════════╝
```

**Возможности:**
- ✅ Импорт из JSON
- ✅ Импорт из Excel
- ✅ Статистика импортированных компонентов
- ✅ Опция "Начать с нуля"

---

### 2. Новые функции в component_database.py 🔧

#### `is_first_run() -> bool`
Проверяет, является ли это первым запуском:
- Если файла БД нет → первый запуск
- Если компонентов ≤ 10 → первый запуск
- Иначе → обычный запуск

#### `initialize_database_from_template()`
Инициализирует БД из шаблона:
- Копирует `component_database_template.json` → `component_database.json`
- Создает папку в `%APPDATA%` если её нет
- Создает пустую БД если шаблона нет

```python
from bom_categorizer.component_database import (
    is_first_run,
    initialize_database_from_template
)
```

---

### 3. Обновлен ANALYSIS_PROJECT.md 📚

Добавлен большой раздел **"🗄️ Архитектура базы данных"** с подробным описанием:

- ✅ Двухуровневая система хранения
- ✅ Жизненный цикл БД у пользователя
- ✅ Жизненный цикл БД у разработчика
- ✅ Преимущества архитектуры
- ✅ Импорт БД при первом запуске
- ✅ Управление БД (CLI/GUI)
- ✅ Техническая реализация
- ✅ API и документация

**Структура:**
```
1. Образцовая БД (разработка)
   └─ component_database.json (локально, НЕ в Git)

2. Шаблон БД (пользователи)
   └─ component_database_template.json (в Git) ✅

3. Пользовательская БД (после установки)
   └─ %APPDATA%\BOMCategorizer\Data\component_database.json
```

---

### 4. Логика работы для пользователя 👤

#### Первый запуск:
```
1. Установка BOMCategorizerSetup.exe
   └─ component_database_template.json копируется

2. Запуск приложения
   ├─ Проверка: есть ли БД в %APPDATA%?
   ├─ Если НЕТ → создается из шаблона
   └─ ДИАЛОГ: "Хотите импортировать существующую БД?"

3. Импорт (опционально)
   ├─ Выбор файла (JSON/Excel)
   ├─ Импорт компонентов
   └─ Статистика: "Импортировано: 69 компонентов"

4. Работа
   ├─ Автоматическая классификация
   ├─ Интерактивный режим
   └─ БД пополняется автоматически
```

#### Последующие запуски:
- ✅ Диалог импорта НЕ показывается
- ✅ Используется существующая БД
- ✅ БД продолжает пополняться

---

## 🛠️ Технические детали

### Изменения в коде:

**bom_categorizer/gui.py:**
```python
def __init__(self):
    ...
    # Проверяем первый запуск и предлагаем импорт БД
    self.after(500, self.check_first_run_and_offer_import)

def check_first_run_and_offer_import(self):
    """Проверяет первый запуск и предлагает импортировать БД"""
    initialize_database_from_template()
    
    if not is_first_run():
        return  # Не первый запуск
    
    # Показываем диалог импорта
    ...
```

**bom_categorizer/component_database.py:**
```python
def is_first_run() -> bool:
    """Проверяет первый запуск (БД пустая или ≤10 компонентов)"""
    db_path = get_database_path()
    if not os.path.exists(db_path):
        return True
    components = load_component_database()
    return len(components) <= 10

def initialize_database_from_template():
    """Инициализирует БД из шаблона"""
    db_path = get_database_path()
    if os.path.exists(db_path):
        return
    template_path = "component_database_template.json"
    shutil.copy2(template_path, db_path)
```

---

## ✅ Преимущества

### Для пользователей:
- ✅ Возможность миграции существующей БД
- ✅ Не теряется накопленная информация
- ✅ Автоматическая классификация с первого запуска
- ✅ Гибкий выбор: импорт или чистый старт

### Для разработчиков:
- ✅ Профессиональная архитектура onboarding
- ✅ Упрощенная миграция между версиями
- ✅ Четкое разделение данных
- ✅ Документированная архитектура БД

---

## 📦 Файлы изменены

### Основные файлы:
- ✅ `bom_categorizer/component_database.py` - новые функции
- ✅ `bom_categorizer/gui.py` - диалог импорта
- ✅ `ANALYSIS_PROJECT.md` - раздел о БД
- ✅ `config.json` - версия 3.1.4
- ✅ `bom_categorizer/__init__.py` - версия 3.1.4
- ✅ `installer_clean.iss` - версия 3.1.4

### Installer копии:
- ✅ `temp_installer/bom_categorizer/component_database.py`
- ✅ `temp_installer/bom_categorizer/gui.py`
- ✅ `temp_installer/config.json`
- ✅ `temp_installer/bom_categorizer/__init__.py`

---

## 🔍 Тестирование

### Сценарий 1: Чистая установка
```
1. Установить BOMCategorizerSetup.exe
2. Запустить приложение
3. Увидеть диалог импорта
4. Выбрать "Начать с нуля"
5. БД пустая, готова к накоплению
```

### Сценарий 2: Импорт существующей БД
```
1. Установить BOMCategorizerSetup.exe
2. Запустить приложение
3. Увидеть диалог импорта
4. Выбрать "Импортировать"
5. Выбрать файл component_database.json (69 компонентов)
6. Увидеть: "Импортировано: 69 компонентов"
7. Классификация работает сразу
```

### Сценарий 3: Повторный запуск
```
1. Запустить приложение второй раз
2. Диалог импорта НЕ показывается
3. Используется существующая БД
```

---

## 📊 Статистика версии

- **Новых функций:** 2 (is_first_run, initialize_database_from_template)
- **Новых диалогов:** 1 (импорт БД при первом запуске)
- **Строк кода добавлено:** ~150
- **Документации добавлено:** ~200 строк
- **Улучшений UX:** Упрощенная миграция данных

---

## 🎓 Выводы

**Version 3.1.4** улучшает первое впечатление от приложения:
- ✅ Гибкая миграция данных
- ✅ Профессиональный onboarding
- ✅ Документированная архитектура БД
- ✅ User-friendly интерфейс

**Следующий релиз:** v3.2.0 - Enhanced Database Management

---

# Version 3.1.3 - GitHub Ready Edition

**Дата релиза / Release Date:** 07.11.2025  
**Разработчик / Developer:** Куреин М.Н. / Kurein M.N.

---

## 🎊 Главное нововведение

### 🚀 Подготовка проекта к публикации на GitHub

Версия 3.1.3 — это **релиз безопасности и публикации**. Проект полностью подготовлен для публикации на GitHub с защитой конфиденциальных данных и оптимизацией для открытого доступа.

---

## 🆕 Что нового

### 1. Изменен пароль по умолчанию 🔒

**Проблема:** Старый пароль `5421` мог быть личным и не подходил для публичного репозитория

**Решение:** Изменен на стандартный демонстрационный пароль `1234`

**Изменено в файлах:**
- ✅ `config.json` - основной конфиг
- ✅ `temp_installer/config.json` - конфиг инсталлятора
- ✅ `README.md` (2 упоминания)
- ✅ `temp_installer/README.md` (2 упоминания)
- ✅ `bom_categorizer/gui.py` - значение по умолчанию
- ✅ `temp_installer/bom_categorizer/gui.py` - значение по умолчанию
- ✅ `docs/QUICK_START.md` - документация
- ✅ `temp_installer/docs/QUICK_START.md` - документация

**Пример изменения:**
```json
"security": {
  "pin": "1234",  // было: "5421"
  "require_pin": true
}
```

---

### 2. Создан комплексный .gitignore 📝

Добавлена защита от случайной публикации конфиденциальных данных:

**Исключено из репозитория:**

#### Python окружение
```
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
*.egg-info/
dist/
build/
```

#### Конфиденциальные данные проекта
```
# База данных компонентов (личные данные)
component_database.json
component_database.xlsx

# Резервные копии БД
database_backups/
*_backup_*.json

# Примеры файлов (могут содержать конфиденциальные проекты)
example/
```

#### Временные и сборочные файлы
```
# Исполняемые файлы (слишком большие для GitHub)
*.exe

# Временные файлы сборки
temp_installer/
temp_for_classification.xlsx
*.tmp
*.temp
```

#### Тестовые файлы
```
quick_test.py
preview_unclassified.py
test_examples.bat
```

**Файл:** `.gitignore` (новый)

---

### 3. Объединены changelog файлы 📋

**Было:**
- `CHANGELOG_3.0.0.md` - changelog для v3.0.0
- `CHANGELOG_3.1.2.md` - changelog для v3.1.2
- Два отдельных файла, сложно отслеживать историю

**Стало:**
- `CHANGELOG.md` - единый файл с историей всех версий
- Хронологический порядок (новые версии сверху)
- Навигация через содержание

**Преимущества:**
- ✅ Вся история изменений в одном месте
- ✅ Удобная навигация
- ✅ Стандартный формат для GitHub
- ✅ Легко добавлять новые версии

---

### 4. Удалены файлы из git индекса 🗑️

Следующие файлы удалены из git tracking (остаются локально, но не в репозитории):

**Исполняемые файлы:**
- `BOMCategorizerSetup.exe` (~50 MB) - слишком большой для GitHub

**Базы данных:**
- `component_database.json` - содержит личные классификации
- `component_database.xlsx` - экспорт БД

**Резервные копии:**
- `database_backups/*` - все резервные копии БД

**Примеры:**
- `example/*` - все файлы примеров (конфиденциальные проекты)

**Временные файлы:**
- `temp_installer/*` - временные файлы сборки
- `temp_for_classification.xlsx` - временный файл классификации

**Python кэши:**
- `bom_categorizer/__pycache__/*` - скомпилированные файлы

**Команда:** `git rm --cached -f <файлы>`

---

### 5. Обновлена документация 📚

**Обновлены файлы:**

#### `ANALYSIS_PROJECT.md`
- Обновлен до версии 3.1.3
- Дата: 07.11.2025
- Добавлены новые функции (база данных, управление)
- Обновлены метрики проекта
- Добавлен раздел "Новые функции в v3.0.0-3.1.3"

#### `docs/PROJECT_STRUCTURE.md`
- Обновлен до версии 3.1.3
- Добавлено описание новых файлов
- Обновлена таблица версий
- Добавлен раздел "Подготовка к GitHub"

#### `TESTING_README.md`
- Обновлен до версии 1.3
- Добавлены предупреждения о папке `example/`
- Инструкции по локальному созданию папки для тестирования
- Объяснение почему `example/` исключена из Git

#### Созданы новые документы:

**`README_GITHUB.md`**
- Пошаговая инструкция по публикации на GitHub
- 7 шагов: от проверки до создания Release
- Команды для git remote, push, tag

**`GITHUB_READY.md`** *(будет удален)*
- Чек-лист готовности к публикации
- Статистика изменений
- Инструкции по финальной проверке

---

## 🔒 Безопасность

### Что защищено:

✅ **Пароль:** Изменен на дефолтный демонстрационный `1234`  
✅ **База данных:** НЕ попадет в репозиторий (`.gitignore`)  
✅ **Резервные копии:** НЕ попадут в репозиторий  
✅ **Примеры файлов:** НЕ попадут в репозиторий (могут содержать конфиденциальные проекты)  
✅ **Временные файлы:** НЕ попадут в репозиторий  
✅ **Исполняемые файлы:** НЕ попадут в репозиторий (будут в Releases)  
✅ **Python кэши:** НЕ попадут в репозиторий  

### Что остается в репозитории:

✅ **ФИО разработчика:** Куреин М.Н. (публичная информация)  
✅ **Версия и дата:** 3.1.3, 07.11.2025  
✅ **Исходный код:** Все модули Python  
✅ **Документация:** Все MD файлы  
✅ **Конфигурация:** `config.json` с дефолтными настройками  

---

## 📊 Статистика изменений

### Удалено из репозитория:
- **Файлы БД:** 3 файла (~50 KB)
- **Примеры:** ~17 файлов (~10 MB)
- **Временные файлы:** ~67 файлов (~15 MB)
- **Executable:** 1 файл (~50 MB)
- **Python кэши:** ~22 файла (~5 MB)
- **Итого:** ~110 файлов, ~80 MB

### Остается в репозитории:
- **Исходный код:** ~30 файлов Python
- **Документация:** ~15 MD файлов
- **Скрипты:** ~20 BAT/PS1 файлов
- **Пакеты офлайн:** 12 wheel файлов (~120 MB)
- **Тесты:** 5 файлов
- **Итого:** ~82 файла, ~120 MB

### Новые/обновленные файлы:
1. `.gitignore` - новый файл
2. `CHANGELOG.md` - объединенный changelog
3. `README_GITHUB.md` - инструкция по публикации
4. `ANALYSIS_PROJECT.md` - обновлен до 3.1.3
5. `docs/PROJECT_STRUCTURE.md` - обновлен до 3.1.3
6. `TESTING_README.md` - обновлен до 1.3
7. `config.json` - PIN изменен на 1234
8. `README.md` - PIN изменен на 1234
9. `bom_categorizer/gui.py` - PIN изменен на 1234
10. `docs/QUICK_START.md` - PIN изменен на 1234
11. И их копии в `temp_installer/`

---

## 🔧 Технические детали

### Изменения в конфигурации

**config.json:**
```json
{
  "app_info": {
    "version": "3.1.3",
    "release_date": "07.11.2025",
    "developer": "Куреин М.Н.",
    "description": "Категоризатор BOM файлов",
    "last_updated": "2025-11-07"
  },
  "security": {
    "pin": "1234",  // ← Изменено с "5421"
    "require_pin": true
  }
}
```

**bom_categorizer/gui.py:**
```python
self.correct_pin = self.cfg.get("security", {}).get("pin", "1234")  # ← Изменен default
```

### Файлы, затронутые изменениями:
- `config.json`
- `temp_installer/config.json`
- `bom_categorizer/gui.py`
- `temp_installer/bom_categorizer/gui.py`
- `README.md`
- `temp_installer/README.md`
- `docs/QUICK_START.md`
- `temp_installer/docs/QUICK_START.md`
- `.gitignore` (новый)
- `CHANGELOG.md` (новый, объединенный)
- `README_GITHUB.md` (новый)
- `ANALYSIS_PROJECT.md` (обновлен)
- `docs/PROJECT_STRUCTURE.md` (обновлен)
- `TESTING_README.md` (обновлен)

---

## 🚀 Публикация на GitHub

### Готово к выполнению:

1. ✅ Личные данные удалены (кроме ФИО разработчика)
2. ✅ Пароль изменен на публичный демо-пароль
3. ✅ `.gitignore` создан и настроен
4. ✅ Конфиденциальные файлы исключены
5. ✅ Документация обновлена
6. ✅ Changelog объединен

### Следующие шаги:

См. подробную инструкцию в **`README_GITHUB.md`**

**Краткая версия:**
```powershell
# 1. Добавить изменения
git add -A

# 2. Коммит
git commit -m "Подготовка к публикации на GitHub v3.1.3"

# 3. Создать репозиторий на GitHub
# https://github.com/new

# 4. Добавить remote
git remote add origin https://github.com/USERNAME/BOMCategorizer.git

# 5. Push
git push -u origin main

# 6. Создать Release с тегом v3.1.3
# Приложить BOMCategorizerSetup.exe
```

---

## 📝 Обновления версий

**Обновлены версии в файлах:**
- `config.json` → `3.1.3`
- `bom_categorizer/__init__.py` → `3.1.3`
- `installer_clean.iss` → `3.1.3`
- `README.md` → `3.1.3`
- `docs/PROJECT_STRUCTURE.md` → `3.1.3`
- `docs/QUICK_START.md` → `3.1.3`

---

## 🧪 Тестирование

### Рекомендуемые проверки:

#### 1. Проверка пароля
```
1. Запустить GUI
2. Попробовать войти с паролем 1234
3. Должно сработать ✅
```

#### 2. Проверка .gitignore
```powershell
# Убедиться что конфиденциальные файлы не в индексе
git ls-files | Select-String "component_database"
git ls-files | Select-String "example/"
# Не должно быть результатов ✅
```

#### 3. Проверка документации
```
1. Открыть CHANGELOG.md
2. Проверить что есть все 3 версии (3.1.3, 3.1.2, 3.0.0)
3. Проверить содержание ✅
```

#### 4. Проверка старого пароля
```powershell
# Не должно быть упоминаний старого пароля
Select-String -Pattern "5421" -Path . -Recurse -Exclude "*.git*","CHANGELOG.md"
# Результатов быть НЕ должно ✅
```

---

## 💡 Для пользователей

### Что изменилось:

**Если обновляетесь с v3.1.2:**
- ⚠️ **Пароль изменен:** Теперь `1234` вместо старого
- ✅ Все остальные функции работают так же
- ✅ База данных сохраняется

**Если устанавливаете впервые:**
- 🔒 Используйте пароль `1234` для входа в GUI
- 📖 Читайте `README.md` для быстрого старта
- 💾 База данных сохраняется в `%APPDATA%\BOMCategorizer\Data\`

---

## 🎯 Приоритет обновления

**НИЗКИЙ** - обновление опционально

Эта версия в основном для:
- ✅ Подготовки проекта к публикации на GitHub
- ✅ Изменения демонстрационного пароля
- ✅ Обновления документации

**Функциональность не изменилась** - все работает так же, как в v3.1.2

---

## 📞 Поддержка

**GitHub репозиторий:** (будет добавлен после публикации)

**Документация:**
- `README.md` - быстрый старт
- `CHANGELOG.md` - история изменений
- `docs/` - подробная документация
- `README_GITHUB.md` - инструкция по публикации

---

## 🎊 Заключение

Версия **3.1.3** — это **релиз подготовки к публикации**. Проект теперь готов к публикации на GitHub с полной защитой конфиденциальных данных.

**Основные достижения:**
- ✅ Защита личных данных
- ✅ Демонстрационный пароль
- ✅ Комплексный .gitignore
- ✅ Обновленная документация
- ✅ Объединенный changelog

**Проект готов к открытой публикации!** 🚀

---

**Дата создания / Created:** 07.11.2025  
**Автор / Author:** Куреин М.Н. / Kurein M.N.  
**Версия документа / Document Version:** 1.0  
**Статус / Status:** ✅ Готово к релизу / Ready for Release

---

---

# Version 3.1.2 - Import from Output & Smart Features

**Дата релиза / Release Date:** 05.11.2025  
**Разработчик / Developer:** Куреин М.Н. / Kurein M.N.

---

## 🎊 Главные нововведения

### ⬇️ Импорт всех компонентов из выходного файла в базу данных

Версия 3.1.2 добавляет **кнопку импорта всех проверенных компонентов** из выходного Excel файла в базу данных одним кликом! Теперь после проверки результата можно сразу добавить все элементы в БД для автоматической классификации в будущем.

### 🎯 Умная генерация имен выходных файлов

Теперь приложение автоматически предлагает правильное имя для выходного файла:
- **Один файл** → `имя_входного_файла_out.xlsx` (в той же папке)
- **Несколько файлов** → `out.xlsx` (в папке первого файла)

### 🖱️ Кликабельное расположение БД в футере

Теперь можно кликнуть по расположению базы данных в футере GUI, чтобы открыть папку с БД в проводнике Windows.

---

## 🆕 Что нового

### 1. Кнопка "⬇️ Добавить все из выходного файла"

**Расположение:** В секции "🗄️ Управление базой данных" (третий ряд, на всю ширину)

**Стиль:** `Accent.TButton` - акцентная синяя кнопка

**Функциональность:**
```python
def on_import_from_output(self):
    """Импорт всех компонентов из выходного файла в базу данных"""
```

#### Что делает:
1. **Проверяет наличие выходного файла**
   - Берет путь из поля `output_xlsx`
   - Показывает ошибку, если файл не найден

2. **Показывает диалог подтверждения**
   ```
   Вы хотите добавить ВСЕ компоненты из файла:
   
   ШСК_М_Kirs_out.xlsx
   
   в базу данных?
   
   Это позволит автоматически классифицировать эти компоненты
   в будущем при обработке других файлов.
   
   Продолжить?
   ```

3. **Создает окно прогресса** (600x400px)
   - Текстовое поле с прокруткой
   - Показывает процесс обработки в реальном времени

4. **Читает все листы категорий**
   - Пропускает служебные листы (`SOURCES`, `SUMMARY`, `Не распределено`, `INFO`)
   - Обрабатывает категории: Резисторы, Конденсаторы, Индуктивности, Полупроводники, Микросхемы, Разъемы, Оптика, СВЧ модули, Кабели, Модули питания, Отладочные платы, Наши разработки, Другие

5. **Добавляет компоненты в базу данных**
   - Ищет колонку "Наименование ИВП" или "Наименование"
   - Пропускает пустые названия
   - Использует функцию `add_component_to_database(name, category_key)`

6. **Показывает статистику**
   ```
   ✅ Импорт завершен!
   
   📈 Статистика:
      Обработано листов: 10
      Добавлено компонентов: 147
      Пропущено (пустые): 3
   
   📊 База данных после импорта:
      Всего компонентов: 289
   ```

7. **Обновляет футер** - автоматически вызывает `self.on_refresh_db_info()`

**Файлы:**
- `bom_categorizer/gui.py` - метод `on_import_from_output()`

**Маппинг русских названий листов:**
```python
SHEET_TO_CATEGORY = {
    'Резисторы': 'resistors',
    'Конденсаторы': 'capacitors',
    'Индуктивности': 'inductors',
    'Полупроводники': 'semiconductors',
    'Микросхемы': 'ics',
    'Разъемы': 'connectors',
    'Оптика': 'optics',
    'СВЧ модули': 'rf_modules',
    'Кабели': 'cables',
    'Модули питания': 'power_modules',
    'Отладочные платы': 'dev_boards',
    'Наши разработки': 'our_developments',
    'Другие': 'others',
}
```

---

### 2. CLI команда `--import-output` в manage_database.py

**Синтаксис:**
```powershell
python manage_database.py --import-output "path/to/output_file.xlsx"
```

**Функциональность:**

#### Новая функция `import_from_output(output_file: str)`

```python
def import_from_output(output_file: str):
    """
    Импортировать компоненты из выходного Excel файла в базу данных
    Читает все листы категорий и добавляет компоненты с их категориями
    """
```

**Процесс:**
1. Проверяет существование файла
2. Читает Excel файл с помощью `pandas.ExcelFile()`
3. Обрабатывает каждый лист категории
4. Ищет колонку с наименованием
5. Добавляет компоненты через `add_component_to_database()`
6. Показывает детальную статистику

**Вывод в консоль:**
```
================================================================================
📥 ИМПОРТ ИЗ ВЫХОДНОГО ФАЙЛА
================================================================================
Файл: example\ШСК_М_Kirs_out.xlsx

📊 Обработка листов:
   ✅ Резисторы: добавлено 45 компонентов
   ✅ Конденсаторы: добавлено 32 компонента
   ✅ Микросхемы: добавлено 18 компонентов
   ...

✅ Импорт завершен!

📈 Статистика:
   Обработано листов: 10
   Добавлено компонентов: 147
   Пропущено (пустые): 3

📊 База данных после импорта:
   Всего компонентов: 289
================================================================================
```

**Исправление кодировки UTF-8:**

Добавлена автоматическая настройка кодировки для корректного отображения русских символов в Windows PowerShell:

```python
# Исправление кодировки для Windows
if sys.stdout.encoding != 'utf-8':
    try:
        # Python 3.7+ - используем reconfigure если доступен
        if hasattr(sys.stdout, 'reconfigure'):
            sys.stdout.reconfigure(encoding='utf-8')
            sys.stderr.reconfigure(encoding='utf-8')
        else:
            # Fallback для старых версий Python
            import codecs
            sys.stdout = codecs.getwriter('utf-8')(sys.stdout.buffer, 'strict')
            sys.stderr = codecs.getwriter('utf-8')(sys.stdout.buffer, 'strict')
    except (AttributeError, OSError):
        # Если не удалось изменить кодировку - продолжаем с текущей
        pass
```

**Зачем нужно:** Решает проблему с отображением эмодзи и русских символов в консоли PowerShell.

**Файлы:**
- `manage_database.py` - функция `import_from_output()`, обновлен `argparse`

---

### 3. Умная генерация имен выходных файлов

**Проблема:** Раньше всегда использовалось `out.xlsx` в корне проекта

**Решение:** Автоматическая генерация правильного имени на основе входных файлов

#### Новый метод `update_output_filename()`

```python
def update_output_filename(self):
    """
    Обновить имя выходного файла в зависимости от количества входных файлов:
    - 1 файл: имя_файла_out.xlsx (в той же папке)
    - Несколько файлов: out.xlsx (в папке первого файла)
    """
```

**Логика:**

1. **Один входной файл:**
   ```python
   # Пример: "C:\Project\example\ШСК_М_Kirs.xlsx"
   # Результат: "C:\Project\example\ШСК_М_Kirs_out.xlsx"
   
   base_name = os.path.splitext(os.path.basename(input_file))[0]
   directory = os.path.dirname(input_file)
   output_name = f"{base_name}_out.xlsx"
   output_path = os.path.join(directory, output_name)
   ```

2. **Несколько входных файлов:**
   ```python
   # Пример: ["file1.xlsx", "file2.docx", "file3.txt"]
   # Результат: "C:\Project\example\out.xlsx" (папка первого файла)
   
   directory = os.path.dirname(input_files[0])
   output_path = os.path.join(directory, "out.xlsx")
   ```

3. **Нет входных файлов:**
   ```python
   # Результат: "C:\Project\ProjectSnabjenie\out.xlsx" (корень проекта)
   output_path = os.path.join(os.getcwd(), "out.xlsx")
   ```

**Автоматический вызов:**
- При добавлении файлов (`on_add_files`)
- При очистке списка файлов (`on_clear_files`)
- После конвертации DOC → DOCX (замена в списке)

**Файлы:**
- `bom_categorizer/gui.py` - метод `update_output_filename()`, вызовы в `on_add_files()`, `on_clear_files()`

---

### 4. Кликабельное расположение БД в футере

**Что было:** Текст с расположением базы данных в футере (только для чтения)

**Что стало:** Кликабельная метка, открывающая папку с БД

#### Изменения в `_create_footer()`

```python
# Кликабельная метка для открытия папки
location_label = tk.Label(db_info_frame,
        text=location,
        font=("Arial", 9, "bold"),
        fg=location_color,
        cursor="hand2")  # ← Курсор-рука при наведении
location_label.pack(side=tk.LEFT)

# Привязываем клик к открытию папки
location_label.bind("<Button-1>", lambda e: self.on_open_db_folder_from_footer())
```

#### Новый метод `on_open_db_folder_from_footer()`

```python
def on_open_db_folder_from_footer(self):
    """Открыть папку с базой данных (из футера, без сообщений)"""
    try:
        db_path = get_database_path()
        db_folder = os.path.dirname(db_path)
        
        if sys.platform == "win32":
            os.startfile(db_folder)
        elif sys.platform == "darwin":
            subprocess.run(["open", db_folder])
        else:
            subprocess.run(["xdg-open", db_folder])
            
    except Exception as e:
        # Тихо игнорируем ошибки при клике на футер
        pass
```

**Отличие от `on_open_db_folder()`:**
- **НЕ показывает** сообщение об успехе
- **НЕ показывает** ошибки (тихий отказ)
- Только открывает папку

**Визуальная индикация:**
- Курсор меняется на "руку" при наведении
- Цвет: синий для установки, оранжевый для проекта
- Жирный шрифт

**Файлы:**
- `bom_categorizer/gui.py` - методы `_create_footer()`, `on_open_db_folder_from_footer()`

---

### 5. Новые функции в component_database.py

#### Функция `backup_database()`

```python
def backup_database() -> str:
    """
    Создает резервную копию базы данных
    
    Returns:
        str: Путь к созданному файлу резервной копии
    
    Raises:
        Exception: При ошибке создания резервной копии
    """
```

**Что делает:**
1. Получает путь к текущей БД
2. Создает папку `database_backups` рядом с БД
3. Генерирует имя файла с временной меткой: `component_database_backup_YYYYMMDD_HHMMSS.json`
4. Копирует файл БД в папку бэкапов
5. Возвращает путь к созданной копии

**Пример:**
```python
backup_path = backup_database()
# Возвращает: "C:\Users\...\AppData\Roaming\BOMCategorizer\Data\database_backups\component_database_backup_20251105_143022.json"
```

**Использование:**
- Кнопка "💾 Резервная копия" в GUI
- CLI команда `python manage_database.py --backup`

#### Изменения в `import_database_from_excel()`

**Было:**
```python
def import_database_from_excel(input_path: str, merge: bool = True) -> bool:
```

**Стало:**
```python
def import_database_from_excel(input_path: str, replace: bool = False) -> int:
```

**Изменения:**
1. **Параметр `merge` → `replace`**
   - `replace=False` - объединить (добавить новые)
   - `replace=True` - заменить (полная замена)
   - Более понятная семантика

2. **Возвращаемое значение `bool` → `int`**
   - Теперь возвращает количество импортированных компонентов
   - Позволяет показать точную статистику

**Файлы:**
- `bom_categorizer/component_database.py` - функции `backup_database()`, `import_database_from_excel()`

---

## 🐛 Исправления багов

### 1. Пустая колонка "ТУ" для категории "Наши разработки"

**Проблема:** В листе "Наши разработки" в колонке "ТУ" показывались артикулы вместо пустых значений

**Решение:** Добавлена очистка `tu_code` для этой категории

```python
# В bom_categorizer/excel_writer.py, функция format_excel_output()

# ВАЖНО: Для категории "Наши разработки" очищаем ТУ (там не нужны производители)
if sheet_name == "Наши разработки":
    tu_code = ""
```

**Файлы:**
- `bom_categorizer/excel_writer.py` - функция `format_excel_output()`

**Тестирование:** 
```
Было: 
Наши разработки | ТУ
195-040        | АМФИ.123456.789ТУ

Стало:
Наши разработки | ТУ
195-040        | (пусто)
```

---

### 2. Очистка тегов замен/подборов в листе SOURCES

**Проблема:** В листе `SOURCES` в колонке `source_file` отображались теги типа:
- `file.xlsx (п/б D3*)`
- `file.xlsx (зам D4)`
- `file.xlsx (п/б D3*), (п/б D5*)`

**Решение:** Удаление ВСЕХ тегов в скобках из имен файлов

```python
# В bom_categorizer/excel_writer.py, генерация листа SOURCES

# Очищаем source_file от тегов в скобках: (зам ...), (п/б ...), (подбор ...)
if source_file:
    # Убираем ВСЕ теги в скобках (может быть несколько)
    import re
    clean_source = source_file
    # Повторяем пока есть скобки (убираем все теги, даже если их несколько)
    while '(' in clean_source:
        prev = clean_source
        clean_source = re.sub(r'\s*\([^)]*\)', '', clean_source)
        # Если ничего не изменилось - выходим (защита от бесконечного цикла)
        if prev == clean_source:
            break
    clean_source = clean_source.strip().rstrip(',').strip()
    unique_sources.add((clean_source, source_sheet))
```

**Зачем `while` цикл:** Один `re.sub()` удаляет только последнюю пару скобок. Цикл гарантирует удаление всех тегов.

**Защита от зависания:** Проверка `if prev == clean_source` предотвращает бесконечный цикл

**Файлы:**
- `bom_categorizer/excel_writer.py` - генерация листа `SOURCES`

**Тестирование:**
```
Было:
file.xlsx (п/б D3*), (п/б D5*), (зам D7)

Стало:
file.xlsx
```

---

### 3. Нормализация допусков резисторов и конденсаторов

**Проблема:** Допуски вида "5Т", "0,1И", "20В" не приводились к стандартному виду

**Решение:** Улучшенные регулярные выражения для нормализации

```python
# В bom_categorizer/formatters.py, функция clean_component_name()

# Для допусков типа "5Т", "0,1И", "20В" и т.д.
# \s+ матчит любые пробельные символы (включая неразрывные пробелы)
text = re.sub(
    r'(Ом|кОм|МОм|пФ|нФ|мкФ|мФ)\s+(\d+(?:[.,]\d+)?)\s*([А-ЯЁа-яёA-Za-z])(?=\s|$|«|"|")',
    r'\1 ±\2% - \3',
    text
)

# Для допусков уже с процентами: "5%Т" → "±5% - Т"
text = re.sub(
    r'(Ом|кОм|МОм|пФ|нФ|мкФ|мФ)\s+(\d+(?:[.,]\d+)?)%\s*([А-ЯЁа-яёA-Za-z])(?=\s|$|«|"|")',
    r'\1 ±\2% - \3',
    text
)

# Для случаев когда уже есть ±: "±5%Т" → "±5% - Т"
text = re.sub(
    r'(±\d+(?:[.,]\d+)?%)\s*([А-ЯЁа-яёA-Za-z])(?=\s|$|«|"|")',
    r'\1 - \2',
    text
)

# Нормализация групп после допуска: "Т  « A »" → "Т - « A »"
# \s+ матчит любые пробельные символы
text = re.sub(
    r'([А-ЯЁа-яёA-Za-z])\s+(«|"|")\s*([^»"]+?)\s*(»|"|")',
    r'\1 - \2\3\4',
    text
)
```

**Улучшения:**
1. **Поддержка десятичных процентов**: `0,1Т` → `±0.1% - Т`
2. **Игнорирование любых пробелов**: `\s+` матчит обычные и неразрывные пробелы
3. **Lookahead для точности**: `(?=\s|$|«|"|")` проверяет что дальше идет разделитель
4. **Нормализация групп**: автоматическое добавление ` - ` между буквой и кавычками

**Файлы:**
- `bom_categorizer/formatters.py` - функция `clean_component_name()`

**Тестирование:**
```
Было:
300 Ом  5Т  « A »
Резистор 1 кОм 0,1И
220 кОм ±5%Т

Стало:
300 Ом ±5% - Т - « A »
Резистор 1 кОм ±0.1% - И
220 кОм ±5% - Т
```

---

### 4. Извлечение ТУ с суффиксами (/02, /Д6)

**Проблема:** ТУ коды с суффиксами извлекались неправильно:
- `дР3.362.029-01ТУ/02` → извлекалось как `дР3.362.029-01ТУ` (без `/02`)
- `1Х3.438.000ТУ/Д6` → извлекалось как `1Х3.438.000ТУ` (без `/Д6`)

**Решение:** Добавлены `/` и буквы в паттерны регулярных выражений

```python
# В bom_categorizer/formatters.py, функция extract_tu_code()

# ВАЖНО: Включаем слэш и буквы после ТУ для захвата суффиксов типа /02, /Д6
tu_patterns = [
    # Буквенно-цифровые коды с точками (начинающиеся с букв или цифр)
    # Добавлено [\d\.\-\/А-ЯЁа-яёA-Za-z]* для захвата /02, /Д6 и т.д.
    (r'([А-ЯЁа-яё]{2,}\.\d+[\d\.\-\/А-ЯЁа-яёA-Za-z]*\s*ТУ[\d\.\-\/А-ЯЁа-яёA-Za-z]*)', re.IGNORECASE),        # АЛЯР.434110.005ТУ, дР3.362.029-01ТУ/02
    (r'([А-ЯЁа-яё]{1,2}\d+\.\d+[\d\.\-\/А-ЯЁа-яёA-Za-z]*\s*ТУ[\d\.\-\/А-ЯЁа-яёA-Za-z]*)', re.IGNORECASE),    # И93.456.000ТУ/02
    (r'(\d+[А-ЯЁа-яё]+\d+\.\d+[\d\.\-\/А-ЯЁа-яёA-Za-z]*\s*ТУ[\d\.\-\/А-ЯЁа-яёA-Za-z]*)', re.IGNORECASE),     # 1Х3.438.000ТУ/Д6
    (r'([А-ЯЁа-яё]{2,}[\d\.\-\/А-ЯЁа-яёA-Za-z]+\s*ТУ[\d\.\-\/А-ЯЁа-яёA-Za-z]*)', re.IGNORECASE),             # ШКАБ434110002ТУ, АЕЯР431200424-07ТУ/02
    (r'(\d+[А-ЯЁа-яё]+[\d\.\-\/А-ЯЁа-яёA-Za-z]+\s*ТУ[\d\.\-\/А-ЯЁа-яёA-Za-z]*)', re.IGNORECASE),             # Цифра+буквы+цифры без первой точки
    # ТУ в начале строки
    (r'ТУ\s+([\d\-\/А-ЯЁа-яёA-Za-z]+)', 0),                                          # ТУ 6329-019-07614320-99/02
]
```

**Изменения в паттернах:**
- Добавлено `\/` (слэш) во все паттерны
- Добавлено `А-ЯЁа-яёA-Za-z` (буквы) после слэша
- Теперь захватываются суффиксы типа `/02`, `/Д6`, `/A1`

**Файлы:**
- `bom_categorizer/formatters.py` - функция `extract_tu_code()`

**Тестирование:**
```
Было:
Диод 2Д522Б дР3.362.029-01ТУ/02
  Наименование: Диод 2Д522Б дР3.362.029-01ТУ/02
  ТУ: дР3.362.029-01ТУ  ❌ (потеряли /02)

Стало:
Диод 2Д522Б дР3.362.029-01ТУ/02
  Наименование: Диод 2Д522Б
  ТУ: дР3.362.029-01ТУ/02  ✅

---

Было:
Диод 2Д522Б дР3.362.029-01ТУ/Д6
  Наименование: Диод 2Д522Б дР3.362.029-01ТУ
  ТУ: дР3.362.029-01ТУ  ❌ (потеряли /Д6)

Стало:
Диод 2Д522Б дР3.362.029-01ТУ/Д6
  Наименование: Диод 2Д522Б
  ТУ: дР3.362.029-01ТУ/Д6  ✅
```

---

## 📝 Обновления документации

### Обновлены до версии 3.1.2:

1. **config.json**
   ```json
   {
     "version": "3.1.2",
     "release_date": "05.11.2025"
   }
   ```

2. **bom_categorizer/__init__.py**
   ```python
   __version__ = "3.1.2"
   ```

3. **installer_clean.iss**
   ```
   #define MyAppVersion "3.1.2"
   ```

4. **README.md**
   - Обновлен номер версии в заголовке
   - Обновлена дата релиза

5. **docs/PROJECT_STRUCTURE.md**
   - Обновлен заголовок "Организация файлов (v3.1.2)"
   - Добавлена версия 3.1.2 в таблицу версий
   - Добавлен раздел "Возможности v3.1.2"
   - Обновлены метаданные внизу документа

6. **docs/QUICK_START.md**
   - Обновлен до v3.1.2
   - Добавлено описание новых функций

7. **docs/OFFLINE_INSTALLER.md**
   - Обновлен до v3.1.2
   - Добавлена история версий

8. **docs/README_DATABASE.md**
   - Обновлен до v3.1.2
   - Добавлены ссылки на новые функции

9. **docs/DATABASE_GUIDE.md**
   - Обновлен до v3.1.2
   - Добавлено описание импорта из выходного файла

10. **docs/DATA_STORAGE.md**
    - Обновлен до v3.1.2

---

## 🔧 Технические детали

### Изменённые файлы:

```
bom_categorizer/gui.py                 # +150 строк (GUI кнопка импорта, умное именование)
bom_categorizer/component_database.py  # +50 строк (backup_database, изменен import)
bom_categorizer/excel_writer.py        # +15 строк (очистка ТУ, очистка SOURCES)
bom_categorizer/formatters.py          # +20 строк (нормализация допусков, извлечение ТУ)
manage_database.py                     # +100 строк (import_from_output, кодировка UTF-8)
config.json                            # версия → 3.1.2
bom_categorizer/__init__.py            # версия → 3.1.2
installer_clean.iss                    # версия → 3.1.2
README.md                              # версия → 3.1.2
docs/PROJECT_STRUCTURE.md              # версия → 3.1.2, новый раздел
docs/QUICK_START.md                    # версия → 3.1.2
docs/OFFLINE_INSTALLER.md              # версия → 3.1.2
docs/README_DATABASE.md                # версия → 3.1.2
docs/DATABASE_GUIDE.md                 # версия → 3.1.2
docs/DATA_STORAGE.md                   # версия → 3.1.2
```

### Новые зависимости:

**Нет новых зависимостей** - все функции используют существующие библиотеки:
- `pandas` - для чтения Excel файлов
- `tkinter` - для GUI
- `os`, `sys`, `subprocess` - стандартная библиотека

---

## 🧪 Тестирование

### Рекомендуемые тесты:

#### 1. Импорт из выходного файла (GUI)
```
1. Обработать любой входной файл
2. Проверить выходной файл
3. Кликнуть "⬇️ Добавить все из выходного файла"
4. Проверить окно прогресса
5. Проверить статистику
6. Кликнуть "📊 Статистика" - проверить что компоненты добавились
```

#### 2. Импорт из выходного файла (CLI)
```powershell
# Windows (PowerShell)
python manage_database.py --import-output "example\ШСК_М_Kirs_out.xlsx"

# Проверить вывод в консоли (должны быть эмодзи и русские символы)
```

#### 3. Умная генерация имен
```
1. Добавить ОДИН файл
   → Проверить что output_xlsx = "путь_к_файлу\имя_файла_out.xlsx"

2. Добавить ЕЩЕ ОДИН файл
   → Проверить что output_xlsx = "путь_к_первому_файлу\out.xlsx"

3. Очистить список
   → Проверить что output_xlsx = "корень_проекта\out.xlsx"
```

#### 4. Кликабельное расположение БД
```
1. Запустить GUI
2. Посмотреть на футер
3. Навести мышь на текст расположения БД (должен стать "hand2")
4. Кликнуть → должна открыться папка в проводнике
```

#### 5. Очистка ТУ для "Наши разработки"
```
1. Обработать файл с нашими разработками
2. Открыть выходной файл
3. Перейти на лист "Наши разработки"
4. Проверить что колонка "ТУ" пустая
```

#### 6. Очистка тегов в SOURCES
```
1. Обработать файл с подборами/заменами
2. Открыть выходной файл
3. Перейти на лист "SOURCES"
4. Проверить что НЕТ тегов типа (п/б D3*), (зам D4)
```

#### 7. Нормализация допусков
```
Входной текст: "300 Ом  5Т  « A »"
Ожидаемый результат: "300 Ом ±5% - Т - « A »"
```

#### 8. Извлечение ТУ с суффиксами
```
Входной текст: "Диод 2Д522Б дР3.362.029-01ТУ/02"
Ожидаемый результат:
  Наименование: "Диод 2Д522Б"
  ТУ: "дР3.362.029-01ТУ/02"
```

---

## 🚀 Как использовать новые функции

### Сценарий 1: Быстрое пополнение базы данных

1. **Обработайте новый файл** через GUI
2. **Проверьте результат** - откройте выходной Excel файл
3. **Убедитесь что классификация правильная**
4. **Кликните "⬇️ Добавить все из выходного файла"**
5. **Готово!** Все компоненты добавлены в базу данных

Теперь при обработке следующих файлов эти компоненты будут автоматически классифицированы правильно!

### Сценарий 2: Массовый импорт через CLI

```powershell
# Обработайте несколько файлов
python split_bom.py file1.docx file2.xlsx file3.txt

# Проверьте результат
# Если все правильно - импортируйте в БД
python manage_database.py --import-output "out.xlsx"
```

### Сценарий 3: Работа с папками БД

1. **В футере GUI** найдите строку с расположением БД
2. **Кликните** по "Проект (разработка)" или "Установка (%APPDATA%)"
3. **Откроется проводник** с папкой базы данных
4. **Можете вручную** скопировать, переместить, проверить файлы

---

## 📊 Статистика изменений

- **Новых функций:** 5
  - Импорт из выходного файла (GUI)
  - Импорт из выходного файла (CLI)
  - Умная генерация имен
  - Кликабельное расположение БД
  - Backup функция в component_database.py

- **Исправлено багов:** 4
  - Пустая колонка ТУ для "Наши разработки"
  - Теги в листе SOURCES
  - Нормализация допусков
  - Извлечение ТУ с суффиксами

- **Обновлено файлов:** 15
- **Добавлено строк кода:** ~335
- **Обновлено документов:** 10

---

## 🎯 Приоритет обновления

**ВЫСОКИЙ** - обязательно обновиться, если:
- Часто обрабатываете новые компоненты
- Хотите быстро пополнять базу данных
- Нужна правильная классификация в будущем

**СРЕДНИЙ** - желательно обновиться, если:
- Используете "Наши разработки"
- Работаете с компонентами с допусками
- Нужны ТУ коды с суффиксами

---

## ⚡ Производительность

**Импорт из выходного файла:**
- Скорость: ~500-1000 компонентов/секунду
- Память: незначительный рост (~10-20 МБ)
- Дисковое пространство: +1-5 КБ на 100 компонентов

**Умная генерация имен:**
- Мгновенно (< 1 мс)
- Без дополнительной нагрузки

---

## 🔐 Безопасность

### Резервное копирование
- Автоматическое создание папки `database_backups`
- Уникальные имена с временными метками
- Защита от перезаписи

### Обработка ошибок
- Проверка существования файлов
- Обработка пустых значений
- Защита от бесконечных циклов (в очистке SOURCES)
- Graceful degradation (тихий отказ при клике на футер)

---

## 📋 Чек-лист для следующей версии

Возможные улучшения для v3.2.0:

- [ ] Фильтрация компонентов при импорте (выбор категорий)
- [ ] Предпросмотр перед импортом (показать что будет добавлено)
- [ ] История импортов (кто, когда, сколько)
- [ ] Экспорт только определённых категорий
- [ ] Поиск дубликатов перед импортом
- [ ] Автоматический импорт после обработки (опция)
- [ ] Горячие клавиши для кнопок БД
- [ ] Drag & Drop для импорта файлов в БД

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте лог** - окно прогресса показывает детальную информацию
2. **Проверьте кодировку** - в PowerShell должны отображаться русские символы
3. **Проверьте путь к БД** - кликните на футер, убедитесь что папка существует
4. **Создайте backup** - кнопка "💾 Резервная копия" перед экспериментами

---

## 🎊 Заключение

Версия **3.1.2** добавляет **критически важную функцию** быстрого пополнения базы данных из готовых результатов! Это значительно ускоряет процесс обучения системы и улучшает качество автоматической классификации.

**Основное преимущество:** Теперь не нужно вручную добавлять каждый компонент - просто обработайте файл, проверьте результат и одним кликом добавьте всё в базу данных!

---

**Дата создания:** 05.11.2025  
**Автор:** Куреин М.Н.  
**Версия документа:** 1.0  
**Статус:** ✅ Готово к релизу

---

---

# Version 3.0.0 - Database Management Edition

**Дата релиза / Release Date:** 01.11.2025  
**Разработчик / Developer:** Куреин М.Н. / Kurein M.N.

---

## 🎊 Главные нововведения

### 🗄️ Полная система управления базой данных компонентов

Версия 3.0.0 представляет **революционное улучшение** в управлении классификацией компонентов через систему базы данных с постоянным хранением и полным набором инструментов управления.

---

## 🆕 Что нового

### 1. Структурированная база данных с метаданными

База данных теперь содержит не только компоненты, но и **метаданные**:

```json
{
  "metadata": {
    "version": "1.0",
    "created": "2025-11-01 12:00:00",
    "last_updated": "2025-11-01 14:30:00",
    "total_components": 42,
    "description": "База данных компонентов BOM Categorizer"
  },
  "components": {
    "Аттенюатор BW-S2W2+": "rf_modules",
    "Резистор С2-23-0.125-2.2 кОм": "resistors",
    ...
  }
}
```

**Файл:** `bom_categorizer/component_database.py`

### 2. Постоянное хранение данных (Data Persistence) 🛡️

База данных теперь хранится **независимо от программы**:

- **Windows:** `%APPDATA%\BOMCategorizer\Data\component_database.json`
- **Режим разработки:** `ProjectSnabjenie\component_database.json`

**Ключевые особенности:**
- ✅ **НЕ удаляется** при деинсталляции программы
- ✅ **Автоматическая миграция** при установке
- ✅ **Маркер установки** (`.installed`) для определения режима работы
- ✅ **Резервные копии** в отдельной папке

**Файлы:** 
- `bom_categorizer/component_database.py` - логика хранения
- `post_install.ps1` - миграция при установке

### 3. Инструмент управления базой данных 🔧

Новый мощный CLI-инструмент `manage_database.py`:

#### Доступные команды:

**Статистика:**
```bash
python manage_database.py --stats
```
Показывает:
- Путь к базе данных
- Версию и дату создания
- Общее количество компонентов
- Распределение по категориям (с русскими названиями)

**Экспорт в Excel:**
```bash
python manage_database.py --export database.xlsx
```
Создает Excel файл с тремя колонками:
- Наименование компонента
- Категория (ID)
- Категория (Название на русском)

**Импорт из Excel:**
```bash
python manage_database.py --import database.xlsx
python manage_database.py --import database.xlsx --replace  # Полная замена
```
Поддержка:
- Объединение с существующей базой (по умолчанию)
- Полная замена базы (`--replace`)
- Валидация категорий
- Пропуск пустых строк

**Резервное копирование:**
```bash
python manage_database.py --backup
```
Создает файл: `component_database_backup_YYYYMMDD_HHMMSS.json`

**Просмотр резервных копий:**
```bash
python manage_database.py --list-backups
```

**Восстановление:**
```bash
python manage_database.py --restore "path/to/backup.json"
```
Автоматически создает резервную копию текущей базы перед восстановлением

**Очистка базы:**
```bash
python manage_database.py --clear
```
Автоматически создает резервную копию перед очисткой

**Файл:** `manage_database.py`

### 4. Удобные BAT файлы для Windows 📁

Созданы BAT файлы для быстрого доступа (двойной клик):

- **`database_stats.bat`** - быстрая статистика
- **`database_backup.bat`** - быстрая резервная копия
- **`database_export.bat`** - быстрый экспорт в Excel
- **`manage_database.bat`** - главное меню (запуск `manage_database.py` без параметров)

**Особенности:**
- Автоматическая активация виртуального окружения
- Правильная кодировка UTF-8 (`chcp 65001`)
- Пауза в конце для просмотра результата

### 5. Интеграция с интерактивной классификацией ✅

**Ключевая функция v3.0.0:** Выбор в интерактивном режиме **запоминается навсегда!**

**Раньше (v2.0.x):**
```
1. Обработали файл → компонент "Аттенюатор X" не распознан
2. Выбрали категорию "СВЧ модули" → создано правило
3. Обработали другой файл → снова тот же компонент
4. ❌ Снова нужно выбирать категорию
```

**Теперь (v3.0.0):**
```
1. Обработали файл → компонент "Аттенюатор X" не распознан
2. Выбрали категорию "СВЧ модули" → сохранено в базу данных
3. Обработали другой файл → тот же компонент
4. ✅ Автоматически классифицирован как "СВЧ модули"!
```

**Приоритеты классификации:**
1. **База данных компонентов** (наивысший) ← НОВОЕ!
2. `rules.json` (правила по паттернам)
3. Автоматическая классификация (по префиксам и ключевым словам)

**Файлы:** 
- `bom_categorizer/gui.py` - интеграция с GUI
- `bom_categorizer/component_database.py` - функция `add_component_to_database()`

### 6. Полная документация по базе данных 📚

Создано **4 новых файла документации** (общий объем ~50 KB):

#### **`docs/README_DATABASE.md`** (3 KB)
📖 **Оглавление** - начните здесь!
- Быстрая навигация по всем файлам
- Рекомендуемый порядок чтения
- Быстрый поиск по задачам

#### **`docs/DATABASE_GUIDE.md`** (18 KB)
**Полное руководство** - всё в одном месте!
- Что нового в v3.0.0
- Быстрый старт
- Структура базы данных
- Инструменты управления (все команды)
- Практические примеры (7 сценариев)
- Обслуживание
- FAQ
- Поддержка

#### **`docs/DATABASE_COMMANDS.md`** (8 KB)
**Откуда запускать команды** - все способы!
- Способ 1: Двойной клик по BAT файлам (самый простой)
- Способ 2: Терминал в папке установки
- Способ 3: Терминал в любом месте (с путями)
- Подробные примеры для каждой команды

#### **`docs/DATA_STORAGE.md`** (24 KB)
**Расположение и сохранность данных** - полная информация!
- Где хранятся данные после установки
- Что происходит при деинсталляции/обновлении
- Резервное копирование (автоматическое и ручное)
- Перенос на другой ПК (3 способа)
- Как найти папку с данными (4 способа)
- Синхронизация между несколькими ПК (облако, сеть)
- Полное удаление данных
- Советы и рекомендации
- FAQ

---

## 🔧 Технические детали

### Изменения в коде

#### `bom_categorizer/component_database.py`
- Добавлена поддержка структурированного формата JSON с метаданными
- Реализована функция `get_database_path()` - определение пути в зависимости от режима
- Реализована функция `load_component_database()` - загрузка с поддержкой старого формата
- Реализована функция `save_component_database()` - автообновление метаданных
- Реализована функция `get_database_stats()` - расширенная статистика
- Реализована функция `export_database_to_excel()` - экспорт в Excel
- Реализована функция `import_database_from_excel()` - импорт из Excel с валидацией
- Добавлен словарь `CATEGORY_NAMES` - русские названия категорий

#### `bom_categorizer/gui.py`
- Интеграция с `add_component_to_database()` в функции `save_classifications_and_rerun()`
- Каждый выбор в интерактивном режиме сохраняется в базу данных

#### `post_install.ps1`
- Создание маркера установки `.installed`
- Создание папки данных `%APPDATA%\BOMCategorizer\Data`
- Автоматическая миграция существующей базы данных
- Создание папки резервных копий `database_backups`
- Информационные сообщения о расположении данных

#### `manage_database.py` (новый файл)
- CLI инструмент с 7 командами
- Исправление кодировки UTF-8 для Windows
- Цветной вывод через emoji
- Табличный формат статистики
- Резервное копирование перед деструктивными операциями

#### BAT файлы (новые)
- `database_stats.bat`
- `database_backup.bat`
- `database_export.bat`
- `manage_database.bat`

### Обновленные файлы

#### Версия и метаданные:
- `config.json` - версия 3.0.0
- `bom_categorizer/__init__.py` - версия 3.0.0
- `installer_clean.iss` - версия 3.0.0

#### Документация:
- `docs/QUICK_START.md` - добавлен раздел по базе данных
- `docs/PROJECT_STRUCTURE.md` - обновлена структура с v3.0.0
- `docs/OFFLINE_INSTALLER.md` - обновлена информация о v3.0.0

---

## 📊 Статистика изменений

### Новые файлы (9):
1. `manage_database.py` (~400 строк)
2. `database_stats.bat`
3. `database_backup.bat`
4. `database_export.bat`
5. `manage_database.bat`
6. `docs/README_DATABASE.md` (~180 строк)
7. `docs/DATABASE_GUIDE.md` (~657 строк)
8. `docs/DATABASE_COMMANDS.md` (~205 строк)
9. `docs/DATA_STORAGE.md` (~660 строк)

### Измененные файлы (10):
1. `config.json` - версия 3.0.0
2. `bom_categorizer/__init__.py` - версия 3.0.0
3. `bom_categorizer/component_database.py` - расширенная функциональность
4. `bom_categorizer/gui.py` - интеграция с БД
5. `post_install.ps1` - миграция и маркер установки
6. `installer_clean.iss` - версия 3.0.0
7. `docs/QUICK_START.md` - раздел по БД
8. `docs/PROJECT_STRUCTURE.md` - структура v3.0.0
9. `docs/OFFLINE_INSTALLER.md` - информация v3.0.0
10. `bom_categorizer/classifiers.py` - приоритет БД в классификации

### Общий объем:
- **Новый код:** ~400 строк Python
- **Новая документация:** ~1700 строк Markdown
- **Обновлено:** ~300 строк в существующих файлах

---

## 🎯 Преимущества для пользователей

### 1. Больше не нужно выбирать повторно ✅
Один раз классифицировали компонент — он запомнился навсегда!

### 2. Данные в безопасности 🛡️
База данных НЕ удаляется при деинсталляции программы.

### 3. Простое управление 📁
Двойной клик по BAT файлам — и всё работает!

### 4. Перенос на другой ПК 🔄
Экспорт → копирование → импорт. Просто и понятно.

### 5. Резервное копирование 💾
Автоматические копии перед каждой деструктивной операцией.

### 6. Полная документация 📚
50+ KB подробной документации на русском языке.

---

## 🚀 Обновление с предыдущих версий

### С версии 2.0.x на 3.0.0:

1. **Запустите новый инсталлятор**
   ```
   BOMCategorizerSetup.exe (v3.0.0)
   ```

2. **Автоматическая миграция**
   - Скрипт `post_install.ps1` автоматически мигрирует существующую базу данных
   - Старая база (если есть) копируется в `%APPDATA%\BOMCategorizer\Data\`

3. **Проверьте миграцию**
   ```
   database_stats.bat
   ```

4. **Создайте резервную копию**
   ```
   database_backup.bat
   ```

### Обратная совместимость:

✅ Старый формат базы данных **полностью поддерживается**
✅ Автоматическая конвертация в новый формат при первом сохранении
✅ Все существующие компоненты сохраняются

---

## 📝 Известные ограничения

1. **Windows Only:** BAT файлы работают только на Windows
   - Linux/Mac: используйте `python manage_database.py` напрямую

2. **UTF-8 кодировка:** Требуется Windows 10+
   - Более старые версии могут показывать иероглифы вместо русского текста

3. **Один пользователь:** База данных не поддерживает одновременную работу
   - Для работы на нескольких ПК используйте экспорт/импорт

---

## 💡 Рекомендации

1. **Создавайте резервные копии еженедельно**
   ```
   database_backup.bat
   ```

2. **Экспортируйте в Excel ежемесячно**
   ```
   database_export.bat
   ```

3. **Храните экспорты в облаке** (Dropbox, OneDrive)

4. **Проверяйте статистику периодически**
   ```
   database_stats.bat
   ```

---

## 🔗 Полезные ссылки

- **Документация по БД:** `docs/README_DATABASE.md`
- **Полное руководство:** `docs/DATABASE_GUIDE.md`
- **Как запускать команды:** `docs/DATABASE_COMMANDS.md`
- **Где данные:** `docs/DATA_STORAGE.md`
- **Быстрая шпаргалка:** `docs/QUICK_START.md`

---

## 🎊 Заключение

Версия 3.0.0 — это **крупнейший релиз** с момента создания проекта!

**Ключевое достижение:** Интерактивная классификация теперь **запоминает выбор навсегда**, что кардинально улучшает пользовательский опыт.

**Что дальше?**
- Синхронизация базы данных через облако (v3.1.0?)
- Веб-интерфейс для управления базой (v3.2.0?)
- Экспорт статистики в PDF (v3.3.0?)

---

**Спасибо за использование BOM Categorizer!** 🎉

*Разработчик: Куреин М.Н.*  
*Дата: 01.11.2025*  
*Версия: 3.0.0 - Database Management Edition*

