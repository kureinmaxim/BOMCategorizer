## ⚡ Быстрый старт (Windows)

### 🚀 Самый простой способ (BAT файлы):

После установки зависимостей, используйте готовые bat-файлы:

```cmd
# Запуск GUI (графический интерфейс с PIN защитой)
start_gui.bat

# Запуск CLI (командная строка)
split_bom.bat --inputs "file.xlsx" --xlsx "output.xlsx" --combine
```

> 🔒 **Новое в v1.7.5:** Графический интерфейс теперь защищен PIN-кодом (5421 по умолчанию)

📘 **Подробнее:** [docs/BAT_FILES.md](docs/BAT_FILES.md) | [docs/QUICK_START.md](docs/QUICK_START.md)

---

### 🔧 Полная установка (PowerShell):

```powershell
# 1) Перейти в папку проекта
cd C:\Project\ProjectSnabjenie

# 2) Создать виртуальное окружение (однажды)
python -m venv .venv

# 3) Активировать окружение (PowerShell)
.\.venv\Scripts\Activate.ps1

# 4) Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt

# 5) Запустить графический интерфейс
python app.py

# 6) (Опционально) Запуск без активации окружения
.\.venv\Scripts\python.exe app.py

# 7) Пример командной строки для разборки
.\.venv\Scripts\python.exe split_bom.py --inputs all_25.xlsx "БЗ.doc" "Докупить в 2025.txt" --sheets 3,4 --xlsx categorized.xlsx --combine --interactive --assign-json rules.json
```

## Быстрый старт (Windows CMD)

```cmd
cd /d C:\Project\ProjectSnabjenie
python -m venv .venv
.venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
python app.py
```

## Быстрый старт (macOS/Linux)

```bash
cd /path/to/ProjectSnabjenie
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python3 app.py
```

## Ключевые опции
- `--inputs` — список входных файлов (XLSX/DOCX/DOC/TXT).
- `--sheets` — номера/имена листов для XLSX (например `3,4`).
- `--xlsx` — путь к выходному XLSX (по умолчанию `categorized.xlsx`).
- `--txt-dir` — папка для создания TXT файлов по категориям (дополнительно к XLSX).
- `--combine` — добавить лист `SUMMARY` с суммарной комплектацией.
- `--interactive` — (устарело) используйте `interactive_classify.py`.
- `--assign-json rules.json` — JSON‑правила автоклассификации; обновляется после интерактива.

## 🚀 Интерактивный режим классификации

Для максимально точной классификации используйте интерактивный режим. Доступны два способа:

### 1. GUI режим (рекомендуется) 🖥️

```bash
# Windows
start_gui.bat
# или
python app.py
```

**Возможности GUI:**
- 🔄 **Конвертация .doc** (НОВОЕ v2.0.10) - автоматическая конвертация старых .doc файлов в .docx через Word или вручную
- ✨ **SUMMARY лист** (НОВОЕ v2.0.10) - жирные заголовки и автоподбор ширины колонок
- 📂 **Разделение по источникам** (v1.8.3) - все категории разделены пустыми строками между файлами
- 🔄 **Перенос компонентов** (v1.8.2) - ручной перенос неправильно классифицированных компонентов в "Не распределено"
- 🔒 **PIN защита** - безопасный доступ к приложению
- 🎯 **Визуальная классификация** - удобный диалог выбора категорий
- 📊 **Автоматическое предложение** - система сама предлагает классифицировать нераспределенные элементы
- 💾 **Автосохранение правил** - все ваши решения сохраняются в `rules.json`
- 🔍 **Умный поиск** - поиск компонентов по частичному совпадению для переноса

### 2. CLI режим (консоль) 💻

```bash
# Windows
.venv\Scripts\python.exe interactive_classify.py --input "example/БЗ.doc"

# macOS/Linux
venv/bin/python3 interactive_classify.py --input "example/БЗ.doc"
```

**Все подробности:** ➡️ **[INTERACTIVE_MODE_GUIDE.md](docs/INTERACTIVE_MODE_GUIDE.md)**

## Список категорий (v1.8.3)
1. **Отладочные платы и модули** - наши разработки, отладочные платы, СВЧ аттенюаторы/делители/ответвители (Qualwave, Mini-Circuits, A-INFO, Quantic PMI), модули связи (NT1, Ebyte)
2. **Микросхемы** - все типы микросхем (улучшенное определение, исключая ложные срабатывания)
3. **Резисторы** - сортировка по номиналу (исправлена в v1.8.2)
4. **Конденсаторы** - сортировка по номиналу (исправлена в v1.8.2)
5. **Индуктивности** (Дроссели) - сортировка по номиналу (исправлена в v1.8.2)
6. **Полупроводники** - диоды, транзисторы, стабилитроны, оптроны
7. **Разъемы** - все типы соединителей, переходы
8. **Оптические компоненты** - оптические модули, передающие/приемные модули, аттенюаторы оптические
9. **Модули питания** - DC-DC, AC-DC преобразователи
10. **Кабели** - провода, кабели, патч-корды
11. **Другие** - предохранители, генераторы, оборудование RITTAL, нагрузки согласованные
12. **Не распределено** - требуют классификации (можно переносить вручную через GUI)

## Пример: только TXT
```powershell
.\.venv\Scripts\python.exe split_bom.py --inputs "Докупить в 2025.txt" --xlsx categorized_from_txt.xlsx --combine --loose
```

## Экспорт в TXT файлы (новое!)
Создание отдельных TXT файлов для каждой категории в удобочитаемом формате:

```powershell
# Базовое использование
.\.venv\Scripts\python.exe split_bom.py --inputs "example/БЗ.doc" --xlsx categorized.xlsx --txt-dir categorized_txt --combine

# С несколькими входными файлами
.\.venv\Scripts\python.exe split_bom.py --inputs "file1.xlsx" "file2.doc" --xlsx output.xlsx --txt-dir output_txt
```

**Что создаётся:**
- Папка с TXT файлами: `Резисторы.txt`, `Конденсаторы.txt`, `Оптические компоненты.txt`, и т.д.
- Каждый файл содержит список компонентов в удобочитаемом формате
- Подробности в файле [TXT_EXPORT_GUIDE.md](docs/TXT_EXPORT_GUIDE.md)

## Проблемы активации PowerShell
Если PowerShell запрещает выполнение скриптов:
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

## Создание инсталлятора (Inno Setup)

### 🚀 Быстрый способ:

```powershell
# Обычная сборка (если окружение уже настроено)
python build_installer.py

# Или полная пересборка с чистым окружением (только при проблемах!)
.\rebuild_venv.ps1
```

**Скрипт `rebuild_venv.ps1` делает всё автоматически:**
- 🔄 Пересоздаёт виртуальное окружение
- 📦 Устанавливает зависимости
- 🚀 Собирает инсталлятор `BOMCategorizerSetup.exe`

### ⚠️ Когда использовать rebuild_venv.ps1:

**НЕ нужно запускать каждый раз!** Используйте только в этих случаях:
- 🆕 **Первая установка** - когда `.venv` еще не создан
- 🔧 **Что-то сломалось** - окружение повреждено, ошибки при импорте
- 📋 **Обновились зависимости** - изменился `requirements.txt`
- 🐛 **Конфликты пакетов** - нужно пересоздать всё с нуля
- 🎯 **Перед релизом** - чистая сборка для распространения

### ✅ Обычная работа (ежедневно):

```powershell
# 1. Активировать окружение (если еще не активно)
.\.venv\Scripts\Activate.ps1

# 2. Собрать инсталлятор
python build_installer.py

# 3. Или запустить приложение для тестирования
python app.py
```

**Требования:**
- Установленный Python 3.10+
- Inno Setup Compiler (`innosetup.com`)

📖 **Подробная инструкция:** [BUILD.md](BUILD.md)

## 📚 Документация

### Для пользователей:
- 📘 [QUICK_START.md](docs/QUICK_START.md) - Подробное руководство для начинающих
- 🚀 [BAT_FILES.md](docs/BAT_FILES.md) - Использование bat-файлов для быстрого запуска (Windows)
- 💻 [CLI_USAGE.md](docs/CLI_USAGE.md) - Использование командной строки
- 🎯 [INTERACTIVE_MODE_GUIDE.md](docs/INTERACTIVE_MODE_GUIDE.md) - Интерактивная классификация компонентов
- 📝 [TXT_EXPORT_GUIDE.md](docs/TXT_EXPORT_GUIDE.md) - Экспорт результатов в текстовые файлы

### Для разработчиков:
- 🔧 [BUILD.md](BUILD.md) - Сборка инсталлятора
- 📦 [OFFLINE_INSTALLER.md](docs/OFFLINE_INSTALLER.md) - Офлайн инсталлятор (v1.1.0+)
- 📁 [PROJECT_STRUCTURE.md](docs/PROJECT_STRUCTURE.md) - Структура проекта
- 🔍 [INSTALL_FIX_SUMMARY.md](docs/INSTALL_FIX_SUMMARY.md) - Решённые проблемы установки

### История изменений:
- 🐛 [FIX_SUMMARY.md](docs/FIX_SUMMARY.md) - Исправленные баги

## 🆕 Что нового в v2.0.13

### 🎯 Улучшенная классификация аттенюаторов (ИСПРАВЛЕНИЕ!)
- **Разделение оптических и СВЧ аттенюаторов** - теперь аттенюаторы правильно классифицируются по типу
  - `Адаптер оптический FC/APC` → **Оптические компоненты** ✅
  - `АТТЕНЮАТОР BW - S2W2+ MINI - CIRCUITS` → **Отладочные платы и модули** ✅
  - `Аттенюатор VAT - 10A+` → **Отладочные платы и модули** ✅
- **Логика классификации:**
  - Аттенюаторы С словом "оптический" → Оптические компоненты
  - Аттенюаторы БЕЗ слова "оптический" → Отладочные платы и модули (СВЧ)

## 🆕 Что нового в v2.0.12

### ➕ Автоматическое добавление знака ± перед процентами
- **Стандартизированный формат допуска** - знак ± автоматически добавляется перед всеми процентами
  - `P1 - 12 - 0,125 - 22 кОм 5% - М` → `P1 - 12 - 0,125 - 22 кОм ± 5% - М` ✅
  - `100 Ом 2%-Т` → `100 Ом ± 2%-Т` ✅
- **Применяется ко ВСЕМ компонентам** - резисторы, конденсаторы, индуктивности с указанием допуска
- **Защита от дублирования** - если знак ± уже есть, повторно не добавляется

### 🔧 Улучшенная нормализация описаний (v2.0.11)
- **Унификация пробелов вокруг дефисов** - автоматическое приведение к единому формату
  - `P1-12-0,1-100` vs `P1 - 12 - 0,1 - 100` → `P1 - 12 - 0,1 - 100` ✅
- **Нормализация пробелов перед процентами** - всегда добавляется пробел между единицами измерения и процентами
  - `100 Ом5%-Т` vs `100 Ом 5%-Т` → `100 Ом 5%-Т` ✅
- **Применяется ко ВСЕМ категориям** - резисторы, конденсаторы, индуктивности, микросхемы и т.д.
- **Работает при флаге --combine** - агрегация теперь всегда выполняется при объединении файлов

**Пример полной обработки:**
```
До:  P1-12-0,1-100 Ом 5%-Т  (4 шт)
     P1-12-0,1-100 Ом5%-Т   (47 шт)
     ↓ (нормализация пробелов + добавление ±)
После: P1 - 12 - 0,1 - 100 Ом ± 5% - Т  (51 шт) ✅
```

## 🆕 Что нового в v1.8.3

### 🔧 Унификация DOCX/XLSX (КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ!)
- **Одинаковая обработка форматов** - DOCX и XLSX обрабатываются по единому алгоритму
- **Извлечение ТУ ПЕРЕД агрегацией** - ТУ-коды извлекаются сразу после парсинга, до объединения
- **Корректное объединение** - одинаковые компоненты из разных форматов правильно объединяются по названию
- **Сохранение производителей** - импортные компоненты с "ф. Производитель" корректно обрабатываются

**Пример работы:**
```
XLSX:  1272ПН3Т + ТУ: АЕЯР.431320.420ТУ  (2 шт)
DOCX:  1272ПН3Т АЕЯР.431320.420ТУ         (3 шт)
       ↓ (ТУ извлекается, названия унифицируются)
Итог:  1272ПН3Т + ТУ: АЕЯР.431320.420ТУ  (5 шт) ✅
```

### 📂 Разделение всех категорий по источникам
- **Пустые строки между файлами** - теперь работает для ВСЕХ категорий (не только отладочных плат)
- **Удобная визуализация** - легко видеть откуда пришёл каждый компонент
- **Сохранение структуры** - компоненты из одного источника остаются вместе

### 🔄 Возможности v1.8.2
- **Перенос компонентов в GUI** - ручной перенос неправильно классифицированных компонентов
- **Умный поиск** - поиск по частичному совпадению для переноса
- **Интеграция с классификацией** - после переноса можно сразу переклассифицировать

### 📊 Исправления и улучшения
- ✅ **Исправлена сортировка по номиналу** - резисторы/конденсаторы теперь сортируются правильно (27 Ом → 220 Ом → 1 кОм → 82.5 кОм)
- ✅ **Улучшено извлечение ТУ** - корректная обработка кодов с дефисами (АЕЯР431200424-07ТУ)
- ✅ **Оптические/СВЧ компоненты** - улучшена классификация Qualwave, Mini-Circuits, A-INFO, Quantic PMI
- ✅ **Увеличено окно GUI** - до 720x750 для размещения новой функциональности
- 🔒 **PIN Защита GUI** - интерфейс защищен PIN-кодом (по умолчанию `5421`)
- 🎯 **Интерактивная классификация** - автоматическое предложение после обработки


