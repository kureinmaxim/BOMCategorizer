# 🚀 BAT файлы для быстрого запуска

## 📁 Доступные файлы

В корне проекта созданы два удобных bat-файла для Windows:

### 1. `split_bom.bat` - CLI обёртка
Запускает скрипт категоризации из командной строки

### 2. `start_gui.bat` - GUI launcher
Запускает графический интерфейс приложения

---

## 💻 Использование

### `split_bom.bat` - Командная строка

#### Простейший запуск:
```cmd
split_bom.bat --inputs "file.xlsx" --xlsx "output.xlsx"
```

#### С экспортом в TXT:
```cmd
split_bom.bat --inputs "БЗ.doc" --xlsx "output.xlsx" --txt-dir "output_txt" --combine
```

#### Несколько файлов:
```cmd
split_bom.bat --inputs "file1.xlsx" "file2.doc" "file3.txt" --xlsx "combined.xlsx" --combine
```

#### Ваш пример (теперь упрощённо):
```cmd
split_bom.bat --inputs "D:/!ШСК_М/Project/Plata_Preobrz.xlsx" --xlsx "D:/!ШСК_М/Project/categorized.xlsx" --txt-dir "D:/!ШСК_М/Project/1_txt" --combine
```

#### Справка:
```cmd
split_bom.bat --help
```

---

### `start_gui.bat` - Графический интерфейс

#### Запуск:
Просто **двойной клик** по файлу `start_gui.bat` или:
```cmd
start_gui.bat
```

Откроется графическое окно приложения BOM Categorizer.

---

## ✨ Преимущества

### ✅ Простота
Не нужно помнить путь к Python и активировать venv

### ✅ Проверка окружения
Автоматически проверяет наличие `.venv` и выводит понятное сообщение об ошибке

### ✅ Короткие команды
```cmd
# Вместо:
.\.venv\Scripts\python.exe split_bom.py --inputs file.xlsx

# Теперь:
split_bom.bat --inputs file.xlsx
```

### ✅ Работает из любой папки
Bat-файлы автоматически переключаются в папку проекта

---

## 🔧 Как это работает

### `split_bom.bat`:
```batch
@echo off
cd /d "%~dp0"                          # Переходим в папку проекта
if not exist ".venv\Scripts\python.exe" (
    # Проверка окружения
    echo [ОШИБКА] Виртуальное окружение не найдено!
    pause
    exit /b 1
)
.venv\Scripts\python.exe split_bom.py %*  # Запуск с переданными аргументами
```

### `start_gui.bat`:
```batch
@echo off
cd /d "%~dp0"                          # Переходим в папку проекта
if not exist ".venv\Scripts\python.exe" (
    # Проверка окружения
    echo [ОШИБКА] Виртуальное окружение не найдено!
    pause
    exit /b 1
)
start "" .venv\Scripts\pythonw.exe app.py  # Запуск GUI в фоне
```

---

## 📝 Примеры использования

### Сценарий 1: Быстрая обработка файла
```cmd
split_bom.bat --inputs "example/БЗ.doc" --xlsx "output.xlsx" --combine
```

### Сценарий 2: Обработка с выбором листов
```cmd
split_bom.bat --inputs "workbook.xlsx" --sheets 3,4,5 --xlsx "output.xlsx"
```

### Сценарий 3: С автоклассификацией
```cmd
split_bom.bat --inputs "БЗ.doc" --xlsx "output.xlsx" --assign-json rules.json
```

### Сценарий 4: Запуск GUI для интерактивной работы
```cmd
start_gui.bat
```

---

## 🚫 Решение проблем

### Ошибка: "Виртуальное окружение не найдено!"

**Причина:** `.venv` не создан или повреждён

**Решение:**
```cmd
python -m venv .venv
.venv\Scripts\pip.exe install -r requirements.txt
```

### Bat-файл не запускается (двойной клик)

**Причина:** Возможно, ассоциации файлов повреждены

**Решение:** Запустите через командную строку:
```cmd
cmd /c split_bom.bat --inputs file.xlsx --xlsx output.xlsx
```

### "python не является внутренней командой"

**Причина:** Python не установлен в системе

**Решение:** Установите Python 3.13+ с [python.org](https://www.python.org/downloads/)

---

## 💡 Советы

### 1. Добавить в PATH (опционально)
Чтобы запускать `split_bom.bat` из любой папки, добавьте папку проекта в PATH:

```cmd
setx PATH "%PATH%;C:\Project\ProjectSnabjenie"
```

После этого:
```cmd
cd D:\MyFiles
split_bom.bat --inputs "file.xlsx" --xlsx "output.xlsx"
```

### 2. Создать ярлык на рабочем столе
Для `start_gui.bat`:
1. ПКМ на `start_gui.bat` → "Создать ярлык"
2. Перетащите ярлык на рабочий стол
3. ПКМ на ярлык → "Свойства" → измените иконку

### 3. Запуск с параметрами через ярлык
1. Создайте ярлык для `split_bom.bat`
2. ПКМ → "Свойства"
3. В поле "Объект" добавьте параметры:
   ```
   C:\Project\ProjectSnabjenie\split_bom.bat --inputs "D:\file.xlsx" --xlsx "D:\output.xlsx"
   ```

---

## 📚 Связанная документация

- 📘 [CLI_USAGE.md](CLI_USAGE.md) - Подробное руководство по CLI
- 📄 [README.md](../README.md) - Главная документация
- 🎯 [QUICK_START.md](QUICK_START.md) - Быстрый старт

---

*Создано: 06.10.2025*  
*Версия: 1.1.1*

