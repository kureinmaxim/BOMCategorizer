# 🚀 Релиз v1.3.1 - Улучшенная классификация и ярлык на рабочем столе

**Дата релиза:** 07.10.2025  
**Разработчик:** Куреин М.Н.

---

## 📋 Краткое описание

Версия 1.3.1 включает улучшения классификации компонентов (особенно разъемов) и добавляет ярлык на рабочий стол для удобного запуска.

---

## ✨ Новые возможности

### 🖥️ Ярлык на рабочем столе
- После установки приложения на рабочем столе автоматически создается ярлык "BOM Categorizer"
- Позволяет быстро запускать приложение одним кликом
- Использует стандартную иконку Windows (папка с документами)

---

## 🔧 Улучшения

### 📊 Улучшенная классификация компонентов

**1. Учет поля `note` при классификации**
- Теперь функция `classify_row` анализирует не только `description`, `value` и `partname`, но и поле `note`
- Это особенно важно для файлов .docx, где производитель/номенклатура может находиться в поле "Примечание"

**2. Разъемы Harting**
- ✅ Добавлено ключевое слово `"harting"` в классификацию разъемов
- Теперь все разъемы Harting (например, с note "p/n: 09 18-514 6-903, ф. Harting") правильно попадают в категорию "Разъемы"

**3. Дополнительные ключевые слова**
- **Конденсаторы**: добавлены `"к10-"`, `"к53-"` для советских обозначений
- **Диоды**: добавлен `"2с630"` для стабилитронов
- **Другие**: добавлены `"предохранитель"`, `"держател"`, `"зажим"`, `"fuzetec"` для вспомогательных компонентов

---

## 🐛 Исправления ошибок

### Синтаксическая ошибка в `split_bom.py`
- Исправлена незавершенная строка кода в функции переименования колонок
- Ошибка возникала при обработке файлов .docx с колонками `description` и `qty`

### Переименование колонок для .docx файлов
- Добавлено автоматическое переименование:
  - `description` → `Наименование ИВП`
  - `qty` → `Кол-во`
- Теперь выходной Excel файл имеет корректные русские названия колонок независимо от входного формата

---

## 📈 Результаты тестирования

### Тест на файле "Плата контроллера.docx"

**До обновления (v1.3.0):**
- Разъемы: 10
- Не распределено: **19** (включая 8 разъемов Harting)

**После обновления (v1.3.1):**
- Разъемы: **18** ✅ (+8 разъемов Harting)
- Другие: 2 (предохранители)
- Не распределено: **9** ✅ (только действительно неизвестные компоненты)

**Улучшение: на 52% меньше нераспределенных элементов!**

---

## 🔄 Изменения в коде

### `split_bom.py`

**1. Сигнатура функции `classify_row`:**
```python
# Было:
def classify_row(ref, description, value, partname, strict, source_file=None):

# Стало:
def classify_row(ref, description, value, partname, strict, source_file=None, note=None):
```

**2. Создание текстового блоба для анализа:**
```python
# Было:
text_blob = " ".join([desc, val, part])

# Стало:
text_blob = " ".join([desc, val, part, note_text])
```

**3. Вызов функции классификации:**
```python
# Добавлено получение и передача note:
note_val = row.get('note') if 'note' in input_df.columns else None
categories_local.append(classify_row(ref, desc, val, part, strict=not args.loose, 
                                     source_file=src_file, note=note_val))
```

**4. Переименование колонок:**
```python
# Добавлено автоматическое переименование для .docx:
if 'description' in result_df.columns and 'Наименование ИВП' not in result_df.columns:
    result_df = result_df.rename(columns={'description': 'Наименование ИВП'})
if 'qty' in result_df.columns and 'Кол-во' not in result_df.columns:
    result_df = result_df.rename(columns={'qty': 'Кол-во'})
```

### `installer_clean.iss`

**Добавлен ярлык на рабочем столе:**
```iss
[Icons]
; ... существующие ярлыки в меню Пуск ...
Name: "{userdesktop}\BOM Categorizer"; Filename: "{app}\run_app.bat"; WorkingDir: "{app}"; IconFilename: "{sys}\shell32.dll"; IconIndex: 2
```

---

## 📚 Обновленная документация

### `docs/QUICK_START.md`
Добавлен раздел **"🚀 Запуск из проекта (Windows)"** с подробными инструкциями:
- Как активировать виртуальное окружение
- Как запускать команды с активацией и без
- Примеры для PowerShell и CMD

---

## 🎯 Для кого этот релиз

Этот релиз полезен для пользователей, которые:
- Работают с файлами .docx (особенно экспорт из Word/САПР)
- Используют разъемы Harting или другие импортные компоненты
- Хотят удобного доступа к приложению через ярлык на рабочем столе
- Разрабатывают/тестируют приложение из исходников

---

## 🔄 Обратная совместимость

✅ **Полная обратная совместимость** с версией 1.3.0:
- Все существующие функции работают без изменений
- Файлы правил (`rules.json`) не требуют обновления
- Старые выходные файлы совместимы с новой версией

---

## 📦 Установка

### Новая установка
1. Скачайте `BOMCategorizerSetup_v1.3.1.exe`
2. Запустите установщик (требуется Python 3.13+ на системе)
3. После установки найдите ярлык "BOM Categorizer" на рабочем столе или в меню Пуск

### Обновление с предыдущей версии
1. Удалите старую версию через "Программы и компоненты"
2. Установите новую версию (ваши правила в `rules.json` не будут затронуты)

---

## 🐛 Известные ограничения

1. **9 нераспределенных элементов** в тестовом файле:
   - Специфические элементы без четких ключевых слов
   - Рекомендуется использовать интерактивный режим классификации

2. **Переименование колонок** применяется только при записи в Excel:
   - Внутренние DataFrame-ы могут использовать оригинальные названия
   - Финальный выходной файл всегда имеет корректные русские названия

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте `debug_log.txt` в папке приложения
2. Убедитесь, что Python 3.13+ установлен
3. Попробуйте переустановить приложение
4. Обратитесь к документации в папке `docs/`

---

## 🎉 Итоги

Версия 1.3.1 - это небольшое, но важное обновление, которое:
- **Улучшает классификацию** на 52% для сложных файлов
- **Повышает удобство использования** благодаря ярлыку на рабочем столе
- **Исправляет критические ошибки** при обработке .docx файлов
- **Сохраняет совместимость** со всеми предыдущими версиями

**Приятной работы с BOM Categorizer v1.3.1!** 🚀

