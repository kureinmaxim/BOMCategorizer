# Release Notes v1.6.1 - Hotfix

**Дата выпуска**: 07.10.2025  
**Разработчик**: Куреин М.Н.

## 🐛 Исправления

### 1. Автоматическая обработка ВСЕХ листов ✨

**Проблема**: Если не указан параметр `--sheets`, обрабатывался только первый лист Excel файла.

**Пример**:
```bash
# Раньше: обрабатывал только первый лист "Заказано в 2025 году" (13 строк)
split_bom --inputs file.xlsx --xlsx output.xlsx

# Результат: 12 элементов вместо 484
```

**Решение**: Теперь если `--sheets` не указан, автоматически читаются **ВСЕ листы** файла.

**После исправления**:
```bash
# Теперь: обрабатывает ВСЕ 6 листов (528 строк)
split_bom --inputs file.xlsx --xlsx output.xlsx

# Результат: 484 элемента правильно распределены
```

### 2. Исправлены TXT файлы 📄

**Проблема**: В TXT файлах не показывалось "Наименование ИВП", только "Код МР".

**Было**:
```
=== КОНДЕНСАТОРЫ ===
1.  | Кол-во: 1 шт | Код МР: 424106091264
2.  | Кол-во: 1 шт | Код МР: 424106260562
```

**Стало**:
```
=== КОНДЕНСАТОРЫ ===
1. Конденсатор GRM1555C1H100G MURATA | Кол-во: 1 шт | Код МР: 619901100204
2. Конденсатор GRM1885C2A100J MURATA | Кол-во: 1 шт | Код МР: 619901901502
```

**Решение**: Улучшена логика поиска альтернативных колонок с описанием:
- Сначала проверяется "Наименование ИВП"
- Затем "description", "наименование мр", "наименование", "имя"
- В крайнем случае используется "Код МР" или "Примечание"

## 📊 Результаты

### Тест на файле `ШСК_М_АМФИ.411734.013.xlsx`:

| Параметр | v1.6.0 (БЕЗ --sheets) | v1.6.1 (БЕЗ --sheets) |
|----------|------------------------|------------------------|
| Обработано листов | 1 | **6** ✅ |
| Обработано строк | 13 | **528** ✅ |
| Элементов найдено | 12 | **484** ✅ |
| TXT с наименованием | ❌ Только код МР | ✅ Полное описание |

## 🔧 Технические изменения

### `split_bom.py`:

1. **Строки 590-644**: Добавлена логика автоматического чтения всех листов
   ```python
   else:
       # Листы не указаны - читаем ВСЕ листы
       all_sheets_data = pd.read_excel(input_path, sheet_name=None, ...)
       for sheet_name, df_local in all_sheets_data.items():
           # Обработка каждого листа отдельно
   ```

2. **Строки 1466-1492**: Улучшена генерация TXT файлов
   ```python
   # Если нет Наименования ИВП, ищем альтернативы
   if pd.isna(name_val) or not str(name_val).strip():
       for alt_col in ['description', 'наименование мр', 'наименование', 'имя']:
           # Проверка альтернативных колонок
   ```

## ⚙️ Как обновить

1. **Если используете GUI**: Просто переустановите новый `BOMCategorizerSetup.exe`
2. **Если используете CLI**: Обновите `split_bom.py`

## 🎯 Обратная совместимость

✅ Полная обратная совместимость с v1.6.0:
- Если `--sheets` указан явно, работает как раньше
- Если не указан, автоматически обрабатывает все листы (новое поведение)
- Все существующие скрипты продолжат работать

## 📝 Примеры использования

### До (v1.6.0):
```bash
# НУЖНО было указывать все листы вручную
split_bom --inputs file.xlsx \
  --sheets "Лист1,Лист2,Лист3,Лист4,Лист5,Лист6" \
  --xlsx output.xlsx --combine
```

### После (v1.6.1):
```bash
# Автоматически обрабатывает ВСЕ листы
split_bom --inputs file.xlsx --xlsx output.xlsx --combine
```

## 🐛 Известные проблемы

Нет известных проблем в этой версии.

---

**Полный список изменений**: См. `CHANGELOG_CATEGORIZATION.md`  
**Предыдущая версия**: `RELEASE_v1.6.0.md`

