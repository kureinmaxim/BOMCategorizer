# ⚡ Быстрые команды для запуска из проекта

## 🚀 Запуск CLI (командная строка)

### Через BAT-файл (рекомендуется):
```cmd
.\split_bom.bat --inputs "путь\к\файлу.xlsx" --xlsx "выходной.xlsx" --combine
```

### Через Python напрямую:
```powershell
.\.venv\Scripts\python.exe split_bom.py --inputs "файл.xlsx" --xlsx "output.xlsx" --combine
```

---

## 🖥️ Запуск GUI (графический интерфейс)

### Через BAT-файл:
```cmd
.\start_gui.bat
```

### Через Python:
```powershell
.\.venv\Scripts\python.exe app.py
```

---

## 📋 Примеры команд

### 1. Обработка одного файла:
```cmd
.\split_bom.bat --inputs "D:/Project/file.xlsx" --xlsx "D:/Project/output.xlsx" --combine
```

### 2. Несколько файлов:
```cmd
.\split_bom.bat --inputs "file1.xlsx" "file2.doc" "file3.txt" --xlsx "combined.xlsx" --combine
```

### 3. С экспортом в TXT:
```cmd
.\split_bom.bat --inputs "file.xlsx" --xlsx "output.xlsx" --txt-dir "output_txt" --combine
```

### 4. С выбором конкретных листов:
```cmd
.\split_bom.bat --inputs "workbook.xlsx" --sheets 3,4,5 --xlsx "output.xlsx"
```

### 5. С автоклассификацией по правилам:
```cmd
.\split_bom.bat --inputs "file.xlsx" --xlsx "output.xlsx" --assign-json rules.json --combine
```

---

## 🔧 Установка и настройка

### Первая установка (если venv не создан):
```powershell
# 1. Создать виртуальное окружение
python -m venv .venv

# 2. Активировать
.\.venv\Scripts\Activate.ps1

# 3. Установить зависимости
pip install -r requirements.txt
```

### Активация окружения (если нужно):
```powershell
# PowerShell
.\.venv\Scripts\Activate.ps1

# CMD
.venv\Scripts\activate.bat
```

---

## 📖 Справка и помощь

### Показать все опции:
```cmd
.\split_bom.bat --help
```

### Проверить версию:
```powershell
.\.venv\Scripts\python.exe -c "import json; print(json.load(open('config.json'))['app_info']['version'])"
```

---

## 🧪 Тестовые команды

### Тест на примере из проекта:
```powershell
.\.venv\Scripts\python.exe split_bom.py --inputs "example/Plata_Preobrz.xlsx" --xlsx "test_output.xlsx" --combine
```

### Тест интерактивного режима:
```powershell
.\.venv\Scripts\python.exe interactive_classify.py --input "example/БЗ.doc"
```

### Просмотр неклассифицированных:
```powershell
.\.venv\Scripts\python.exe preview_unclassified.py --input "example/БЗ.doc"
```

---

## 🐛 Отладка

### Проверить установленные пакеты:
```powershell
.\.venv\Scripts\pip.exe list
```

### Проверить Python версию:
```powershell
.\.venv\Scripts\python.exe --version
```

### Логи приложения:
- **Установка:** `%APPDATA%\BOMCategorizer\install_log.txt` (после установки через инсталлятор)
- **Запуск:** `debug_log.txt` (создаётся в папке проекта при запуске через run_app.bat)

---

## 📚 Дополнительная документация

- 📄 [README.md](README.md) - Главная документация
- 🔧 [BUILD.md](BUILD.md) - Сборка инсталлятора
- 🚀 [docs/BAT_FILES.md](docs/BAT_FILES.md) - Подробно про BAT-файлы
- 💻 [docs/CLI_USAGE.md](docs/CLI_USAGE.md) - Использование командной строки
- 📘 [docs/QUICK_START.md](docs/QUICK_START.md) - Подробное руководство

---

## ⚙️ Часто используемые параметры

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--inputs` | Входные файлы (XLSX/DOCX/DOC/TXT) | `--inputs "file.xlsx"` |
| `--xlsx` | Выходной Excel файл | `--xlsx "output.xlsx"` |
| `--combine` | Создать лист SUMMARY с суммарными данными | `--combine` |
| `--txt-dir` | Папка для TXT файлов по категориям | `--txt-dir "output_txt"` |
| `--sheets` | Номера листов для обработки | `--sheets 3,4,5` |
| `--assign-json` | JSON файл с правилами классификации | `--assign-json rules.json` |
| `--loose` | Более свободные эвристики | `--loose` |

---

## 💡 Подсказки

### Для Windows PowerShell:
- Используйте обратный слеш `\` в путях или прямой `/`
- Пути с пробелами заключайте в кавычки: `"D:/My Files/file.xlsx"`
- Для многострочных команд используйте `` ` `` в конце строки

### Для CMD:
- Используйте обратный слеш `\` в путях
- Для многострочных команд используйте `^` в конце строки

---

*Версия: 1.2.1*  
*Обновлено: 06.10.2025*

