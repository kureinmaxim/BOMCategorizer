# ⚙️ Инструкция по сборке инсталлятора (`.exe`)

Этот документ описывает, как создать установочный файл `BOMCategorizerSetup.exe` для Windows с помощью Inno Setup Compiler.

## 1. Предварительные требования

Для сборки инсталлятора вам понадобится **Inno Setup Compiler**.

- **Скачайте** его с официального сайта: [innosetup.com](https://jrsoftware.org/isinfo.php)
- **Установите** программу. Обычно она устанавливается в `C:\Program Files (x86)\Inno Setup 6\`.

## 2. Ключевые файлы

- **`installer_clean.iss`**: Основной скрипт для сборки инсталлятора. Он использует метод "чистой сборки" — сначала копирует все нужные файлы во временную папку `temp_installer`, а затем создает инсталлятор из неё. Это предотвращает ошибки и гарантирует, что в инсталлятор не попадут лишние файлы.
- **`config.json`**: Файл конфигурации, из которого приложение берет номер версии.
- **`run_app.bat`**: Скрипт, который будет запускаться по ярлыку в меню "Пуск". Он активирует виртуальное окружение и запускает GUI.

## 3. Обновление версии (перед сборкой)

Перед созданием новой версии инсталлятора, не забудьте обновить номер версии в двух местах:

1.  **В файле `config.json`:**
    ```json
    "version": "1.0.5",
    ```

2.  **В файле `installer_clean.iss`:**
    Найдите и измените строку `#define MyAppVersion`:
    ```iss
    #define MyAppVersion "1.0.5"
    ```

## 4. Процесс сборки

**Важно:** Скрипт `installer_clean.iss` использует метод "чистой сборки". Это значит, что перед запуском компилятора нужно сначала скопировать все актуальные файлы проекта во временную папку `temp_installer`.

### Шаг 1: Подготовка (создание и заполнение `temp_installer`)

Откройте PowerShell в папке проекта и выполните следующие команды, чтобы создать папку и скопировать в нее все необходимые файлы:

```powershell
# Создаем временную папку (если ее нет)
New-Item -ItemType Directory -Force -Path "temp_installer"

# Копируем все необходимые файлы
Copy-Item -Path "app.py", "split_bom.py", "config.json", "README.md", "requirements.txt", "BUILD.md", "interactive_classify.py", "interactive_classify_improved.py", "post_install.ps1", "preview_unclassified.py", "rules.json", "run_app.bat" -Destination "temp_installer" -Force

# Копируем папку с документацией
Copy-Item -Path "docs" -Destination "temp_installer\docs" -Recurse -Force

# ✨ Копируем offline_packages для офлайн инсталлятора
Copy-Item -Path "offline_packages" -Destination "temp_installer\offline_packages" -Recurse -Force
```

**📦 Подготовка offline_packages (для офлайн инсталлятора):**

Если директория `offline_packages` не существует или нужно обновить пакеты:

```powershell
# Создать директорию
New-Item -ItemType Directory -Force -Path "offline_packages"

# Скачать все wheel файлы зависимостей для Python 3.13 Windows x64
python -m pip download -r requirements.txt `
    -d offline_packages `
    --only-binary=:all: `
    --platform win_amd64 `
    --python-version 313
```

Это создаст ~38 МБ wheel файлов, которые будут встроены в инсталлятор.

### Шаг 2: Компиляция

После того как папка `temp_installer` готова, запустите сборку одним из способов:

#### Способ A: Через графический интерфейс Inno Setup

1.  Запустите **Inno Setup Compiler**.
2.  Откройте файл `installer_clean.iss` в редакторе (`File -> Open`).
3.  Нажмите **`Build -> Compile`** (или клавишу **F9**).
4.  Дождитесь завершения компиляции.

#### Способ B: Через командную строку (PowerShell)

Этот способ используется в наших автоматических скриптах.

1.  Откройте PowerShell в папке проекта.
2.  Выполните команду:

    ```powershell
    & "C:\Program Files (x86)\Inno Setup 6\iscc.exe" "installer_clean.iss"
    ```
    *(Путь к `iscc.exe` может отличаться, если вы установили Inno Setup в другую директорию).*

## 5. Результат

После успешной компиляции в корневой папке вашего проекта появится файл:

- **`BOMCategorizerSetup.exe`**

Это и есть готовый инсталлятор, который можно передавать пользователям.

---
*Документ создан: 03.10.2025*


