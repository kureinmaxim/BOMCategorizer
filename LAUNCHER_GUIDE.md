# 🚀 Руководство по запуску BOM Categorizer

## 📋 Файлы для запуска

В корне проекта находятся два файла для удобного запуска приложения:

- **`run_modern_debug.bat`** - запуск Modern Edition (PySide6)
- **`run_standard_debug.bat`** - запуск Standard Edition (Tkinter)

---

## ✨ Преимущества использования `.bat` файлов

### 1. **Запуск из любого места**
   - Положите `.bat` файл на **Рабочий стол**
   - Создайте **ярлык** в любой папке
   - Добавьте в **Меню Пуск** или **Панель задач**
   - Приложение всегда запустится корректно, независимо от того, где находится файл

### 2. **Автоматическая настройка окружения**
   - Автоматический переход в папку проекта `C:\Project\BOMCategorizer`
   - Использование правильной виртуальной среды `.venv`
   - Не нужно вручную активировать venv или переходить в папку проекта

### 3. **Режим отладки (Debug Mode)**
   - **Окно консоли остается открытым** - вы видите все логи и ошибки
   - **Полная информация о запуске** - показывает пути и статус
   - **Удобная диагностика** - если что-то пошло не так, вы сразу увидите ошибку
   - **Pause в конце** - окно не закроется автоматически, можно изучить вывод

### 4. **Не требуется PowerShell или терминал**
   - Просто двойной клик по файлу
   - Не нужно вводить команды вручную
   - Не нужно помнить пути и параметры

---

## 🎯 Как использовать

### Вариант 1: Запуск из папки проекта
1. Откройте `C:\Project\BOMCategorizer`
2. Двойной клик по нужному `.bat` файлу
3. Готово! ✅

### Вариант 2: Запуск с Рабочего стола
1. **Скопируйте** `.bat` файл на Рабочий стол
2. Двойной клик по файлу
3. Готово! ✅

### Вариант 3: Создание ярлыка (рекомендуется)
1. Правой кнопкой мыши на `.bat` файл → **Создать ярлык**
2. Переместите ярлык куда угодно (Рабочий стол, Панель задач, Меню Пуск)
3. Можете изменить **иконку** ярлыка для красоты
4. Двойной клик по ярлыку - готово! ✅

### Вариант 4: Запуск через WSL с Alacritty + Zellij (для разработчиков)

Для продвинутой работы с терминалом используйте вашу готовую конфигурацию:

#### Предварительные требования:
- ✅ **Alacritty** установлен в `C:\Program Files\Alacritty\`
- ✅ **Zellij** установлен в WSL
- ✅ Конфигурации настроены в `C:\Project\terminal-configs\windows`
- ✅ Скрипт `wa.bat` скопирован в `C:\Windows\System32\` (для быстрого запуска)

#### Как работает ваша конфигурация:

1. **Запуск Alacritty** автоматически:
   - Запускает WSL
   - Переходит в домашнюю директорию
   - Запускает zsh
   - Выполняет скрипт `~/start-zellij-choose.sh`

2. **Выбор layout** через меню:
   ```
   Выберите workspace layout:
   1) workspacePrjSnabjenie (40% лево / 60% право)
   2) my-workspace (50% / 50%)
   3) Запустить без layout
   ```

#### Быстрый запуск для BOM Categorizer:

**Способ 1: Через проводник Windows**
1. Откройте `C:\Project\BOMCategorizer` в проводнике
2. Кликните в адресную строку
3. Введите `wa` и нажмите Enter
4. Alacritty откроется прямо в папке проекта!
5. Выберите любой layout или нажмите 3 (без layout)

**Способ 2: Через двойной клик**
1. Двойной клик по иконке Alacritty
2. Выберите layout (рекомендуется вариант 3 для работы с проектом)
3. Создайте новый таб: `Ctrl+t` + `n` (назовите "BOM")
4. Перейдите в проект:
   ```bash
   cd /mnt/c/Project/BOMCategorizer
   ```

#### Рабочий процесс в двух табах:

**Таб 1 - Запуск приложения:**
```bash
# Переходим в папку проекта
cd /mnt/c/Project/BOMCategorizer

# Если виртуального окружения для WSL еще нет - создаем:
# python3 -m venv .venv-wsl

# Активируем виртуальное окружение WSL
source .venv-wsl/bin/activate

# Если первый запуск - устанавливаем зависимости (для WSL используем requirements-linux.txt):
# pip install -r requirements-linux.txt

# Запускаем Modern Edition
python3 app_qt.py

# Или для Standard Edition:
# python3 app.py
```

**Таб 2 - Lazyvim для редактирования:**
```bash
# Переходим в папку проекта
cd /mnt/c/Project/BOMCategorizer

# Запускаем Neovim/Lazyvim (откроется с вашей конфигурацией из ~/.config/nvim)
nvim

# Или открываем конкретный файл:
# nvim bom_categorizer/gui_qt.py
# nvim bom_categorizer/component_database.py
```

#### Преимущества вашей конфигурации:
- ✅ **Мгновенный запуск** - `wa` в любой папке
- ✅ **Автоматическая настройка** - Alacritty сразу запускает Zellij
- ✅ **Готовые layouts** - выбор workspacePrjSnabjenie или my-workspace
- ✅ **Навигация Alt+стрелки** - удобное переключение между панелями
- ✅ **Единая конфигурация** - все в `C:\Project\terminal-configs\windows`
- ✅ **Быстрое переключение табов** - `Alt+1`, `Alt+2`, `Alt+3`...

#### Использование готового layout для BOM Categorizer:

В корне проекта находится файл `bom-categorizer.kdl` с готовым layout.

**Способ 1: Прямой запуск из папки проекта**
```bash
# Откройте проводник в C:\Project\BOMCategorizer
# В адресной строке введите: wa
# После запуска Alacritty выполните:
zellij --layout /mnt/c/Project/BOMCategorizer/bom-categorizer.kdl
```

**Способ 2: Копирование в ~/.config/zellij/layouts (рекомендуется)**
```bash
# Скопируйте layout в конфигурацию Zellij
cp /mnt/c/Project/BOMCategorizer/bom-categorizer.kdl ~/.config/zellij/layouts/

# Теперь можно запускать из любой папки:
zellij --layout bom-categorizer
```

**Способ 3: Добавление в меню выбора**

Отредактируйте `~/start-zellij-choose.sh` в WSL и добавьте:

```bash
echo "4) BOM Categorizer (2 таба: App + Lazyvim)"

# В блоке case добавьте:
4)
    echo "Запускаю BOM Categorizer layout..."
    exec zellij --layout "$HOME/.config/zellij/layouts/bom-categorizer.kdl"
    ;;
```

Теперь при запуске Alacritty появится вариант 4 для работы с BOM Categorizer!

#### Горячие клавиши вашей конфигурации:

**Zellij:**
- **`Ctrl+t` + `n`** → Новый таб
- **`Alt+1`, `Alt+2`, `Alt+3`...** → Переключение между табами
- **`Alt+←` `Alt→` `Alt↑` `Alt↓`** → Навигация между панелями
- **`Ctrl+p` + `d`** → Закрыть панель
- **`Ctrl+q`** → Выход

**Alacritty:**
- **`Ctrl+Shift+C`** → Копировать
- **`Ctrl+Shift+V`** → Вставить
- **`Ctrl+Plus/Minus`** → Увеличить/уменьшить шрифт
- **`F11`** → Полноэкранный режим

---

## 📊 Что показывает окно Debug Mode

При запуске вы увидите:

```
====================================
BOM Categorizer Modern Edition
Debug Mode (Console visible)
====================================

Project directory: C:\Project\BOMCategorizer
Current directory: C:\Project\BOMCategorizer
Python executable: .\.venv\Scripts\python.exe

Starting application...
```

### Это означает:
- ✅ **Project directory** - куда файл переключился
- ✅ **Current directory** - подтверждение текущей папки
- ✅ **Python executable** - какой Python используется (из venv)
- ✅ **Starting application...** - приложение запускается

### После закрытия приложения:
```
====================================
Application closed
====================================
Для продолжения нажмите любую клавишу . . .
```

- Окно консоли **остается открытым**
- Можно **изучить логи** работы приложения
- При нажатии любой клавиши - окно закроется

---

## 🔧 Устранение неполадок

### Проблема: "Системе не удается найти указанный путь"

**Причина:** Папка проекта не находится по адресу `C:\Project\BOMCategorizer`

**Решение:**
1. Откройте `.bat` файл в блокноте
2. Измените строку:
   ```batch
   cd /d "C:\Project\BOMCategorizer"
   ```
   На путь к вашей папке проекта, например:
   ```batch
   cd /d "D:\MyProjects\BOMCategorizer"
   ```

### Проблема: "Python executable not found"

**Причина:** Виртуальная среда `.venv` не создана

**Решение:**
1. Откройте PowerShell в папке проекта
2. Выполните:
   ```powershell
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
   pip install -r requirements.txt
   ```

### Проблема: "did not find executable at '/usr/bin\python.exe'" в Windows PowerShell

**Причина:** Виртуальное окружение было создано в WSL, а вы пытаетесь запустить в Windows PowerShell

**Решение для Windows PowerShell:**

1. **Деактивируем текущее окружение:**
   ```powershell
   deactivate
   ```

2. **Удаляем старое .venv (создано в WSL):**
   ```powershell
   Remove-Item -Recurse -Force .venv
   ```

3. **Проверяем Python в Windows:**
   ```powershell
   python --version
   ```

4. **Создаем новое виртуальное окружение для Windows:**
   ```powershell
   python -m venv .venv
   ```

5. **Активируем новое окружение:**
   ```powershell
   .\.venv\Scripts\Activate.ps1
   ```
   
   *Если возникает ошибка политики выполнения, выполните:*
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

6. **Устанавливаем зависимости:**
   ```powershell
   pip install -r requirements.txt
   ```

7. **Запускаем приложение:**
   ```powershell
   python app_qt.py
   ```

**Важно - Виртуальные окружения для разных платформ:** 

В проекте используются **разные виртуальные окружения** для каждой платформы:

| Платформа | Имя окружения | Команда создания | Файл зависимостей |
|-----------|---------------|------------------|-------------------|
| **Windows PowerShell** | `.venv/` | `python -m venv .venv` | `requirements.txt` |
| **macOS** | `venv/` | `python3 -m venv venv` | `requirements-linux.txt` |
| **WSL Linux** | `.venv-wsl/` | `python3 -m venv .venv-wsl` | `requirements-linux.txt` |

⚠️ **Важные замечания:**
- **Не используйте окружение одной платформы в другой!** Это приведет к ошибкам типа "did not find executable".
- **`requirements.txt`** содержит `pywin32` (только для Windows)
- **`requirements-linux.txt`** без `pywin32` (для WSL/Linux/macOS)

✅ **Все три окружения добавлены в `.gitignore`** и не попадут в репозиторий.

💡 **Рекомендация:** Создавайте окружение только для той платформы, которую используете.

### Проблема: "ERROR: No matching distribution found for pywin32" в WSL/Linux/macOS

**Причина:** `pywin32` - это библиотека только для Windows

**Решение:**

В WSL/Linux/macOS используйте `requirements-linux.txt`:
```bash
pip install -r requirements-linux.txt
```

Файл `requirements-linux.txt` содержит все зависимости кроме `pywin32`.

### Проблема: Окно консоли мешает

**Совет:** В будущем можно создать версии без консоли, использующие `pythonw.exe` вместо `python.exe`. Но для отладки рекомендуется использовать текущие версии с консолью.

---

## 💡 Рекомендации

### Для ежедневного использования:
1. Создайте **ярлык** на Рабочем столе
2. Назовите его красиво: "BOM Categorizer Modern" / "BOM Categorizer Classic"
3. Измените **иконку** (можно использовать любую .ico)
4. Закрепите на **Панели задач** для быстрого доступа

### Для разработки:
- Используйте `.bat` файлы для **тестирования** изменений
- **Консоль** покажет все ошибки Python сразу
- Удобно для **отладки** новых функций

### Для пользователей:
- Отправьте пользователю `.bat` файл
- Он сможет запустить приложение **одним кликом**
- Не нужно объяснять про Python, venv, и терминалы

---

## 📝 Технические детали

### Что делает `.bat` файл:

```batch
@echo off                               # Не показывать сами команды
cd /d "C:\Project\BOMCategorizer"       # Перейти в папку проекта
echo ====================================
echo BOM Categorizer Modern Edition
echo Debug Mode (Console visible)
echo ====================================
echo.
echo Project directory: C:\Project\BOMCategorizer
echo Current directory: %CD%            # Показать текущую папку
echo Python executable: .\.venv\Scripts\python.exe
echo.
echo Starting application...
echo.
.\.venv\Scripts\python.exe app_qt.py   # Запустить приложение из venv
echo.
echo ====================================
echo Application closed
echo ====================================
pause                                   # Не закрывать окно автоматически
```

### Почему используется `.venv\Scripts\python.exe`:
- ✅ Использует **изолированное окружение** проекта
- ✅ Все **зависимости** (PySide6, pandas, etc.) установлены в `.venv`
- ✅ Не конфликтует с **системным Python**
- ✅ Гарантирует **корректные версии** библиотек

---

## 🎨 Дополнительные возможности

### Создание красивого ярлыка:

1. **Найдите иконку** (можно скачать .ico файл или использовать системную)
2. **Создайте ярлык** `.bat` файла
3. **Правой кнопкой → Свойства → Сменить значок**
4. **Выберите иконку**

### Добавление в контекстное меню "Отправить":

1. Нажмите `Win + R`, введите `shell:sendto`
2. Скопируйте **ярлык** `.bat` файла в эту папку
3. Теперь можно открывать файлы прямо через "Отправить → BOM Categorizer"

---

## ⚙️ Сравнение версий

| Характеристика | Modern Edition | Standard Edition |
|---------------|----------------|------------------|
| Интерфейс | PySide6 (Qt) | Tkinter |
| Темная тема | ✅ Да | ❌ Нет |
| Светлая тема | ✅ Да | ❌ Нет |
| Современный дизайн | ✅ Да | ⚠️ Базовый |
| Файл запуска | `run_modern_debug.bat` | `run_standard_debug.bat` |
| Python файл | `app_qt.py` | `app.py` |

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль (окно Debug Mode) на наличие ошибок
2. Убедитесь, что путь к проекту правильный
3. Проверьте, что виртуальная среда `.venv` создана
4. Посмотрите логи в приложении (раздел "Лог выполнения")

---

**Создано для проекта BOM Categorizer**  
*Версия: 2024*

