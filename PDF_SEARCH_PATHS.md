# 🔍 Настройка путей поиска PDF по умолчанию

## ✅ Реализовано

Локальный поиск PDF теперь автоматически ищет в следующих местах:

### 🎯 Приоритет поиска

1. **Пользовательские папки** (настраиваются в интерфейсе или через `config_qt.json`)
2. **Папка базы данных** и её подпапки с префиксом `pdf*`
3. **macOS**: `/Users/olgazaharova/Project` + подпапки с префиксом `pdf*`
4. **Windows**: `C:\Project` + подпапки с префиксом `pdf*`

---

### 🛠️ Пользовательские папки (НОВОЕ!)

## ⚡ Быстрый способ: Прямо из окна поиска (рекомендуется)

1. Откройте: **`📄 Поиск PDF`** → **`🔍 Локальный поиск`**
2. В разделе **"📂 Папки для поиска"** вы увидите все активные папки с иконками:
   - **👤** Пользовательские папки (сохраняются в config)
   - **💾** Папка базы данных (системная)
   - **📄** Папки pdf* (системные)
   - **📁** Папки Project (системные)

3. **Управление папками:**
   - **➕ Добавить папку** - добавить свою папку (будет помечена 👤)
   - **➖ Удалить** - удалить выбранную папку из списка
   - **💾 Сохранить в конфиг** - сохранить пользовательские папки в `config_qt.json`
   - **🔄 Сброс** - вернуть список по умолчанию

**ВАЖНО:** В конфиг сохраняются ТОЛЬКО пользовательские папки (👤). Системные папки (💾 📄 📁) используются автоматически и не попадают в `config_qt.json`.

---

## 🔧 Через настройки AI

1. **Меню** → `📄 Поиск PDF` → `⚙️ Настройки AI`
2. Выберите вкладку **`📂 Пути PDF`**
3. Нажмите **`➕ Добавить папку`** и выберите нужную директорию
4. Нажмите **`OK`** для сохранения

---

## 📝 Редактирование `config_qt.json` вручную

Откройте файл `config_qt.json` и добавьте свои пути:

```json
{
  "pdf_search": {
    "custom_directories": [
      "/Users/username/Documents/Datasheets",
      "/Volumes/External/PDF_Archive",
      "C:\\Users\\username\\Desktop\\PDFs"
    ]
  }
}
```

**Внутри пользовательских папок поиск идет рекурсивно по всем вложенным папкам!**

---

### 1️⃣ Папка базы данных
- **Основная папка**: где находится `component_database.json`
- **Подпапки с префиксом `pdf*`**: любой регистр (pdf, PDF, Pdf, pDf и т.д.)
- **Рекурсивный поиск**: внутри всех подпапок

**Примеры:**
```
BOMCategorizer/
├── component_database.json
├── PDF_Datasheets/         ← ищем здесь рекурсивно
│   ├── Capacitors/
│   └── Resistors/
└── pdf-docs/               ← и здесь рекурсивно
    └── old/
```

### 2️⃣ macOS: `/Users/olgazaharova/Project`
- **Подпапки с префиксом `pdf*`**: любой регистр
- **Рекурсивный поиск**: внутри всех вложенных папок

**Примеры:**
```
/Users/olgazaharova/Project/
├── PDF_Archive/            ← ищем здесь рекурсивно
│   ├── 2024/
│   └── 2025/
├── pdfDocs/                ← и здесь рекурсивно
│   └── components/
└── other_folder/           ← НЕ ищем (нет префикса pdf*)
```

### 3️⃣ Windows: `C:\Project`
- **Подпапки с префиксом `pdf*`**: любой регистр
- **Рекурсивный поиск**: внутри всех вложенных папок

**Примеры:**
```
C:\Project\
├── PDF_Library\            ← ищем здесь рекурсивно
│   ├── Chips\
│   └── Modules\
├── PdfFiles\               ← и здесь рекурсивно
│   └── datasheets\
└── documents\              ← НЕ ищем (нет префикса pdf*)
```

---

## 🎯 Логика поиска

### Автоматический поиск (без указания пути):
1. Ищет **во всех** папках по умолчанию одновременно
2. Объединяет результаты из всех источников
3. Удаляет дубликаты (если один файл найден в нескольких местах)
4. Сортирует по релевантности

### Ручное указание пути:
- Если пользователь указывает конкретную папку, поиск идет **только** там
- Рекурсивный обход всех подпапок

---

## 🔠 Регистр букв в названии папок

Префикс `pdf*` распознается **в любом регистре**:

✅ **Найдет:**
- `pdf`
- `PDF`
- `Pdf`
- `pDf`
- `pdF`
- `PdF`
- `pDF`
- `PDf`
- `PDF_Datasheets`
- `pdf-archive`
- `PdfDocs`

❌ **Не найдет:**
- `documents` (нет префикса pdf*)
- `datasheets` (нет префикса pdf*)
- `archived_pdf` (префикс в конце, а не в начале)

---

## 📝 Изменения в коде

### 1. `bom_categorizer/pdf_search.py`

#### Функция `get_default_pdf_directories()`:
- Добавлен поиск подпапок с префиксом `pdf*` в папке БД
- Добавлен поиск в `/Users/olgazaharova/Project` (macOS)
- Добавлен поиск в `C:\Project` (Windows)
- Проверка регистра через `.lower().startswith('pdf')`

#### Метод `LocalPDFSearcher.search()`:
- Улучшена логика рекурсивного обхода
- Если `base_directory` сама начинается с `pdf*`, ищет везде внутри
- Если `base_directory` НЕ начинается с `pdf*`, ищет только в подпапках с префиксом `pdf*`

### 2. `bom_categorizer/pdf_search_dialogs.py`

#### Метод `run_local_search()`:
- Изменено поведение: теперь ищет **во всех** папках по умолчанию, а не только в первой
- Добавлено объединение результатов из всех источников
- Добавлено удаление дубликатов по пути

#### Интерфейс:
- Обновлен placeholder: `"По умолчанию: папки БД, Project/pdf* (macOS), C:\Project\pdf* (Win)"`

---

## 🧪 Как проверить

1. **Запустить приложение**
2. **Открыть меню:** `📄 Поиск PDF` → `🔍 Локальный поиск`
3. **Посмотреть список папок** в разделе "📂 Папки для поиска":
   - Все папки с иконками (👤 💾 📄 📁)
   - Системные папки всегда есть по умолчанию
4. **Добавить свою папку** (опционально):
   - Нажать **➕ Добавить папку**
   - Выбрать нужную директорию
   - Папка появится с иконкой 👤
   - Нажать **💾 Сохранить в конфиг** (для постоянного использования)
5. **Ввести название компонента** (например: "STM32")
6. **Нажать "Найти"** - поиск пройдет во всех папках из списка
7. **Проверить результаты** - должны быть файлы из всех настроенных папок

---

## 💡 Рекомендации для пользователей

### Для максимальной эффективности:
1. **Создайте папки с префиксом `pdf`** в удобных местах:
   - macOS: `/Users/olgazaharova/Project/PDF_Datasheets`
   - Windows: `C:\Project\PDF_Library`
   
2. **Организуйте PDF файлы по категориям** внутри этих папок:
   ```
   PDF_Datasheets/
   ├── Microcontrollers/
   ├── Sensors/
   ├── Power/
   └── Communication/
   ```

3. **Называйте файлы по артикулу** для точного поиска:
   - ✅ `STM32F407VGT6.pdf`
   - ✅ `LM358_datasheet.pdf`
   - ❌ `Document1.pdf` (неинформативное название)

---

## 🔄 История изменений

**Версия 4.4.0** (16.11.2025):
- ✨ **НОВОЕ:** Визуальный список папок с иконками в окне локального поиска
- ✨ **НОВОЕ:** Разделение на пользовательские (👤) и системные (💾 📄 📁) папки
- ✨ **НОВОЕ:** Кнопка "💾 Сохранить в конфиг" прямо в окне поиска
- ✅ Только пользовательские папки сохраняются в `config_qt.json`
- ✅ Системные папки используются автоматически всегда
- ✅ Добавлен поиск в папках с префиксом `pdf*` (любой регистр)
- ✅ Добавлена поддержка `/Users/olgazaharova/Project` (macOS)
- ✅ Добавлена поддержка `C:\Project` (Windows)
- ✅ Реализован одновременный поиск во всех папках
- ✅ Добавлено удаление дубликатов при объединении результатов
- ✅ Улучшена логика рекурсивного обхода папок
- ✅ Новая вкладка "📂 Пути PDF" в настройках для управления путями
- ✅ Тултипы показывают тип папки (пользовательская/системная)

---

## 📚 Связанные документы

- [`docs/TXT_EXPORT_GUIDE.md`](docs/TXT_EXPORT_GUIDE.md) - Экспорт результатов
- [`docs/GUI_DATABASE_INFO.md`](docs/GUI_DATABASE_INFO.md) - Работа с базой данных
- [`README.md`](README.md) - Общая документация

---

**Автор:** BOM Categorizer Team  
**Дата:** 16.11.2025

